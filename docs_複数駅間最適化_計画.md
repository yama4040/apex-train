# 複数駅間の運転曲線最適化 — 計画書（2026-08-19 作成 / 2026-08-20 改訂2）

現行の単一駅間（羽前成田→白兎）DQNを、**東京メトロ東西線 東陽町→木場→門前仲町の3駅・2駅間**へ拡張するための
設計・実装・研究手順をまとめる。
既存の単一区間用スクリプト（`apex2.py` / `environment2.py` / `track.py` / `train.py` など）は**一切変更せず残す**。
複数駅間用は既存をベースにした**新規スクリプト**として作成する。

> **✅ 車両モデルは確定（2026-08-21）**
> **東京メトロ15000系（10両編成）**を採用する。これにより 2026-08-20 に判明していた
> 「現行 `train.py` の車両では標準運転時間を達成できない」問題は解消した（§6.5）。
> 一方で**10両編成＝列車長200m**により、CBTC車間・速度制限の適用範囲に新たな対応が必要になった（§6.6）。

> **改訂履歴**
> - 2026-08-20 改訂1: 対象を東西線 東陽町・木場・門前仲町（下りB線）に確定／勾配符号は進行方向基準で確認／
>   標準運転間隔140秒／後続列車を正式スコープへ
> - 2026-08-20 改訂2: **§6.5 車両性能モデルの不整合**と**§7 目標速度の算出方式**を追加。
>   制限速度・勾配の変化が大きい路線では現行の目標速度算出が破綻することを定量的に確認し、代替方式を設計した
> - 2026-08-21 改訂3: **車両を東京メトロ15000系（10両編成）に確定**（`15000系_車両情報.pdf`・走行抵抗係数は指定値）。
>   標準運転時間の達成可能性を再検証し両区間とも成立を確認（§6.5）。
>   **§6.6 列車長200mの影響**を追加し、§5.4 の `DWELL_MAX` 設計計算を実測ベースで置き換えた
> - 2026-08-21 改訂9: **フェーズ0の実装に着手**。`line_config.py` / `track_multi.py` /
>   `train_multi.py` / `actions_multi.py` / `generate_standard_curve_multi.py` を新規作成し、
>   **東西線の全3駅間で標準運転曲線の生成に成功**（停止位置誤差 0.000〜0.003 m）。
>   ±35‰でB2/P2が真の定速保持を行うことを実走行で確認（§14）
> - 2026-08-21 改訂8: **行動空間を5ノッチへ拡張**（勾配力行P2＝+35‰で定速／勾配ブレーキB2＝−35‰で定速）。
>   §7.4.3(5)で指摘した「急な下りで維持帯が11.6km/hに広がる」問題が直接解消し、帯幅3.3km/h・
>   平均速度+3.8km/hになる。ノッチ変更の加加速度も0.51〜1.54と緩やかにできる（§4.7）。
>   **標準停車時間は30秒のまま**（60秒案は撤回）
> - 2026-08-21 改訂7: **§7.4.3 プロンプト改修方針**を追加。遅延回復モードの基準速度を
>   `speed_limit` から `v_ceiling`（先読み現示パターン）へ変更し、維持帯を固定幅（制限−8〜−2）から
>   **勾配・速度に応じた可変幅**へ改める。現行の固定数値が15000系で成立しないことを実測で確認
>   （幅6km/hでは力行相が3.4秒しか続かずノコギリ判定に該当する）
> - 2026-08-21 改訂6: **ATC現示のモデルを (a) 予見型に決定**（手前から緩やかに減速し、
>   不要な制動なしで制限区間へ進入＝乗り心地の向上）。延伸時に備え **(b) 追従型を切替可能に実装**する方針とし、
>   門前仲町→茅場町で両者を実測比較した（(a) 131.9s / (b) 129.0s・標準140秒に対しどちらも定時可能）（§7.4.1）
> - 2026-08-21 改訂5: **`Tozai_line_speed_limit.csv` は CS-ATC の信号現示が切り替わる位置**であることが判明。
>   先頭が記載位置を越えた時点で現示が変わる＝**先頭位置で判定するのが正しい**（改訂4で入れた
>   「最後尾が抜けるまで適用」は誤りなので撤回）。余裕時分は 東陽町→木場 6.2% → **10.8%** に戻る（§6.2・§6.6）
> - 2026-08-21 改訂4: **制動を 2.5 km/h/s に確定**（3.5は常用最大。一般的な駅停車ブレーキは2.5）。
>   CBTC停止限界の定義を確定（**先行の最後尾から50m手前に自列車の先頭**）。
>   **§9.1 既存手法の保全**を追加 — 既存コード・プロンプト・学習済みモデルを一切壊さず、
>   いつでも実行可能な状態に保つための分離規約を明文化した

---

## 1. 目的

### 1.1 制御上の目標
- 3駅（東陽町A・木場B・門前仲町C）＝2駅間を対象に、先行列車・後続列車の動きに応じた運転曲線を最適化する。
- **中核となる獲得目標**: 先行列車がC駅で長時間停車（急病人救護等）した場合、自列車はB駅を**発車せずに待ち**、
  先行の発車を見届けてからB駅を出発する。これにより駅間（機外）停車を回避する。
- 後続列車がいる場合は、自列車の待機が後続を詰まらせるため、**列車群全体での機外停車を最小化**する判断を学習する（§8.2）。
- **テストケース0（先行・後続なし・遅延なし）では、標準運転曲線に沿って走ることを最良とする**（§7.6）。
- 単一区間で既に獲得済みの目標（省エネ・定時・乗り心地・CBTC遵守・停止精度）は維持する。

### 1.2 研究上の目標
「一駅間ずつ独立に最適化する（現行方式）」と「複数駅間を1エピソードとして最適化する（提案方式）」を
**同一路線・同一シナリオで定量比較**し、後者の優位性（特に機外停車回避と総遅延）を示す。

---

## 2. 結論（推奨構成のサマリ）

| 論点 | 決定 | 根拠 |
|---|---|---|
| 路線・区間 | **東西線 東陽町→木場→門前仲町**（下りB線・964.5m + 1058.0m・標準165秒） | 駅間が短くエピソードが軽い。A→Bが下り／B→Cが登りの対照的な構成（§6.1） |
| **車両モデル** | **東京メトロ15000系・10両編成**（`train_multi.py` で差し替え。`train.py` は温存） | 起動加速度3.3・常用減速度3.5 km/h/s。両区間とも標準運転時間に11〜16%の余裕で成立（§6.5・**確定**） |
| **制動ノッチ** | **2.5 km/h/s を維持**（現行 `train.py` と同値）。CBTCパターンも2.5で構築 | 仕様の3.5は常用最大。駅停車は2.5。制動能力とパターンの前提は必ず一致させる（§6.5） |
| **列車長** | **200m（20m×10両）を明示的にモデル化する** | CBTC停止限界＝先行の最後尾から50m手前に自列車の先頭（＝先行先頭の250m手前）。**速度制限側は列車長を考慮しない**（ATC現示のため先頭判定。§6.6・**新規対応**） |
| **速度制限データ** | `Tozai_line_speed_limit.csv` は**CS-ATCの信号現示の切替位置**。先頭が越えた時点で現示が変わる | 物理的な制限区間そのものではなく、制限に当たらないよう手前で切り替わる設定。列車長の考慮は不要（§6.2） |
| **ATC現示の扱い** | **(a) 予見型を採用**（低下点で既に新現示以下）。**(b) 追従型も切替フラグで実装**しておく | (a)は手前から緩やかに減速でき乗り心地が良い。延伸区間（70→55の低下が2箇所）でも(a)で定時可能だが余裕が5.8%と薄いため(b)を用意（§7.4.1） |
| **遅延回復の基準速度** | `speed_limit` → **`v_ceiling`（先読み現示パターン）** に変更 | 現示が変化する路線では`speed_limit`基準だと低下点で急制動する運転を高評価してしまう（§18・§22の再来）（§7.4.3(4)） |
| **維持帯** | 固定幅（制限−8〜−2）→ **Python算出の可変幅**（`band_lower`〜`band_upper`） | 15000系は加速度が3.7倍高く、幅6km/hでは力行相3.4秒＝ノコギリ判定に該当。必要幅は勾配で変動（§7.4.3(2)(5)） |
| **`T_min`（乗り心地）** | **5秒**（ノコギリ判定のしきい値も7秒→**5秒**に統一） | 7秒だと東陽町→木場が81.0秒となり遅延回復モードでも定時未達。5秒は必要条件（§7.4.3(5)） |
| **既存資産の保全** | 既存のコード・**プロンプト**・学習済みモデルは一切変更せず、いつでも実行可能に保つ | 上書き事故を防ぐ分離規約と回帰確認手順を §9.1 に明文化 |
| **目標速度の算出** | **前方の勾配・制限速度プロファイルを積分する方式へ改める**。閾値は作らない | 現行はスカラー1つを駅まで一定と仮定。東西線では惰行減速度が最大3.7倍ずれる（§7.1〜§7.2） |
| **通常運転の目標** | **標準運転曲線の速度 `v_std(x)` を状態量として与える** | テストケース0で標準曲線に沿わせるため。各モードは「標準曲線に対しどちらへ外すか」で統一的に記述できる（§7.3・§7.6） |
| **制限速度の変化** | **制限低下点を通過点とする先読みパターン `v_limit_pattern(x)`** を算出する | 既存 `cbtc_signal_speed` と同じ逆積分の構造。ad-hocな閾値が不要（§7.4） |
| **勾配による速度低下** | 「何km/h落ちたら加速」という閾値ではなく**惰行到達可能性**で判定する | `generate_forward_train.py` で実装・検証済みのロジック。+29.7‰では自然に「ランプ手前で加速」が出る（§7.5） |
| **下り勾配での惰行** | 惰行加速度が正なら**惰行も禁止**（制動のみ）にする | −16.3‰では惰行しても +0.33〜0.45 km/h/s で加速し、設計メモ §18 の前提が崩れる（§7.7） |
| エピソード単位 | **A駅発車 → B駅停車（発車判断）→ C駅停車 を1エピソード** | 発車判断の帰結（機外停車の有無）はB→C区間でしか観測できず、区間分割すると信用割当が切れる（§3） |
| **行動空間** | **3ノッチ → 5ノッチ**（`P1` 力行 / **`P2` 勾配力行** / `C` 惰行 / **`B2` 勾配ブレーキ** / `B1` 制動） | P2は+35‰、B2は−35‰でちょうど定速。急な下りでの維持帯が11.0→3.3km/hに狭まり平均速度+3.8km/h。ノッチ変更の加加速度も緩やかになる（§4.7） |
| 停車時間の扱い | 停車中も**同一MDPを回し**、行動を「発車／待機」に読み替える | `actions.py`・`forbidden_action`の枠組みを変えずに済む（§4.3） |
| 停車中の行動縮退対策 | 停車中は `coasting` を `forbidden_action` で禁止し、**実質2択（待機＝制動 / 発車＝力行）**にする | 3行動が同一結果になるとmax演算子で過大評価が累積する（設計メモ §26 の再発防止）（§4.3） |
| 停車中の報酬 | フェーズ2は暫定ルール、**フェーズ3でLLM評価に停車判断モードを追加して蒸留** | 「報酬はNN出力のみ」というCLAUDE.mdの制約を守るため（§5） |
| 停車時間の上限 | **最低30秒／上限240秒で強制発車** | 設計計算より D_C=240秒・headway=100秒で必要停車195秒（§5.4）。エピソード無限延長によるQ値発散（設計メモ §29）も防ぐ |
| 標準運転間隔 | **140秒**（ラッシュ時2分20秒） | 実ダイヤ準拠。`STD_DEPARTURE_INTERVAL` を140にする（§8.1） |
| 残り時間・遅延 | 最終駅までの**累積標準ダイヤ**（Σrt ＋ Σ標準停車30秒）を基準に通算 | 遅延が区間をまたいで累積し、遅延回復モードが自然に効く（§4.2） |
| 後続列車 | **フェーズ2.5で環境実装、フェーズ3のLLM再評価に `spacing` モードを同時投入** | LLM評価は高コストなので `hold_at_station` と1回にまとめる（§8.2・§10） |
| 学習アルゴリズム | この機会に **Double DQN 化を併せて導入** | エピソード長が2〜3倍になり、既知の過大評価バイアス（CLAUDE.md「残る副作用」）が悪化するため（§8.3） |

---

## 3. 論点1: 一駅間ずつ最適化 か、複数駅間で1エピソードか

### 3.1 結論：複数駅間を1エピソードとする（ただし段階的に移行する）

### 3.2 根拠
「B駅で発車を待つ」という行動の**良し悪しは、B駅停車中には一切観測できない**。
待った結果が良かったか（＝B→C区間で機外停車せずに済んだか）は、次の駅間を走り切って初めて分かる。

- **一駅間ずつのMDP**では、B駅停車はエピソード境界そのものであり、
  「発車を遅らせる」という行動に対する報酬信号が原理的に存在しない。
  停車時間を外生的に与えるしかなく、獲得目標（§1.1）を学習できない。
- **複数駅間1エピソード**なら、発車判断 → B→C走行 → C駅到着 が同一のリターンに含まれる。
  γ=0.9975（1秒あたり）でも B→C の85秒先までは γ^85 ≈ 0.81 と十分伝播する。

### 3.3 一駅間ずつが有利な点と、その扱い
| 一駅間ずつの利点 | 複数駅間での対処 |
|---|---|
| エピソードが短く学習が安定・サンプル効率が良い | フェーズ1（停車固定30秒）で構造を検証してからフェーズ2へ |
| 区間ごとに標準走行曲線 `sr_*.csv` を用意すれば遅延計算が単純 | 区間ごとの `sr` に**累積時刻オフセット**を足して連結する（§4.2） |
| 過大評価バイアスが溜まりにくい | Double DQN 化＋停車時間の上限で抑える（§8.3） |

### 3.4 したがって、一駅間ずつは「廃止」ではなく「比較用ベースライン」として残す
研究上は、現行 `apex2.py` の成果物を**ベースラインB1**（区間ごとに独立学習し逐次実行）として保持し、
提案手法との比較材料にする（§11）。これが既存スクリプトを残す理由でもある。

---

## 4. MDP設計

### 4.1 エピソードの定義

```
[東陽町 停止] --80s走行--> [木場 停止] --停車(30〜240s・可変)--> [木場 発車] --85s走行--> [門前仲町 停止] = done
   t=0(+自列車遅延)                        ↑ここが発車判断
```

- 開始: A駅（東陽町）に停止した状態（`t = 自列車の出発遅延`）。既存の `start_position_offset` による駅間途中発車も維持する。
- 中間駅B（木場）: 停止窓（−10m〜+5m、0.5km/h以下）に入ったら**到着イベント**として扱う。`done` にはせず、停車フェーズへ遷移する。
- 終了: C駅（門前仲町）の停止窓に入ったら `done`（正常終了）。その他の終了条件は §4.5。
- 一般化: 駅数は設定で可変にする（`n_stations = 3` を既定とし、2（＝現行相当）や4以上も動くようにする）。

### 4.2 状態空間

現行30次元の観測（`environment2.normalized_state`）は「**次駅**までの距離・残り時間」で記述されており、
`arrival_station` を区間ごとに差し替えるだけで**そのままマルチ区間に流用できる**。追加するのは以下。

| # | 特徴量 | 正規化 | 導入 | 目的 |
|---|---|---|---|---|
| 31 | 停車中フラグ | 0/1 | F1 | 走行中と停車中を区別する |
| 32 | 停車経過時間 | min(t_dwell, 240)/240 | F1 | 発車判断のタイミング |
| 33 | 最終駅までの残り距離 | /2.5 km | F1 | 区間をまたいだ先読み |
| 34 | 最終駅までの残り時間（累積ダイヤ基準） | /300 s | F1 | 通算遅延の把握 |
| 35 | 残り駅数 | /3.0 | F1 | 区間位置の識別 |
| 36 | 先行列車の停車経過時間 | min(t, 300)/300 | F1 | 既存 `forward_dwell_elapsed` を観測に昇格。**発車判断の主要入力** |
| 37 | 先行列車が次駅を発車済みか | 0/1 | F1 | 既存 `forward_departed_next` を観測に昇格 |
| 38 | **標準運転曲線の速度 `v_std(x)`** | /80.0 | F1 | **テストケース0で標準曲線に沿わせる基準**（§7.6） |
| 39 | **標準運転曲線との速度差 `v − v_std(x)`** | clip(±20)/20 | F1 | 標準からの逸脱量を直接観測させる |
| 40 | **制限速度の先読みパターン `v_limit_pattern(x)`** | /80.0 | F1 | 制限低下点に間に合う最大速度（§7.4） |
| 41 | 後続列車までの距離 | (clip(d,−0.5,2.0)+0.5)/2.5 | F2.5 | 後続を詰まらせているかの把握 |
| 42 | 後続列車の速度 | /80.0 | F2.5 | 後続の接近速度 |
| 43 | 後続列車の遅延（自列車に起因する分） | min(t,180)/180 | F2.5 | 待機の代償の直接観測 |

**重要（メモリ [[retrain-both-nns-on-feature-change]] との関係）**
- 上表は **QNetworkの観測（`normalized_state`）だけ**の拡張であり、報酬NN／モードNNの入力（`state_info`）とは別系統。
  よってフェーズ1・2では**報酬NNの再学習は不要**。
- 一方 **フェーズ3で停車判断・後続列車・標準運転曲線を報酬NNに学ばせる際は `state_info` を拡張するため、
  評価NN・モードNNをセットで再学習**すること。

**残り時間・遅延の通算化**
```
最終駅までの累積標準ダイヤ = Σ(各区間の rt) + Σ(中間駅の標準停車時間 30秒) = 80 + 30 + 85 = 195秒
次駅の標準到着時刻 T_arr(k) = Σ_{i<k} rt_i + Σ_{i<k} dwell_std_i
remaining_time = T_arr(次駅) − t          （現行の定義を区間累積へ拡張）
current_delay  = max(0, −remaining_time)  （現行どおり）
```
`_scheduled_time_at()` は区間ごとの `sr_*.csv` に累積時刻オフセットを足して連結する。

### 4.3 行動空間と停車中の扱い

走行中は §4.7 の5ノッチをそのまま使う。停車中は次のように読み替える。

| 状況 | `P1` | `P2` | `C` | `B2` | `B1` |
|---|---|---|---|---|---|
| 走行中 | 力行 | 勾配力行 | 惰行 | 勾配制動 | 制動 |
| 停車中 | **発車** | （禁止） | （禁止） | （禁止） | **待機** |

**なぜ停車中に3つ禁止するか**: 停車中は「発車」と「待機」の2つしか意味のある行動がない。
同じ意味の行動が複数並ぶと**完全に同一の結果**を生み、設計メモ §26 のとおり
Q学習の max 演算子が過大評価バイアスを累積させる。**停車中は必ず2択に絞る**。

**停車中の `forbidden_action`**（5要素）
```
t_dwell <  DWELL_MIN (30s)        → P1・P2・C・B2 禁止（B1=待機 のみ）   発車できない
DWELL_MIN ≤ t_dwell < DWELL_MAX   → P2・C・B2 禁止（P1=発車 / B1=待機）  2択
t_dwell ≥ DWELL_MAX (240s)        → P2・C・B2・B1 禁止（P1=発車 のみ）   強制発車
```
※全5行動を禁止すると `masked_qs` が全て −inf になり `argmax` が壊れる。**同時禁止は最大4つまで**。

**停車中の time_step**
`environment2.time_step` は「駅手前100m以内で0.1秒」だが、停車中は `station_remaining_distance ≈ 0` なので
このままだと停車240秒が2400ステップに膨れる。**停車中は専用の `DWELL_TIME_STEP = 1.0秒` を明示的に使う**（要オーバーライド）。
遷移が多すぎてリプレイバッファを圧迫する場合は 2.0〜5.0秒へ粗くする、あるいは
「発車判断を待つセミMDP（1回の待機オプション＝1遷移）」への置き換えを検討する。

### 4.4 報酬設計

**走行中**: 現行と完全に同じ。
```
reward = (llm_reward − 0.5) × (time_step / base_time_step)
```
**駅到着イベント（中間駅Bを含む）**: 現行の終端報酬と同じく、time_stepスケールを外して満額で与える。
```
reward = (llm_reward − 0.5)      # B駅到着でも C駅到着でも同じ
```
これにより中間駅の停止精度にも±1m精度の学習勾配が生まれる。

**停車中**: §5 で詳述する。

**制約の再確認（CLAUDE.md）**: 報酬はLLMを蒸留したNNの出力のみとし、環境側で時間ペナルティや終端ボーナスを足さない。
標準運転曲線 `v_std(x)` も**報酬項ではなく入力特徴量**として与える（§7.6）。
フェーズ2の暫定ルール報酬はこの制約の**一時的な例外**であり、フェーズ3で必ずNNに置き換える。

### 4.5 終了条件

| # | 条件 | 中間駅B（木場） | 終着駅C（門前仲町） |
|---|---|---|---|
| ① | タイムオーバー | — | 累積標準ダイヤ195秒 + 240秒 で `done`（失敗） |
| ② | 停止窓内に停止 | **停車フェーズへ遷移**（`done` にしない）＋到着イベント報酬 | `done`（成功） |
| ③ | 先行への異常接近（40m未満で停止） | `done`（失敗） | `done`（失敗） |
| ④ | オーバーラン（+5m超で走行中） | `done`（失敗） | `done`（失敗） |
| ⑤ | 手前停止（−10m未満で0.5km/h以下。先行手前100m以内は除外） | `done`（失敗） | `done`（失敗） |
| ⑥ | 停滞検出（ちんたら運転） | `done`（失敗） | `done`（失敗） |
| ⑦ | **停車時間上限240秒** | 強制発車（`done` にしない） | — |

**タイムオーバーの余裕幅について**: 現行は「標準運転時間+60秒」だが、複数駅間では**待機が正解**のシナリオがある。
D_C=240・H=100 で必要停車195秒（§5.4）＝標準30秒に対して+165秒なので、+60秒では正解行動が打ち切られてしまう。
**+240秒**に広げる。ただし広げるほどエピソードが長くなりQ値が発散しやすくなるため、
エピソード長の分布を毎サイクル監視すること。

**Q値発散の再発防止（設計メモ §29 の教訓）**
§29では「TASC作動でエピソード終了」にしたことで、**クリープがエピソード終了の回避手段**となり、
エピソードが819歩まで伸びてQ値が16万まで発散した。今回も「停車を続ければ終了が遠のく」という同型の構造がある。

対策は3点セット。
1. **`DWELL_MAX`=240秒 による強制発車**（構造的に無限延長を不可能にする — 最も重要）
2. **①タイムオーバーを累積ダイヤ基準で必ず効かせる**（停車で遅延すればタイムオーバーが近づく）
3. **ゼロ中心化された報酬 + 割引**（良い運転が正の報酬を生むなら、待機はそれを割引で目減りさせる＝待機に自然な機会費用がある）

3.は間接的で弱いので、1.と2.が実質的な防波堤になる。学習中は**エピソード長の分布とmaxQの推移を毎サイクル監視**する。

**停滞検出⑥との干渉**: 現行の停滞検出は「一定時間の前進量」で判定するため、**停車中は必ず無効化**する。
再有効化は発車後（`self._stall_check_t / _stall_check_pos` を発車時にリセット）。

### 4.6 割引率と時間刻み

- エピソード長の見積り: 走行165秒 + 停車30〜240秒 → 実時間 195〜405秒。
  駅手前100mの0.1秒刻みが2駅分入るので **500〜900ステップ**。
- `Actor.gamma` の実時間換算（`gamma**(env.time_step/self.time_step)`）は現行のまま流用できる。
- γ=0.9975（1秒あたり）では 400秒先が 0.368 まで減衰する。
  B駅発車判断（t≈80〜240）からC駅到着（t≈195〜440）までは概ね85〜160秒なので γ^160 ≈ 0.67 と実用範囲。
  余裕を見て **γ=0.999 への引き上げを実験項目**とする。γを上げると過大評価バイアスも増えるため、
  Double DQN 化（§8.3）とセットで評価すること。
- 駅手前100mの0.1秒刻みが**駅の数だけ増える**ため、CLAUDE.md記載の「駅前区間でのブートストラップ過剰」も駅数倍になる。
  これも Double DQN 化の動機。

---

### 4.7 行動空間の拡張 — 5ノッチ化（2026-08-21 決定）

既存の3行動（力行 / 惰行 / 制動）に、**勾配を打ち消す中間ノッチを2つ追加**して5行動にする。

#### 4.7.1 定義

| 記号 | 名称 | 実装 | 勾配 g での加速度 |
|---|---|---|---|
| `P1` | 力行 | 引張力 `TF(v)`（既存） | `(TF(v) − R(v) − g) / FI` |
| **`P2`** | **勾配力行** | 引張力 `min(TF(v), R(v) + 35)` | **`(35 − g) / FI`** |
| `C` | 惰行 | 引張力 0（既存） | `−(R(v) + g) / FI` |
| **`B2`** | **勾配ブレーキ** | 制動ノッチ `(35 − R(v)) / FI` | **`−(35 + g) / FI`** |
| `B1` | 制動 | 制動ノッチ `2.5`（既存） | `−2.5 − (R(v) + g) / FI` |

**P2 は +35‰ で、B2 は −35‰ でちょうど定速になる**（仕様どおり）。
どちらも走行抵抗 `R(v)` を完全に打ち消す形なので、加速度が**勾配だけの関数**になり、
平坦では対称に **±35/FI = ±1.235 km/h/s** となる。定義が単純で対称なのが利点。

> **実装上の必須事項 — P2 の引張力クリップ**
> `R(v) + 35` は速度とともに増えるため、**約85 km/h 以上で P1 の引張力を上回る**（v=85: P1 39.7 / P2 45.3 kg/t）。
> `min(TF(v), R(v)+35)` のクリップを必ず入れること。クリップしないと物理的にありえない引張力になる。
> さらにクリップ後は P1 と P2 が**同一の行動になり**、設計メモ §26 の「同一結果の行動が並ぶと
> max演算子で過大評価が累積する」問題が再発する。ATC現示の上限が75 km/hなので実走行では発生しないが、
> **起動時アサーションで「運用速度域で P2 < P1」を確認**しておく。

#### 4.7.2 5ノッチの加速度表（速度67 km/h・単位 km/h/s）

| 勾配 | `P1` 力行 | `P2` 勾配力行 | `C` 惰行 | `B2` 勾配制動 | `B1` 制動 |
|---|---|---|---|---|---|
| −35.0‰ | +2.975 | +2.470 | +0.961 | **0.000** | −1.539 |
| −16.3‰（東陽町→木場 主部） | +2.315 | +1.810 | +0.301 | −0.660 | −2.199 |
| 0‰ | +1.740 | **+1.235** | −0.274 | **−1.235** | −2.774 |
| +29.7‰（ランプ） | +0.693 | +0.187 | −1.322 | −2.283 | −3.822 |
| +35.0‰ | +0.506 | **0.000** | −1.509 | −2.470 | −4.009 |

#### 4.7.3 効果1 — 維持帯が狭くなる（§7.4.3(5) の問題への直接的な解）

§7.4.3(5) で「急な下り勾配では惰行(+0.30)と制動(−2.2)の非対称が7.3倍あり、
両相を T_min=5秒 以上にすると帯幅が11.6 km/hにもなる」という構造問題を指摘した。
**B2 の追加はこれを直接解消する。**

| 勾配 | 該当区間 | 3ノッチの帯幅 | **5ノッチの帯幅** | 使用ノッチ対 | 平均速度（上端72.4想定） |
|---|---|---|---|---|---|
| −16.3‰ | 東陽町→木場 主部 | 11.0 km/h | **3.3 km/h** | `C` ↔ `B2` | 66.9 → **70.8 km/h** |
| −14.2‰ | 東陽町→木場 序盤 | 11.4 km/h | **3.7 km/h** | `C` ↔ `B2` | — |
| 0‰ | （参考） | 8.7 km/h | **6.2 km/h** | `P2` ↔ `C` | 68.0 → **69.3 km/h** |
| +4.0‰ | 木場→門前仲町 序盤 | 8.0 km/h | **5.5 km/h** | `P2` ↔ `C` | — |
| +29.7‰ | ランプ | 6.6 km/h | 6.6 km/h | `P2` ↔ `C` | 変化なし |

**下り勾配で帯幅が3.3倍狭くなり、平均速度が +3.8 km/h 改善する。**
一方 **+29.7‰ のランプでは改善しない**。惰行の減速（−1.322）が律速側であり、
それより弱い減速手段が無いため。ここは構造的な限界として残る。

#### 4.7.4 効果2 — ノッチ変更の加加速度が小さくなる（乗り心地）

ノッチ変更時の加速度のジャンプ幅 [km/h/s]（平坦・67 km/h）:

| | `P1` | `P2` | `C` | `B2` | `B1` |
|---|---|---|---|---|---|
| `P1` | — | **0.51** | 2.01 | 2.98 | 4.51 |
| `P2` | 0.51 | — | **1.51** | 2.47 | 4.01 |
| `C` | 2.01 | 1.51 | — | **0.96** | 2.50 |
| `B2` | 2.98 | 2.47 | 0.96 | — | **1.54** |
| `B1` | 4.51 | 4.01 | 2.50 | 1.54 | — |

3ノッチでは `P1↔C`(2.01) / `C↔B1`(2.50) / `P1↔B1`(4.51) の3種類しか選べなかったが、
5ノッチでは **0.51〜1.54 の緩やかな移行**が可能になる。**帯幅とは独立した乗り心地の改善**である。

→ **ノコギリ運転の評価をノッチ間の「距離」で重み付けすべき**（§7.4.3 のプロンプト設計に反映）。
現行ルールは全ての反転を同列に扱うが、`P1→P2`(0.51) と `P1→B1`(4.51) を同じ減点にするのは不適切。

#### 4.7.5 効果3 — 遅延回復の実測（維持帯運転のシミュレーション・T_min=5秒）

| 区間 | 標準 | 理論最速 | 3ノッチ | **5ノッチ** | 改善 |
|---|---|---|---|---|---|
| 東陽町 → 木場 | 80 s | 71.3 s | 74.8 s | **73.1 s** | **+1.7 s** |
| 木場 → 門前仲町 | 85 s | 77.3 s | 78.4 s | 78.9 s | −0.5 s |
| 合計 | 165 s | 148.6 s | 153.2 s | **152.0 s** | +1.2 s |

下り区間は予測どおり改善した。登り区間の −0.5 s は単純なヒステリシス制御の挙動差の範囲であり、
帯幅の解析（§4.7.3）では5ノッチが等しいか狭いので、**DQNが学習する方策では悪化しないはず**。
フェーズ1で実測して確認する。

#### 4.7.6 実装への影響

| 箇所 | 変更 |
|---|---|
| `actions_multi.py`（新規） | 行動を5つに定義。既存 `actions.py` は変更しない |
| `model.py` | QNetwork の出力次元 3 → 5。**入力次元と同じく引数化**すれば既存と共用できる |
| `train_multi.py` | `step()` に P2 / B2 の分岐を追加（引張力クリップ必須） |
| `environment_multi.py` | `forbidden_action` を5要素に。同時禁止は**最大4つまで**（全禁止でargmaxが壊れる） |
| `evaluate_csv_with_llm_multi.py` | 5ノッチの記述、`current_notch` / `prev_notch` の文字列、ノコギリ評価の重み付け |
| 報酬NN・モードNN | **5ノッチのデータで再学習が必要**（フェーズ3で実施予定のため追加コストは小さい） |
| `runcurve_plot_multi.py` / `drive_monitor.py` | ノッチ別配色を5色に |

#### 4.7.7 研究上の注意 — ベースライン比較の交絡

既存手法（3ノッチ・単一区間）と提案手法（5ノッチ・複数駅間）を直接比べると、
**「エピソード構造の違い」と「行動空間の違い」が交絡する**。§11.1 の主張は前者についてのものなので、
アブレーションを追加する。

| ID | エピソード | 行動空間 |
|---|---|---|
| B1 | 単一区間 | 3ノッチ |
| **P1a** | 複数駅間 | **3ノッチ** ← フェーズ1・2はこれで進める |
| P1b | 複数駅間 | 5ノッチ |

**フェーズ1・2は3ノッチで構造検証を行い、5ノッチはフェーズ2.6として追加する**（§10）。
こうすれば B1 vs P1a でエピソード構造の効果を、P1a vs P1b で行動空間の効果を分離できる。

---

## 5. 停車中の報酬と、発車判断の因果性

### 5.1 何が問題か
現行の報酬NNは「**走行中のノッチ操作**」を評価するモデルであり、停車中の「待つ／発車する」を評価できない。
- 単純に **0（中立）** にすると、待機に何のコストも無く「とりあえず待つ」が最適になりやすい。
- 環境側で **負のペナルティ** を足すと「報酬はNN出力のみ」という設計制約を破る。かつ待機の学習を潰す。

### 5.2 段階的な解（推奨）

**フェーズ2（構造検証用・暫定ルール報酬）**
停車中だけ、以下のルールで 0〜1 の評価値を作り、走行中と同じくゼロ中心化・time_stepスケールして与える。
LLMプロンプトに書く評価軸の**下書き**を兼ねる。

```
状況判定:
  blocked = (先行が次駅Cに停車中) かつ (先行の停車経過が標準30秒を超過している)
            ＝「今発車するとB→Cで機外停車する見込みが高い」状態
  crowded = (後続列車がB駅手前のCBTC干渉圏に接近している)   ※フェーズ2.5で追加

評価:
  最低停車時間内(30秒)        → 0.5   （行動不能なので中立）
  blocked  かつ 待機          → 0.8   （正しい発車抑止）
  blocked  かつ 発車          → 0.1   （機外停車を招く発車）
  ¬blocked かつ 待機          → 0.2   （不要な延発。遅延だけ増える）
  ¬blocked かつ 発車          → 0.9   （定時発車）
  ※ crowded かつ 待機 は上記から −0.3（後続を詰まらせる代償）  ※フェーズ2.5で追加
```
`blocked` の判定には既存の `forward_clear_remaining_time` / `forward_observed_delay` / `forward_departed_next` を使う。

**フェーズ3（本番・LLM蒸留）**
1. `evaluate_csv_with_llm.py` のプロンプトに **駅停車中の発車判断**の評価軸を追加する。
   `reward_features.MODE_CLASSES` に第5のモード `hold_at_station`（駅停車中の発車判断）を追加し、
   `MODE_CLASSES_ACTIVE` にも入れる。**同時に既存の `spacing`（間隔調整）も ACTIVE 化**する（§8.2）。
2. `state_info` に停車中の状態量・後続列車の状態量・標準運転曲線の速度を追加する。
3. **評価NN・モードNNをセットで再学習**する（[[retrain-both-nns-on-feature-change]]）。
4. `analyze_reward_nn_vs_llm.py` / `analyze_reward_imbalance.py` で停車判断ラベルの分布と校正を確認する。
   停車中のサンプルは走行中に比べて圧倒的に少ないため、**ラベル不均衡の診断は必須**。

### 5.3 因果性の検証（設計メモ §13 の原則）— 実測タイムライン

「先行がC駅で長く停まっている」ことを自列車がB駅停車中に**実際に観測できるか**を計算した。

**前提**（東陽町A→木場B→門前仲町C、rt = 80 / 85 秒、標準停車30秒、標準運転間隔 H=140秒）
```
自列車（遅延0）:  A発 t=0  /  B着 t=80  /  B標準発 t=110  /  C標準着 t=195
先行列車（headway H）:  A発 t=−H  /  B着 t=80−H  /  B発 t=110−H  /  C着 t=195−H  /  C発 t=195−H+D_C
```

| headway H | 先行の遅延 | 先行C着 | 延着が観測される時刻<br>（C着+30秒） | 自列車の標準発車時点(t=110)での<br>先行のC駅停車経過 |
|---|---|---|---|---|
| 140秒（定時） | 0秒 | t=55 | **t=85** | **55秒**（＝延着が明確に観測済み） |
| 120秒 | 20秒 | t=75 | t=105 | 35秒（かろうじて観測済み） |
| 100秒 | 40秒 | t=95 | t=125 | **15秒**（＝まだ延着と分からない） |

**得られた知見**
1. **H=140（先行定時）では因果性が成立する。** 自列車がB駅に在線するのは t=80 以降であり、
   先行の延着は t=85 に観測される。標準発車時刻 t=110 の時点で55秒の超過停車を観測済みなので、
   「先行がC駅で長く停まっている」という判断材料が揃った状態で発車判断ができる。**中核シナリオは学習可能**。
2. **H=100（先行が40秒遅延）では、標準発車時刻の時点でまだ延着が観測できない**（停車経過15秒）。
   自列車は「先行がこの後30秒で発車するか、240秒停まるか分からない」状態で判断を迫られる。
   これは未来情報リークを避ける以上、**本質的に避けられない不確実性**である。
   - 対処案A: 既存の `ASSUMED_DELAYED_DWELL_S` 先読み機構（先行が前駅を遅延発車していれば次駅停車も延びると想定）を流用する。
   - 対処案B（**推奨**）: **待てば情報が増える**という構造をそのまま学習させる。
     観測 #36（先行の停車経過時間）が入っているので、「少し待って先行の様子を見てから決める」という
     情報獲得のための待機を DQN が獲得しうる。強化学習として自然であり、研究上も面白い。
3. したがって **H=140 が最も判断しやすく、H が小さいほど難しい**という自然なカリキュラムになっている。
   学習は H を幅広くランダムに、検証は H ∈ {140, 120, 100} の3点で行う。

**実装**: この検証を自動化する `check_forward_observability.py` を作り、
全シナリオを「発車抑止が正解」「定時発車が正解」「判断困難（観測不足）」の3つに分類して表出力する。

### 5.4 `DWELL_MAX` の設計計算（2026-08-21 実測ベースで再計算）

機外停車を避けるために必要なB駅停車時間を、**15000系の実走行シミュレーション**から求めた。

**停止限界の位置**（§6.6）
```
先行がC駅に停車中のとき、自列車が停止すべき点
  = C駅 − 列車長200m − CBTC停止限界50m = C駅の 250m 手前
```

**自列車がそこへ到達する時刻**（木場→門前仲町を標準85秒で走る省エネ運転を実測・制動2.5）

| 地点 | 木場発車からの経過 | 門前仲町到着の何秒前か |
|---|---|---|
| 門前仲町の470m手前（70km/hからの制動開始点＝CBTC干渉開始） | 41.9 s | 43.0 s前 |
| **門前仲町の250m手前（停止限界）** | **56.9 s** | 28.0 s前 |

**条件**: `先行のC駅発車時刻 < 木場発車時刻 + 56.9秒`

| headway | D_C=30 | 60 | 120 | 180 | 240 |
|---|---|---|---|---|---|
| 140秒 | 30秒 | 30秒 | 38秒 | 98秒 | **158秒** |
| 120秒 | 30秒 | 30秒 | 58秒 | 118秒 | **178秒** |
| 100秒 | 30秒 | 30秒 | 78秒 | 138秒 | **198秒** |

（表の値は必要なB駅停車時間。30秒＝標準停車のままでよい＝発車抑止不要）

→ 最大で198秒。**`DWELL_MAX` = 240秒**なら全シナリオをカバーでき、42秒の余裕がある。
D_C=300秒のシナリオを追加する場合は再検討が必要。

> 改訂2では「CBTC干渉開始＝到着の60秒前」という概算で最大195秒と見積もっていた。
> 車両モデル確定・列車長200m・制動2.5 をすべて反映して最大198秒となり、
> **結論（240秒で足りる）は3回の再計算を通じて変わらなかった**。この表を正式版とする。

## 6. 路線データ（東京メトロ東西線）

### 6.1 対象区間の確定

**東陽町 → 木場 → 門前仲町**（下り B線）を対象とする。

| 区間 | 距離 | 標準運転時間 | 表定速度 | 平均勾配 | 勾配の特徴 |
|---|---|---|---|---|---|
| 東陽町 → 木場 | 964.5 m | 80 s | 43.4 km/h | **−10.3‰** | 下り勾配が続く（−5 → −14.2 → −16.3‰、末尾のみ+2‰） |
| 木場 → 門前仲町 | 1058.0 m | 85 s | 44.8 km/h | **+11.3‰** | 登り勾配が多い（+2 → +3.5 → +4 → **+27 → +28 → +29.7** → +3‰） |

**この区間を選ぶ理由**
- 駅間が短く（合計2022.5m・165秒）、エピソードが軽い。停車240秒を足しても現行の2〜3倍で収まる。
- **A→Bが下り、B→Cが登り**という対照的な勾配構成で、惰行判断が区間ごとに大きく変わる。良い題材になる。
- 木場→門前仲町の +27〜+29.7‰ は急勾配で、惰行のみでは駅に届かない。
  羽前成田→白兎の「白兎手前が上り勾配で惰行のみでは届かない」構造と類似しており、
  既存の先行列車生成ロジックの考え方をそのまま適用できる。

### 6.2 既存 `track.py` をそのまま使えない理由（要対処）

**(1) 位置が減少方向**
本区間は**下り列車（B線）**であり、進行方向に沿って km が減少するのは正常な仕様である。
一方 `track.py` の `get_grade_resistance` / `get_curve_resistance` / `get_section_id` / `get_front_*` は
**位置が単調増加**する前提で書かれており、そのままでは動作しない。`train.py` の位置積分も加算前提。

→ **読み込み時に「進行方向に単調増加する内部座標」へ変換する**。
```
x_internal = x0 − x_csv          （x0 = 起点駅のキロ程 16.0118）
東陽町 → 0.0 km / 木場 → 0.9645 km / 門前仲町 → 2.0225 km
```
区間の start/end も反転して昇順に並べ替える。**勾配の符号はそのまま保つ**（§6.3）。
既存の羽前成田線は増加方向なので恒等変換のまま。
路線メタデータに `direction: "descending" | "ascending"` を持たせ、ローダが吸収する。

**(2) 勾配フィルタが範囲外の勾配を無言で 0.0 にする**
`track.py` は `if -40 < grade_val and grade_val <= 30:` で範囲外の勾配を**無言で 0.0 に置き換える**。
- 今回の対象区間の勾配は **−16.3 〜 +29.7‰** で辛うじてフィルタ内に収まるが、
  **+29.7‰ は上限 +30 のわずか0.3‰手前**であり極めて危険。
- 門前仲町→茅場町へ拡張すると **±35‰** が現れ、+35‰ が 0.0 に潰される。

→ 新ローダでは上限を **±40‰** 程度に緩めるか、範囲外を例外にして気付けるようにする。羽前成田線側の挙動は変えない。

**(3) `curve.csv` が無い（影響は小さい・§6.2.1）**
東西線には曲線データが無いため、**曲線抵抗 0 のフォールバック**を入れる（ファイル欠損を許容する）。

#### 6.2.1 速度制限データの意味（2026-08-21 確定）— **CS-ATCの信号現示**

`Tozai_line_speed_limit.csv` に記載された位置は、**CS-ATC の信号現示が切り替わる位置**である。
**列車の先頭が記載位置を越えた時点で現示が変わる**。

重要な帰結:
- **列車長200mを考慮する必要はない。** 現示は先頭位置で決まる。
  （改訂4で「最後尾が抜けるまで制限を適用」というモデルを入れたが、**誤りなので撤回**した。
  これにより東陽町→木場の余裕時分は 6.2% → **10.8%** に戻る）
- 記載値は**物理的な制限区間そのものではない**。CS-ATCの現示は「制限区間で制限速度に当たらないよう」
  実際の制限区間の**少し手前**で切り替わるよう設定されている。つまり**制動余裕が既に織り込まれている**。
- 観測 #11「路線制限速度」は、正確には「**線路条件によるATC現示**」である。
  先行列車によるATC現示（`cbtc_signal_speed`）と合わせて、実際の現示は両者の **min** になる。
  既存実装は `cbtc_signal_speed` 内で線路側の現示を上限に使っており、この構造と整合している。

**対象区間の現示（内部座標）**
```
0.0000〜0.0133  45 km/h   ← 東陽町の出発直後 13.3m のみ
0.0133〜0.1218  70 km/h
0.1218〜1.6718  75 km/h   ← 木場を挟む長い区間
1.6718〜2.3618  70 km/h   ← 門前仲町を含む（低下点は門前仲町の350.7m手前）
```
対象区間で現示が**低下**するのは `75 → 70`（内部座標1.6718）の1箇所のみ。
なお区間先頭の 45 km/h は13.3mしかなく、列車は起動から13.3m地点で約17 km/h しか出ていないため、
先頭判定では**事実上効かない**。

#### 6.2.2 曲線データについて（優先度は低い）

曲線抵抗 `800/R [kg/t]` を区間一律に加えて感度を測った。

| 曲線半径（区間一律） | 曲線抵抗 | 東陽町→木場（標準80s） | 木場→門前仲町（標準85s） | 力行仕事 |
|---|---|---|---|---|
| なし | 0.00 kg/t | 79.9 s（最高62.4） | 84.9 s（最高74.7） | 1.00 |
| R=800 m | 1.00 kg/t | 79.9 s（最高61.6） | 85.0 s（最高74.7） | 1.03 |
| R=400 m | 2.00 kg/t | 80.0 s（最高61.0） | 85.0 s（最高74.7） | 1.06〜1.07 |

**R=400mを区間全体に一律に掛けても、標準運転時間は依然として達成でき、力行仕事の増分は6〜7%に留まる。**
実際には曲線は区間の一部にしか存在しないので、影響はこれよりさらに小さい。

さらに、**比較対象の羽前成田→白兎も実質的に曲線が無い**。
`input/curve.csv` のうちego区間（21.112〜23.29km）に掛かるのは2箇所・計91mだけで、
距離加重平均は **0.05 kg/t**（R=500mが40m、R=1000mが51m）。

→ **現在の3駅スコープでは曲線データを用意しなくても、既存路線と同等の忠実度が保たれる。**
対象区間の現示が70〜75 km/h であること自体、急曲線が無いことを示唆している。

曲線データが意味を持つのは**門前仲町→茅場町へ拡張する場合**（55 km/h の現示が現れる＝
急曲線の存在が示唆される）。その時点で作成を依頼する。

### 6.3 勾配の符号（確認済み・2026-08-20）

**資料の勾配情報は進行方向（東陽町→門前仲町）基準であり、符号をそのまま使える。**
座標を反転しても符号は反転させない。

| 区間 | 距離加重平均勾配 | 標高差 | 資料の記述との整合 |
|---|---|---|---|
| 東陽町 → 木場 | −10.32‰ | −10.0 m | 「下り勾配が続く」と一致 ✓ |
| 木場 → 門前仲町 | +11.26‰ | +11.9 m | 「登り勾配が多い」と一致 ✓ |

### 6.4 生成が必要な派生データ
- **標準走行曲線 `sr_*.csv` と 標準運転曲線**: `generate_standard_curve.py` を区間ごとに実行する。
  §7.6 で `v_std(x)` として状態量に使うため、**フェーズ0で必ず作る**。
- **先行列車・後続列車の走行パターンCSV**: 東西線用に、かつ複数駅間を連続で走るパターンとして再生成が必要（§8）。
- 東西線には `curve.csv` / `delay.csv` / `departure_Diagram.csv` が無い。シナリオを引数で与える方式にして依存を切る。

### 6.5 車両モデル — 東京メトロ15000系（2026-08-21 確定）

`15000系_車両情報.pdf`（東京地下鉄 鉄道本部 車両部設計課「東京メトロ 東西線 15000系 主要諸元」）に基づき確定した。

**PDFから読み取った諸元**

| 項目 | 値 |
|---|---|
| 編成 | **10両×16編成（5M5T）**、160両（2023-03-31時点） |
| 車体最大寸法 | **20,000 mm**（先頭車のみ20,520 mm）× 2,850 × 4,022 mm |
| 車両性能（定員時） | **加速度 3.3 km/h/s** ／ 減速度 **3.5 km/h/s（常用最大）**・5.0 km/h/s（非常） ／ 設計最高速度 110 km/h |
| 定員（1編成） | 1,520人（#01〜13編成）／ 1,532人（#14〜16編成） |
| 主電動機・制御 | 三相かご形誘導電動機 ／ VVVFインバータ制御 |
| ブレーキ | CS-ATC連動 電気指令式電空併用ブレーキ（回生付） |
| 営業開始 | 2010年 |

**走行抵抗**（指定値）
```
R(v) = 2.089 + 0.0394 v + 0.000675 v²   [kg/t]
（現行 train.py: 2.39 + 0.0224 v + 0.00062 v²）
```
低速域では現行より小さく、高速域では大きい（70 km/h で 8.155 vs 6.996 kg/t）。

**引張力曲線**（PDFに記載が無いため、VVVF通勤電車の一般形で構成）

定トルク → 定出力 → 特性域の3領域モデルとし、**定トルク域の値は起動加速度3.3 km/h/s を満たすよう決める**。
```
F(v) = F0                    (v ≤ V1)        定トルク域
     = F0 · V1/v             (V1 < v ≤ V2)   定出力域
     = F0 · V1·V2/v²         (v > V2)        特性域

F0 = 3.3 × FACTOR_OF_INERTIA + R(0) = 3.3 × 28.34467 + 2.089 = 95.626 [kg/t]
V1 = 40 km/h（定トルク域の終端）,  V2 = 75 km/h（定出力域の終端）
```
V1・V2 は一般的な通勤電車の値。**PDFに記載が無い部分なので、感度確認の対象**とする（§13）。

**制動**: `DECELERATE = -2.5 km/h/s`（**現行 `train.py` と同じ値を維持する**）。

PDFの「減速度 3.5 km/h/s」は**常用最大**ブレーキであり、通常の駅停車で使う減速度ではない。
DQNの行動空間は制動ノッチが1つしかないため、その1つは**一般的な駅停車ブレーキ 2.5 km/h/s** とする。

> **CBTCパターンも同じ 2.5 km/h/s で構築すること。**
> 3.5（常用最大）を前提にパターンを引くと、2.5でしか減速できない列車はパターンに追従できず
> 停止限界を越えてしまう。制動能力とパターンの前提は必ず一致させる。
> 常用最大3.5・非常5.0は、将来ATC/非常制動をモデル化する場合に使う予備の値として記録しておく。

**慣性係数について**: `FACTOR_OF_INERTIA = 28.34467` は「kg/t → km/h/s」の単位換算そのもので、
回転部分の慣性（慣性係数 1.06〜1.09）を見込んでいない。実車に合わせるなら約 30.6 にすべきだが、
- 起動加速度は F0 を較正しているので**どちらでも 3.3 km/h/s を再現する**
- 差が出るのは勾配・抵抗の効き方のみで **約7%**、しかも現行値の方が勾配に厳しい（安全側）
- 修論の定式化との連続性が保てる

ことから、**現行値を維持**する。変更する場合は F0 も再計算すること。

**達成可能性の再検証**（制動2.5・制限速度厳守・全力行・駅にちょうど停止）

| 区間 | 現行（山形28t） | **15000系10両** | 標準 | 余裕 |
|---|---|---|---|---|
| 東陽町 → 木場 | 79.1 s | **71.3 s**（最高75.0 km/h） | 80 s | **+8.7 s（10.8%）** |
| 木場 → 門前仲町 | 97.0 s ✗ | **77.3 s**（最高74.7 km/h） | 85 s | **+7.7 s（9.0%）** |

（速度制限はCS-ATC現示なので**先頭位置で判定**する。§6.2）

**両区間とも成立する。** 余裕率 9〜11% は実ダイヤの余裕時分として妥当な水準で、
`rt`（80 / 85秒）が15000系に対して現実的な運転時分であることの裏付けにもなる。

**標準運転時間ちょうどで走る省エネ運転が実際に構成できることを確認済み**

| 区間 | 所要 | 最高速度 | 惰行開始点 |
|---|---|---|---|
| 東陽町 → 木場 | 79.9 s | **62.4 km/h** | 発車から100 m（**残り865 m＝区間の90%を惰行＋制動**） |
| 木場 → 門前仲町 | 84.9 s | 74.7 km/h | 発車から373 m（残り685 m） |

東陽町→木場は**下り勾配のため62.4 km/hまでしか加速せず、区間の90%を惰行で走れる**。
制限75 km/hまで上げる必要がない、きれいな省エネ運転になる。
一方 木場→門前仲町 は登り勾配のため 74.7 km/h（制限一杯）まで加速が必要で、対照的な運転曲線になる。
**この2区間の性格の違いが、複数駅間最適化の題材として好都合である。**

**急勾配での挙動が根本的に改善**

| 勾配 | 力行均衡速度（現行） | 力行均衡速度（**15000系**） | 惰行均衡速度（15000系） |
|---|---|---|---|
| −16.3‰ | — | — | **118.8 km/h** |
| 0‰ | 121.7 km/h | 126.6 km/h | — |
| +27.0‰ | **41.0 km/h** | **87.3 km/h** | — |
| +29.7‰ | **39.3 km/h** | **84.7 km/h** | — |

+29.7‰ での力行均衡速度が 39.3 → 84.7 km/h となり、**急勾配でも制限速度75km/hを維持して力行できる**。
「ランプで40km/hまで落ちる」という現行モデルの非現実的な挙動は解消した。

**制動距離**（制動2.5 km/h/s ＋ 走行抵抗・勾配抵抗）

| 速度 | 0‰ | −16.3‰ | +29.7‰ |
|---|---|---|---|
| 40 km/h | 84.5 m | 108.2 m | 60.4 m |
| 60 km/h | 187.2 m | 238.5 m | 134.4 m |
| 70 km/h | **252.5 m** | **321.0 m** | 181.8 m |
| 75 km/h | 288.4 m | 366.2 m | 207.9 m |

下り勾配（−16.3‰）では停止距離が平坦比 **+27%** 伸び、急勾配（+29.7‰）では **−28%** 縮む。
同じ70 km/hでも 181.8〜321.0 m と **1.77倍の開き**がある。
§7.2 で `brake_stop_distance` をプロファイル対応にする必要性がここに端的に現れる。
現行実装のように「現在地点の勾配1つ」で停止距離を出すと、東陽町→木場（下り）では
**停止距離を最大68m過小評価**して駅を通り過ぎることになる。

**実装時の注意 — 物理パラメータの重複**

走行抵抗の式は現在 **4箇所にハードコード**されている。`_multi` 版では必ず1箇所に集約すること。
```
train.py:69            travel_resistance プロパティ
train.py:147           （制動シミュレーション内）
environment2.py:616    _calc_brake_distance 内
environment2.py:888    _build_tasc_pattern 内
```
制動減速度（2.5 → 3.5）も同様に `train.py` の `DECELERATE`、`environment2.py` の上記2箇所、
`required_speed.py` の `BRAKE_NOTCH_DECEL_KMHS` の**4箇所すべて**を揃える必要がある。
`generate_standard_curve.py` は起動時に `train.py` の実値と突き合わせて例外を出す仕組みを持つので、
そちらの参照先も `train_multi.py` に切り替える。

### 6.6 列車長200mの影響（2026-08-21 追加・**新規対応が必要**）

現行の実装は**1両編成20m**を前提にしており、しかも**列車長がコード上どこにも現れない**。
10両編成200mでは、これが複数箇所で効いてくる。駅間964.5mに対し列車長は **21%** を占める。

**(1) CBTC車間が200m厳しくなる（最重要）**

**定義（2026-08-21 確定）**: 停止限界は「**先行列車の最後尾から50m手前に自列車の先頭が来る点**」。
CLAUDE.md の記述と一致する。
現行 `environment2.cbtc_signal_speed` は
```python
target_distance = self.fowerd_train_remaining_distance - 0.05   # 先行の「位置」から50m
```
としており、**列車長を引いていない**。位置は先頭位置とみなす運用（`drive_monitor.py` の記述）なので、
20m編成では20mの誤差で済んでいたが、200m編成では**200mの誤差**になる。

→ 修正:
```python
target_distance = self.fowerd_train_remaining_distance - TRAIN_LENGTH - CBTC_LIMIT
                # = 先行先頭までの距離 − 0.200 − 0.050
```
衝突判定（終了条件③、現行は先行位置の40m手前）も同様に列車長ぶん手前へずらす。

**影響の大きさ**: 先行がC駅に停車中のとき、自列車が進める限界は
`C駅の250m手前`（＝B→C区間1058mのうち808m、76%地点）。現行実装だと914m（86%）まで進めてしまう。

**(2) 速度制限には列車長を適用しない（改訂4の内容を撤回）**

改訂4では「速度制限は最後尾が抜けるまで守る」というモデルを入れ、東陽町の45km/h制限（13.3m）が
実質213.3mになるとして 3.8秒のロスを計上していた。

**これは誤りだった。** `Tozai_line_speed_limit.csv` は物理的な制限区間ではなく
**CS-ATCの信号現示の切替位置**であり、**先頭が越えた時点で現示が変わる**（§6.2.1）。
現示は先頭位置で決まるため、列車長を考慮する必要はない。

→ `track_multi.py` に後方参照の関数は**不要**。既存 `Track` と同じ「先頭位置で引く」方式のままでよい。
→ 東陽町→木場の最速走行は 75.1 s → **71.3 s**、余裕は 6.2% → **10.8%** に戻る。

> 列車長200mが効くのは **CBTC車間（上記(1)）と可視化・後続列車の頭打ち**であって、速度制限側ではない。
> この区別を実装時に取り違えないこと。

**(3) その他**
- **停止位置**: 先頭位置基準は変わらないが、ホーム有効長200m以上が前提になる（東西線は10両対応なので問題なし）。
- **駅間停車の判定**: 200m編成が駅間に停まると、駅を跨いで停車する状況もありうる（B→C 1058mに対し列車長200m）。
  終了条件⑤「手前停止」の判定は先頭位置基準のままでよいが、**可視化（`drive_monitor.py`）は実スケールで描く**こと
  （CLAUDE.md に「実スケール・列車長20m」と記載があるので**200mへ更新が必要**）。
- **後続列車の頭打ち**（§8.2）も列車長200mで計算する:
  `後続の位置 = min(記録軌道, 自列車の位置 − (200m + 50m))`。改訂2では70mとしていたが**250m**が正しい。

## 7. 目標速度の算出方式（2026-08-20 追加）

制限速度と勾配の変化が大きい東西線では、**現行の単純な目標速度算出では成立しない**。
「どこまで加速するか」「制限速度が変わったらどうするか」「勾配でどれだけ速度が落ちたら加速するか」を、
場当たり的な閾値ではなく**前方プロファイルの積分**から導く方式に改める。

### 7.1 現行方式の限界（定量）

`required_speed.py` の `simulate_trip_time()` は、
- **勾配を「現在地点の値 1つ」で受け取り、駅までずっと一定と仮定**する（`grade_resistance: float`）
- **制限速度も「現在値 1つ」のスカラー**である（`speed_limit: float`）

羽前成田→白兎ではこれで問題なかった。理由は次のとおり。
- 制限速度は**全区間70km/hで変化しない**（メモリ [[ego-segment-route-characteristics]]）
- 勾配の範囲が **−6.7 〜 +12.5‰** と狭く、惰行減速度の変動が小さい

**東西線ではこの前提が両方とも崩れる。**

| | 羽前成田→白兎 | 東西線 東陽町→門前仲町 |
|---|---|---|
| 制限速度 | 70 km/h（変化なし） | **45 → 70 → 75 → 70 km/h（3回変化）** |
| 勾配範囲 | −6.7 〜 +12.5‰ | **−16.3 〜 +29.7‰** |
| 惰行減速度(70km/h)の幅 | −0.01 〜 −0.63 km/h/s | **+0.29 〜 −1.34 km/h/s（符号が反転する）** |
| 列車長 | 20 m（1両） | **200 m（10両）** |

具体的な破綻例（木場→門前仲町）:
- 列車が **+3‰ の地点**で目標速度を計算すると、惰行減速度を −0.43 km/h/s 程度と見積もる。
- しかし実際にこの先には **+29.7‰ のランプ**があり、そこでの惰行減速度は **−1.336 km/h/s**（15000系）。**3.1倍の過小評価**。
- 結果、「惰行で間に合う」と誤判定して惰行に入り、ランプで急激に速度を失って遅延、
  あるいは駅に届かず駅間停車する。

### 7.2 改修A: プロファイル対応の走行シミュレーション

`simulate_trip_time()` を、スカラーではなく**前方プロファイル**を受け取る形に変更する。

```python
# 現行
simulate_trip_time(v0, target_cruise, distance_m, grade_resistance: float)

# 改修後
simulate_trip_time(v0, target_cruise, distance_m,
                   grade_profile: list,   # [{"distance": m, "grade": ‰}, ...]
                   limit_profile: list)   # [{"distance": m, "speed_limit": km/h}, ...]
```

**実装は容易**である。`track.py` に既に
`get_front_grades(start, end)` / `get_front_sections(start, end)` があり、
まさにこの形式（`{"distance": …, "grade": …}` のリスト）を返す。
`environment2.py` も `train.front_grades` 経由で既に使っている。渡す先を増やすだけでよい。

**性能**: `calculate_required_speed` は二分探索24回 × 各シミュレーション約500ステップ。
プロファイルは累積距離の配列にしてポインタで進めれば **1ステップあたり O(1)** で参照でき、
現行とほぼ同じコストに収まる。毎ステップ呼ばれるため、プロファイルの前処理は
`reset()` 時か区間切替時に1回だけ行いキャッシュする。

`brake_stop_distance_m()` も同様にプロファイル対応にする。
下り勾配では停止距離が伸びるため、−16.3‰区間で停止距離を過小評価すると停止位置精度が崩れる。

### 7.3 改修B: 3層構造の目標速度

目標速度を、**安全側の上限**と**モード別の目標**の合成として定義する。

```
v_target(x) = min(
    v_limit_pattern(x),      # ① 路線制限速度の先読みパターン（§7.4）
    cbtc_signal_speed,       # ② 先行列車のCBTC現示（既存実装をそのまま使用）
    v_mode(x)                # ③ 運転モード別の目標速度
)

v_mode(x) = {
    normal          → v_std(x)                 # 標準運転曲線（§7.6）
    delay_recovery  → required_speed(x)        # 標準より速く（定時回復）
    anti_mid_stop   → target_speed_no_stop(x)  # 標準より遅く（機外停車回避）
    spacing         → target_speed_spacing(x)  # 標準より遅く（間隔調整）
}
```

**この構造の利点**: 既存のモード設計と綺麗に整合する。
各モードは「**標準運転曲線に対して、どちらへどれだけ外すか**」として統一的に記述でき、
LLM評価プロンプトの評価軸も「標準からの逸脱が状況に照らして適切か」という一貫した形で書ける。

①②は安全上の上限であり、モードによらず常に効く。③だけがモードで切り替わる。

### 7.4 制限速度の変化への対応 — `v_limit_pattern(x)`

**「制限速度が変化したらどうするか」への答え。閾値は作らない。**

制限が低下する地点を「その制限速度で通過すべき点」とみなし、
**制動曲線を逆向きに積分**して、各地点で許容される最大速度を求める。

```
v_limit_pattern(x) = min over 前方の全ての制限低下点 i (
    「地点 x から制動して、低下点 i に制限速度 L_i 以下で到達できる最大速度」
)
```

これは既存の `cbtc_signal_speed`（先行列車の50m手前を停止限界とする減速パターン）と**まったく同じ構造**である。
違いは終端条件だけで、CBTCは「停止限界で 0 km/h」、こちらは「低下点で L_i km/h」。
既存の二分探索＋`_calc_brake_distance` をそのまま流用できる。

対象区間で効くのは1箇所:
```
75 → 70 km/h の低下点  内部座標 x = 1.6718 km（門前仲町の 350.7 m 手前）
必要な先行制動距離: 36.0 m（所要2.0秒）
```

**ATC現示との関係（§6.2.1）— 2つの解釈**

記載位置は既に「制限区間の少し手前」であり**制動余裕が織り込まれている**ため、モデルには2つの選択肢がある。

| | 意味 | 乗り心地 |
|---|---|---|
| **(a) 予見型（採用）** | 現示低下点に到達した時点で**既に**低い速度になっている | ◎ 手前から緩やかに減速し、制限区間へは適正速度で進入する |
| (b) 追従型 | 現示低下点で減速を**開始**し、制動曲線に沿って下げる | △ 低下点で減速を始めるため制動が遅れがちになる |

**(a) を採用する（2026-08-21 決定）。**
現示低下を予見して手前から緩やかに減速すれば、**不要な・急な制動を避けて制限区間に進入でき、乗り心地が向上する**。
実際の運転士の扱いにも近い。定義も明快で保守的である。

#### 7.4.1 (b) 追従型の実装も用意しておく（茅場町延伸への備え）

制限速度が大きく変わる区間では (a) の先行減速が時間を食うため、
**延伸時には (b) でないと定時到着できないのではないか**という懸念がある。実際に計算して確かめた。

**門前仲町→茅場町（1801.8 m・標準140秒）— この区間には 70→55 の現示低下が2箇所ある**
```
x=2.3618（門前仲町から  339.3 m）  70 → 55 km/h
x=2.9018（門前仲町から  879.3 m）  55 → 70 km/h
x=3.4668（門前仲町から 1444.3 m）  70 → 55 km/h
```

| モデル | 最速走行 | 標準140秒に対する余裕 |
|---|---|---|
| **(a) 予見型** | **131.9 s** | **+8.1 s（5.8%）** |
| (b) 追従型 | 129.0 s | +11.0 s（7.9%） |

**結論: (a) でも定時到着できる。** ただし (a) は (b) より **3.0秒遅く**、余裕率が 7.9% → 5.8% に減る
（1回の 70→55 低下あたり約1.5秒のコスト × 2箇所）。

補足の検証:
- **標準140秒ちょうどの省エネ運転は (a) でも構成でき、力行仕事は (b) とほぼ同じ**（差 0.0%）。
  余裕8.1秒を惰行に回せるため、エネルギー面での不利は生じない。
- **引張力パラメータ V1/V2 への感度は小さい**。(a) の最速走行は
  V1/V2 = 40/75 → 131.9 s、38/65 → 132.3 s、45/80 → 131.2 s、35/60 → 133.0 s。
  最も悲観的な 35/60 でも余裕 5.0% を確保する。
- **曲線抵抗は定時性にほぼ影響しない**。55km/h現示の区間（897.5 m＝区間の50%）に R=300m の曲線を
  仮定しても最速走行は 131.5 s とむしろわずかに速くなる（制動距離が縮み、制動開始を遅らせられるため）。
  曲線が効くのは力行仕事であって所要時分ではない。

**それでも (b) を実装可能にしておく。** 理由:
- 余裕 5.8% は薄く、引張力曲線が実車より楽観的だった場合に余裕が消える可能性がある
  （§6.5 のとおり引張力はPDFに記載が無く、こちらで構成したもの）
- 両者の天井速度は**どちらも制動曲線の積分**で、実装は各15行程度。切替フラグ1つで済む
- 延伸を判断する時点で「どちらでも動く」状態にしておけば、判断がデータ待ちにならない

```python
# line_config.py
atc_mode = "anticipatory"   # (a) 既定。"pattern" にすると (b)
```
- **(a)**: 現示低下点から**後ろ向きに**制動曲線を逆積分し、各地点の天井速度を下げる
- **(b)**: 現示低下点から**前向きに**制動曲線を順積分し、低下点直後は前の現示から下がる途中を許容する

いずれも位置グリッド上に天井速度の配列を1度だけ構築してキャッシュすればよく、
実行時のコストは配列参照のみ（§7.2 のプロファイル前処理と同じ扱い）。

> (b) を採る場合でも「**本来の制限区間の速度**」データは必ずしも要らない。
> 現示低下点を制動開始点とみなし、そこから常用制動で新現示まで下げる曲線を天井にすれば、
> 追加データなしで定義できる（現示が制限区間の手前に置かれているという前提と整合する）。
> より正確に模擬したい場合にのみ、実際の制限区間データが必要になる。

#### 7.4.2 対象3駅区間での効き方

対象区間で現示が**低下**するのは1箇所のみ。
```
75 → 70 km/h の低下点  内部座標 x = 1.6718 km（門前仲町の 350.7 m 手前）
必要な先行制動距離: 36.0 m（所要2.0秒）
```
(a) と (b) の所要時分の差は **0.0〜0.1秒**で、実質的に無視できる。
また区間先頭の 45 km/h は13.3mしかなく先頭判定では効かないため、
**3駅スコープでは ATC モデルの選択は結果に影響しない**。仕組みとして入れておく意味は延伸への備えにある。
茅場町方面（70→55、55→70、70→55）へ拡張したときに必須になるうえ、
「制限接近フェーズ」が羽前成田線では構造的に発生しなかった（メモリ [[ego-segment-route-characteristics]]）ため、
**東西線で初めて意味を持つ観測**になる。観測 #40 として QNetwork にも与える。

### 7.4.3 プロンプト改修方針 — 目標速度と制限速度の評価（2026-08-21 追加）

現行プロンプト（`evaluate_csv_with_llm.py`）の目標速度まわりは、**羽前成田線と山形28t車両を前提とした
固定数値が直接書き込まれている**。東西線＋15000系ではその数値が成立しないため、書き換えが必要になる。

#### (1) 原則 — 数値判断はPython側へ、プロンプトは評価に徹する

これは新方針ではなく、**既存の設計思想の徹底**である。現行プロンプトには既にこう書かれている:

> 必要速度（required_speed）は…あらかじめ算出済みの値である。current_statusのrequired_speedを
> **必ずそのまま評価に使用し、LLM自身が平均速度などから必要速度を再計算してはならない。**

`required_speed` / `target_speed_no_stop` はこの原則に従っている。
**一方で「制限速度 − 8km/h ～ − 2km/h」「−6‰より急な下り」「1秒あたり約0.5km/h加速する」といった
車両・路線依存の数値がプロンプトに直書きされており、ここだけ原則から外れている。**
これらをすべてPython側の算出値に置き換える。

#### (2) 現行プロンプトの固定数値が15000系で成立しない — 実測

**維持帯の幅（遅延回復モードの中核）**

現行プロンプトの維持帯は「制限速度 − 8 〜 − 2 km/h」＝**幅6km/h**で、
その根拠は「力行相・惰行相ともに7秒以上continueするのでノコギリ運転にならない」（設計メモ §18）。
ノコギリ判定は「`holding_time < 7秒` **かつ** `prev_notch_duration < 7秒`」なので、
**両相が7秒以上あることがこの帯幅の存在理由**である。

| 車両 | 勾配 | 力行加速度 | 惰行加速度 | 力行相 | 惰行相 |
|---|---|---|---|---|---|
| 山形28t（現行） | 平坦 | +0.466 km/h/s | −0.235 km/h/s | 12.9 s ✓ | 25.5 s ✓ |
| **15000系** | 平坦 | **+1.740 km/h/s** | −0.274 km/h/s | **3.4 s ✗** | 21.9 s ✓ |
| 15000系 | +29.7‰ | +0.693 km/h/s | −1.322 km/h/s | 8.7 s ✓ | **4.5 s ✗** |
| 15000系 | −16.3‰ | +2.315 km/h/s | **+0.301 km/h/s** | 2.6 s ✗ | **∞（惰行で加速）** |

**15000系は力行加速度が3.7倍高いため、幅6km/hでは力行相が3.4秒しか続かずノコギリ判定に該当する。**
しかも必要な帯幅は勾配で逆転する（平坦は力行相が律速、急勾配は惰行相が律速）。

**両相7秒以上を満たす最小の帯幅（15000系・67km/h）**

| 勾配 | 該当箇所 | 必要帯幅 | 律速側 |
|---|---|---|---|
| −16.3‰ | 東陽町→木場 主部 | **成立しない** | 惰行で加速するため帯を往復できない |
| −5.0‰ | 東陽町→木場 序盤 | 13.4 km/h | 力行相 |
| +2.0〜+4.0‰ | 両区間 | 11.2〜11.7 km/h | 力行相 |
| +29.7‰ | 木場→門前仲町 ランプ | 9.3 km/h | 惰行相 |

→ **固定値では書けない。勾配と速度から都度算出するしかない。**

**下り勾配の判定閾値**

現行プロンプトは「−6‰より急な下り」を境界にしているが、惰行加速度が0になる勾配は**速度依存**である。

| 速度 | 惰行加速度が0になる勾配 |
|---|---|
| 40 km/h | −4.74‰ |
| 55 km/h | −6.30‰ |
| 67 km/h | −7.76‰ |
| 75 km/h | −8.84‰ |

固定閾値 −6‰ は 40km/h では甘く（−5‰で既に加速する）、75km/h では厳しい（−8‰でもまだ減速する）。
**閾値ではなく `coast_accel` の符号で判定させる。**

#### (3) `current_status` に追加する算出値

| キー | 内容 | 用途 |
|---|---|---|
| `v_ceiling` | `min(v_limit_pattern(x), cbtc_signal_speed)` ＝ 超えてはならない上限 | 全モード共通の安全上限 |
| `v_target` | モード別の目標速度（下表） | 評価の基準速度 |
| `band_upper` / `band_lower` | 遅延回復モードの維持帯の上下端 | 「制限−8〜−2」の置き換え |
| `coast_accel` | 現在位置・速度での惰行加速度 [km/h/s] | 下り勾配判定（符号）・惰行で減速できるかの判断 |
| `power_accel` | 同じく力行加速度 [km/h/s] | 「1秒あたり約0.5km/h加速」の置き換え |
| `v_std` | 標準運転曲線のその位置での速度（§7.6） | 通常運転モードの基準 |
| `limit_drop_ahead` | 次の現示低下点までの距離と低下後の現示 | 予見的減速の評価（下記(6)） |

#### (4) 基準速度 `v_target` のモード別定義（§7.3 の再掲）

| モード | `v_target` | 現行からの変更 |
|---|---|---|
| `normal` | `v_std(x)`（標準運転曲線） | **新規**（現行は `required_speed`） |
| `delay_recovery` | `v_ceiling`（＝先読み現示パターン） | **`speed_limit` から変更** |
| `anti_mid_stop` | `target_speed_no_stop` | 変更なし |
| `spacing` | `target_speed_spacing` | 新規（フェーズ3） |
| `hold_at_station` | — （停車中） | 新規（フェーズ3） |

**遅延回復モードの基準を `speed_limit` から `v_ceiling` に変える理由**が今回の要点である。

羽前成田線は**全区間70km/hで現示が変化しない**ため（メモリ [[ego-segment-route-characteristics]]）、
`speed_limit` はスカラーとして意味を持ち、「制限速度まで加速せよ」で正しかった。

東西線では現示が 45→70→75→70 と変化し、しかも**予見型(a)を採用**した（§7.4）。
`speed_limit` を基準にすると、門前仲町の350.7m手前の低下点（75→70）に対して
「75まで加速せよ」と評価してしまい、**低下点で急制動する運転を高評価してしまう**。
これは §18・§22 で3度作り直した「制限まで加速→急減速」の再来である。

`v_ceiling` は先読みパターンなので、低下点の36m手前から自然に下がり、
「予見して緩やかに減速する」運転が高評価になる。

#### (5) 維持帯を可変幅にする

```
band_upper = v_ceiling − M_up
band_lower = band_upper − W

M_up = max(2.0, power_accel × time_step × 1.5)   … 1ステップ力行しても超過しない余裕
                                                   （15000系は1秒で1.74km/h上がるため2.0では薄い）
W    = T_min × max(power_accel, |coast_accel|)   … 両相が T_min 秒以上continueする最小幅
```
`coast_accel ≥ 0`（＝惰行で減速できない）の場合は**帯を定義しない**。下記(7)の別ルールに切り替える。

**T_min（乗り心地の要求）と平均速度のトレードオフ**（平坦・上限73km/h・時間加重平均 = `band_upper − W/2`）

| T_min | 帯幅W | 維持帯 | 平均速度 | 力行相 | 惰行相 | 周期 | 1058m所要 |
|---|---|---|---|---|---|---|---|
| 3 s | 5.2 km/h | 67.8〜73.0 | 70.4 km/h | 3.0 s | 19.1 s | 22.1 s | 54.1 s |
| 5 s | 8.7 km/h | 64.3〜73.0 | 68.6 km/h | 5.0 s | 31.8 s | 36.8 s | 55.5 s |
| **7 s（現行基準）** | **12.2 km/h** | **60.8〜73.0** | **66.9 km/h** | 7.0 s | 44.5 s | 51.5 s | **56.9 s** |
| 10 s | 17.4 km/h | 55.6〜73.0 | 64.3 km/h | 10.0 s | 63.6 s | 73.6 s | 59.2 s |

**T_min = 5秒に決定（2026-08-21）。ノコギリ運転の判定しきい値も同じ5秒に揃える。**

帯幅の根拠（両相が T_min 秒以上continueする）と、ノコギリ判定のしきい値は**同じ数値でなければ整合しない**。
帯幅を5秒基準にしながら判定を7秒のままにすると、帯の端での正常な切替がノコギリとして減点されてしまう。

**ノコギリ運転のペナルティ表（`evaluate_csv_with_llm_multi.py` 用）**

| 現行（7秒基準・変更禁止） | **新（5秒基準）** |
|---|---|
| 2秒未満 → 0.0 | 1.5秒未満 → 0.0 |
| 2〜5秒 → 0.1〜0.3 | 1.5〜3.5秒 → 0.1〜0.3 |
| 5〜7秒 → 0.3〜0.5 | 3.5〜5秒 → 0.3〜0.5 |
| 7秒以上 → 減点なし | **5秒以上 → 減点なし** |

判定条件（`holding_time < T_min` **かつ** `prev_notch_duration < T_min`）の構造は変えず、しきい値だけ 7→5 にする。

**実走行シミュレーションによる検証（実際の勾配プロファイル上で帯運転させた結果）**

| 区間 | 標準 | 理論最速 | **T_min=5** | T_min=7 |
|---|---|---|---|---|
| 東陽町 → 木場 | 80 s | 71.3 s | **75.4 s（+4.6 s回復）** | **81.0 s（−1.0 s＝定時未達 ✗）** |
| 木場 → 門前仲町 | 85 s | 77.3 s | **78.4 s（+6.6 s回復）** | 79.2 s（+5.8 s回復） |

**T_min=7 では東陽町→木場が81.0秒となり、遅延回復モードなのに標準運転時間にすら届かない。**
つまり 5秒は「好ましい」のではなく**必要条件**である。この区間が最も厳しい理由は下記のとおり。

**なぜ東陽町→木場（−16.3‰）が最も厳しいか**

この区間は惰行で加速するため、帯運転は「惰行で上がる ↔ 制動で下げる」のサイクルになる。
その加速度の非対称が極端である（惰行 +0.30 km/h/s に対し制動 −2.2 km/h/s ＝ **7.3倍**）。

- 両相を T_min 秒以上にするには帯幅を **T_min × 制動減速度** まで広げる必要がある
  （T_min=5 で11.6 km/h、T_min=7 で16.2 km/h）
- 帯が広いほど平均速度が下がるうえ、**下り勾配で得た速度を制動で捨ててから重力で取り戻す**ため
  時間・エネルギーの両方で浪費が大きい
- 帯を狭くすれば速度は保てるが、制動相が1〜2秒の「軽い制動の連打」になりノコギリに該当する

**3ノッチ制御では、急な下り勾配で高い速度を保つことと乗り心地の両立が原理的に難しい**
（実車は刻みノッチで制動を弱く連続的にかけ、定速を保てる）。これは車両モデルの制約であり、
プロンプトで解決できる問題ではない。**この区間の遅延回復には限界がある**ことを前提に評価軸を設計する。

> 上記の数値は単純なヒステリシス制御での実測であり、DQNが学習する方策の上限を示すものではない。
> 「どの区間が難しいか」「T_min の選択がどれだけ効くか」の目安として使う。

#### (5-2) 勾配急変点での帯パラメータの不連続（実装上の注意）

帯の上下端を**その地点の勾配から瞬時に**算出すると、勾配が急変する地点で帯が飛び、ノッチが振動する。
実測では 東陽町→木場 の −16.3‰ → +2.0‰ の変わり目（発車から707m）で
2.1〜2.3秒の短い相が2回発生した（帯の性質が「惰行で加速（帯なし）」から「通常の帯」へ切り替わるため）。

→ **帯パラメータは前方数十mの平均勾配で平滑化して算出する**（§7.2 のプロファイル対応と同じ考え方）。
瞬時値ではなく「これから走る区間の代表値」を使う。

#### (6) 現示低下点への予見的減速 — **新規の評価軸**

現行プロンプトには「制限速度区間に接近中（500m以内に制限区間在り）」というフェーズが定義されているが、
**羽前成田線では構造的に発生せず学習データは0行**である（メモリ [[ego-segment-route-characteristics]]）。
つまり**この評価軸は事実上未定義・未学習のまま放置されている**。

東西線では初めて実際に発生するため（75→70の低下点）、**プロンプトに評価基準を新規に書き、
学習データも新規に生成する必要がある**。

評価の骨子:
- `v_ceiling` が現在の現示より低い＝低下点に接近中。`current_speed > v_ceiling` なら減速が必要。
- **予見して緩やかに減速し、低下点に新現示以下で到達する → 高評価**（乗り心地・省エネの両面で正しい）
- **低下点まで力行を続け、低下点で急制動する → 減点**（§18・§22 の再発パターン）
- 低下点を過ぎた後、新現示に対して余裕があるのに減速を続ける → 減点（不要な減速）

#### (7) 惰行で減速できない下り勾配の扱い

`coast_accel ≥ 0` の区間（東西線では −16.3‰ の東陽町→木場 主部が該当）では、
**力行↔惰行のサイクルが成立しない**（惰行しても速度が落ちない）。

- **惰行で `band_upper` に張り付くのが正しい運転**。力行は不要（重力で加速するため）。
- `v_ceiling` に接近したら**超過する前に制動**する。この制動は正当であり減点しない。
- 現行プロンプトの「急な下り勾配」節と同じ思想だが、**判定を勾配閾値から `coast_accel` の符号に変える**。

なお東陽町→木場は通常運転でも**最高62.4 km/h・区間の90%を惰行**で走る区間である（§6.5）。
遅延回復モードでも下り勾配では力行がほとんど不要になるはずで、
「遅延回復＝力行を続ける」という単純な図式が通用しない。この点をプロンプトに明記する。

#### (7-2) 5ノッチ化に伴うプロンプトの変更（§4.7）

- **ノッチの記述を5種に**。`current_notch` / `prev_notch` の文字列を
  「力行」「勾配力行」「惰行」「勾配ブレーキ」「制動」に拡張する。
- **維持帯の上下端は、その勾配で0をまたぐ最も緩やかなノッチ対から算出する**（§4.7.3）。
  下り勾配では `C`↔`B2`、平坦〜緩い上りでは `P2`↔`C`、急な上りでは `P2`↔`C`。
  `current_status` には帯だけでなく **使用すべきノッチ対**も渡すと評価が安定する。
- **ノコギリ評価をノッチ間距離で重み付けする**（§4.7.4）。
  現行は全ての反転を同列に扱うが、5ノッチでは加速度のジャンプ幅が 0.51〜4.51 km/h/s と9倍の開きがある。
  `notch_jump`（前ノッチとの加速度差）を `current_status` に渡し、
  **ジャンプが小さい移行は短い保持時間でも減点を軽くする**。
- **急な下り勾配では `B2` が正解ノッチになる**。現行プロンプトは下りで「惰行を最高評価」としているが、
  −16.3‰ では惰行では減速できない。`B2` による穏やかな速度維持を最高評価とし、
  `B1`（2.5 km/h/s）は落としすぎとして減点する。
- **`P2` を「中途半端な力行」として減点してはならない**。省エネ運転の理論（Pontryagin）では
  定速保持フェーズ（特異弧）で中間ノッチが最適になる。急勾配区間での `P2` は正しい運転である。

#### (8) プロンプトから削除すべき車両・路線依存の数値（一覧）

| 現行の記述 | 置き換え先 |
|---|---|
| 「制限速度 − 8km/h ～ 制限速度 − 2km/h」（維持帯） | `band_lower` 〜 `band_upper` |
| 「1秒あたり約0.5km/h加速するため」 | `power_accel` |
| 「平坦で惰行約26秒・力行約12秒」 | 記述ごと削除（T_min で担保） |
| 「−6‰以浅のゆるい下り勾配」「−6‰より急な下り」 | `coast_accel` の符号 |
| 「required_speed＋10km/hまでは許容」 | `v_target` 基準の相対量に統一 |
| 「現在速度がすでに制限速度−10km/h以上」 | `band_lower` との比較 |
| 遅延回復の基準速度 `speed_limit` | `v_ceiling` |

> **保全（§9.1）**: 上記の改修は **`evaluate_csv_with_llm_multi.py`（新規）に対して行う**。
> 既存の `evaluate_csv_with_llm.py` のプロンプトは資産として一切変更しない。

### 7.5 勾配による速度低下への対応 — 惰行到達可能性で判定する

**「勾配でどれくらい速度が落ちたら加速するか」への答え。km/h の閾値は決めない。**

判定基準は「**今から惰行を続けたとき、前方プロファイルを積分して制動開始点に到達できるか／定時に間に合うか**」。
届かない・間に合わないと判定した時点で再加速する。

これは新しい発想ではなく、**`generate_forward_train.py` で既に実装・検証済みのロジック**である
（CLAUDE.md 記載:「惰行を続けても制動開始点に届かないと判定した時点でVまで再加速して駅間停車を避ける」）。
羽前成田→白兎の白兎手前上り勾配で機能しており、そのまま持ち込める。

**+29.7‰ ランプでこれがどう働くか**
- ランプ手前（+4‰地点）で惰行到達可能性を評価すると、プロファイル積分により
  ランプでの −1.295 km/h/s の減速が織り込まれる。
- 「このまま惰行するとランプで速度を失い、制動開始点に届かない（または定時に間に合わない）」と判定される。
- → **ランプに入る前に加速せよ**、という判断が自然に出る。閾値を書く必要がない。

**均衡速度による上限**（§6.5 の表を参照）
15000系では +29.7‰ の力行均衡速度が **84.7 km/h** となり、線区制限75 km/h を上回る。
つまり**急勾配でも制限速度まで力行で維持できる**ため、「均衡速度で頭打ち」という制約は事実上効かなくなった。

**それでも惰行到達可能性の判定は必須である。** 力行すれば維持できることと、惰行で足りることは別問題で、
+29.7‰ での惰行減速度は **−1.336 km/h/s**（15000系）と現行モデル（−1.295）より**むしろ大きい**
（走行抵抗の指定値が高速域で大きいため）。省エネ運転は惰行を最大限使うので、
「惰行のままランプを越えられるか」の判定こそが本質になる。

### 7.6 テストケース0 = 標準運転曲線に沿う

**要件**: 先行・後続なし・自列車の遅延なしの条件では、標準運転曲線に沿うことが望ましい。

**方式**: `generate_standard_curve.py` が出力する標準運転曲線から、
**位置ごとの速度 `v_std(x)` を参照テーブルとして読み込み、状態量として与える。**

- **QNetwork観測**: #38 `v_std(x)/80`、#39 `(v − v_std(x))` を ±20 でクリップして正規化（§4.2）
- **報酬NN入力（`state_info`）**: `standard_curve_speed` と `standard_curve_deviation` を追加（フェーズ3）
- **LLM評価プロンプト**: 「通常運転モードでは標準運転曲線に沿うことが最良」という評価軸を明記する

**CLAUDE.md の制約との関係**: `v_std(x)` は**報酬項ではなく入力特徴量**として与える。
「報酬はLLMを蒸留したNNの出力のみ」という制約は守られる。
環境側で `−|v − v_std|` のようなペナルティを直接足してはならない。

**現実性**: 実車の運転士は線区の運転曲線・運転時分を熟知して運転しており、
標準運転曲線を知識として持つのは不自然ではない。

**モードとの関係**（§7.3 の再掲）
| モード | 標準運転曲線に対して |
|---|---|
| `normal` | **沿う**（テストケース0はこれ） |
| `delay_recovery` | 上に外す（速く走って遅延回復） |
| `anti_mid_stop` | 下に外す（早めに惰行して先行に追いつかない） |
| `spacing` | 下に外す（間隔を保つため速度を落とす） |
| `hold_at_station` | 駅停車中（走行曲線の対象外） |

これにより「標準からどちらへ外すのが正しいか」がモードの定義そのものになり、
評価軸が一貫する。

**注意**: `v_std(x)` は車両モデルに依存する。§6.5 で車両モデルを更新したら**標準運転曲線を作り直す**こと。
モデル・スケーラ・マニフェストの世代を揃える運用（CLAUDE.md 記載）と同じ考え方で、
**車両モデル・標準運転曲線・報酬NN の3点セットの世代を揃える**。

### 7.7 下り勾配での惰行加速 — `forbidden_action` の修正

**新たに必要になる対処。**

設計メモ §18 で「制限速度超過時は力行のみ禁止し、**惰行は許可**する」と決めた。
根拠は「0.1km/hの微小超過でも最大ブレーキしか選べないのは過剰。惰行（約0.25km/h/s）なら1秒で解消する」だった。

**東陽町→木場の −16.3‰ ではこの前提が崩れる。**

| 勾配 | 惰行時の加速度（15000系） |||
|---|---|---|---|
| | 30 km/h | 50 km/h | 70 km/h |
| −16.3‰ | **+0.438** | **+0.372** | **+0.287** |
| −14.2‰ | **+0.364** | **+0.298** | **+0.213** |
| −5.0‰ | +0.040 | −0.026 | −0.111 |
| 0‰ | −0.137 | −0.203 | −0.288 |

（単位 km/h/s。正＝惰行しても加速する）

−16.3‰ では**全速度域で惰行しても加速する**（惰行の均衡速度は 118.8 km/h で、制限75km/hの遥か上）。
**車両モデルを15000系に変えても、この問題はそのまま残る。**
羽前成田線の最急下りは −6.7‰ で、高速域では惰行がほぼ中立だったため問題にならなかった。

**修正**:
```python
# forbidden_action 内
if self.speed > self.current_speed_limit:
    acceleration = True
    # 惰行しても加速する勾配では、惰行では超過を解消できないため惰行も禁止する
    coast_accel = -(travel_resistance(self.speed) + grade_resistance) / FACTOR_OF_INERTIA
    if coast_accel > 0.0:
        coasting = True
```
※`deceleration` は必ず残るので、3行動同時禁止にはならない。

同じ理由で、**下り勾配では「惰行＝減速」という暗黙の前提を置いているコードを洗い出す**必要がある。
`required_speed.py` の `simulate_trip_time` は既に符号を正しく扱っているが（`coast` フェーズの加速度計算）、
`_build_brake_table` の上限速度に `+15.0` のマージンを置いている箇所などは、
下り勾配での惰行加速を考えると不足する可能性がある。フェーズ0で確認する。

---

## 8. 先行列車・後続列車のモデル

### 8.1 先行列車
現行は `input/f_train/coast{V}_stop{D}.csv` を**位置・速度の記録として直接再生**する方式（設計メモ §32 で確立）。
物理再生ではなく記録再生なので、複数駅間へは**素直に延長できる**。

`generate_forward_train.py` をベースに、複数駅間連続版を作る。
```
A駅発車 → 惰行ポイントV → B駅停車(D_B秒) → 発車 → 惰行ポイントV → C駅停車(D_C秒)
```
命名案: `input/f_train_multi/tozai_coast{V}_stopB{D_B}_stopC{D_C}.csv`

**中核シナリオ**（先行のC駅長時間停車）:
- `D_C ∈ {30, 60, 90, 120, 180, 240}` 秒 — 30が標準、120以上が「急病人救護」相当
- `D_B ∈ {30, 45}` 秒
- `V`（惰行ポイント）: 学習時はランダム、検証時は「標準運転曲線と一致するV」と「標準より遅いV」の2点。
  **具体値は車両モデル確定後（§6.5）に `generate_standard_curve.py` を実行して決める**。
- headway `∈ {40〜140}` 秒（学習時ランダム）/ `{140, 120, 100}`（検証時）

**`STD_DEPARTURE_INTERVAL = 140.0`**（ラッシュ時2分20秒間隔）。
既存の headway 換算モデル（`先行の出発遅延 = STD_DEPARTURE_INTERVAL − headway`）はそのまま流用でき、
headway 140 → 先行遅延0秒、120 → 20秒、100 → 40秒となる。

### 8.2 後続列車（フェーズ2.5で実装）

**なぜ重要か — 待機のトレードオフを作る本質的な制約**

先行列車だけの世界では、自列車はいくらでも待てる（`DWELL_MAX` という人工的な上限しかない）。
しかし後続列車がいると、**自列車がB駅で待つほど後続がB駅手前で機外停車する**。
つまり「自列車がC駅手前で機外停車するか、後続がB駅手前で機外停車するか」のトレードオフになる。
これはCLAUDE.mdの目標「先行と後続の列車間隔が一定となるように調整する」そのものであり、
**待機時間の上限が報酬の中に自然に組み込まれる**という意味で設計上きわめて重要。

**運転整理の原則**: 現実には「列車は駅に留める（駅間に溜めない）」が原則である。
- 自列車がB駅で待つ → 自列車は**駅に停車**（客扱い可能・安全）、後続はB駅手前で機外停車
- 自列車が発車 → 自列車がC駅手前で**機外停車**、後続はB駅に入れる

どちらか一方は避けられないため、目的関数は「**列車群全体の機外停車列車数・機外停車時間の最小化**」になる。
評価指標に必ずこれを入れる（§11.3）。

**実装方式（推奨）**: 先行と同じ**記録再生**をベースに、**簡易CBTC追従**を1行だけ足す。
```
後続の位置 = min( 記録軌道の位置,  自列車の位置 − (列車長200m + CBTC停止限界50m) )
```
記録軌道をなぞりつつ、自列車の後方**250m**より前には出られないように頭打ちにする（§6.6）。
物理的な減速パターンまで再現しなくても、「**後続が自列車に詰まって失った時間**」が計測できれば
報酬・評価指標としては十分であり、実装コストが極めて低い。

> 記録再生のまま頭打ちしないと、後続が自列車を追い越す軌跡になり観測 #41 が負値で暴れる。必ず入れる。

**後続列車のシナリオ**:
- `back_headway ∈ {140, 120, 100}` 秒（自列車の後ろ）、および「後続なし」
- 後続の出発遅延 ∈ {0, 30, 60} 秒
- 後続の惰行ポイント V は先行と同じ候補から

**報酬側**: `backward_info` が現在 "後続列車なし" 固定。フェーズ3のLLM再評価で後続情報を入れ、
`reward_features.MODE_CLASSES_ACTIVE` に **`spacing`（間隔調整）を追加**する（現在は「フェーズ2まで学習対象外」として除外されている）。
`hold_at_station` と同じタイミングで投入することで、**LLM評価の実行を1回にまとめられる**（コスト面で重要）。

### 8.3 学習アルゴリズム：Double DQN 化（併せて導入を推奨）
CLAUDE.md「残る副作用（未解決）」に記載のとおり、駅手前の0.1秒刻みによりブートストラップ回数が過剰になり、
vanilla max の DQN では過大評価バイアスが溜まる。複数駅間ではこれが**駅数倍**になる。
- `Actor.rollout` の TD 計算と `Learner.update_network` の TD 計算の両方で、
  行動選択をオンラインネット、価値評価をターゲットネットに分ける。
- 導入前後を `analyze_qnet_coverage.py` の maxQ ヒートマップで比較し、Q値の絶対値が発散していないか確認する。

---

## 9. 実装構成

### 9.1 既存手法の保全（**絶対条件**）

**既存の単一区間手法は、コード・プロンプト・学習済みモデルのすべてを変更せず、いつでも `python apex2.py` で
実行できる状態に保つ。** 複数駅間版は既存を「改良してコピーする」形で新規スクリプトとして作る。

#### 9.1.1 既存手法を成立させている資産（**変更禁止リスト**）

| 層 | ファイル |
|---|---|
| 学習本体 | `apex2.py`, `model.py`, `actions.py`, `segment_tree.py`, `runcurve_plot.py`, `apply_tasc_to_runcurve.py` |
| 環境・物理 | `environment2.py`, `train.py`, `track.py`, `required_speed.py` |
| 報酬推論 | `direct_reward_predictor2.py`, `reward_features.py`, `histogram_loss.py` |
| **学習済みモデル** | `direct_reward_model2.h5`, `direct_reward_gate2.h5`, `direct_reward_scaler2.pkl`, `direct_reward_manifest.json`, `mode_model.h5`, `mode_scaler.pkl`, `mode_manifest.json` |
| **LLM評価プロンプト** | `evaluate_csv_with_llm.py`（プロンプト本文を含む） |
| 学習スクリプト | `train_reward_network2.py` |
| 入力データ | `input/Station.csv`, `input/speed_limit.csv`, `input/grade.csv`, `input/curve.csv`, `input/sr_11.csv`, `input/f_train/`, `input/simulationcase1/` |
| データセット | `train_reward_csv_direct/`, `評価用csv/`, `評価済ログ/` |

（`apex_def.py` / `environment_def.py` は CLAUDE.md で既に書き換え禁止。同じ扱いとする）

#### 9.1.2 特に危険な3つの上書き事故

**(1) 報酬NNの成果物が上書きされる — 最も危険**

`train_reward_network2.py` は実行するたび `direct_reward_model2.h5` / `direct_reward_scaler2.pkl` /
`direct_reward_manifest.json` / `mode_model.h5` / `mode_scaler.pkl` / `mode_manifest.json` を**同じ名前で上書き**する。
複数駅間版の新特徴量（停車判断・後続列車・標準運転曲線）で再学習すると、
**既存の `apex2.py` が入力次元不一致で即座に動かなくなる**（メモリ [[retrain-both-nns-on-feature-change]] の症状）。

→ 複数駅間版は**出力ファイル名を必ず分ける**（§9.1.3）。学習スクリプト自体も別ファイルにする。

**(2) `reward_features.py` の `MODE_CLASSES_ACTIVE` を触ると既存モデルと不整合になる**

現在は `["normal", "delay_recovery", "anti_mid_stop"]` の3クラス。
ここに `hold_at_station` / `spacing` を足すと**モードone-hotの次元が変わり**、
既存の `direct_reward_model2.h5`（3クラス前提で学習済み）と食い違う。

→ `reward_features_multi.py` を新規に作り、既存には手を触れない。

**(3) LLM評価プロンプトの書き換え**

`evaluate_csv_with_llm.py` のプロンプトを変更すると、既存データセットの生成条件が再現できなくなる。
LLM評価は高コストで再実行が容易でないため、**プロンプトは資産として保全する**。

→ `evaluate_csv_with_llm_multi.py` を新規に作り、プロンプトもそちらに持たせる。

#### 9.1.3 新旧の対応表（成果物のファイル名を必ず分ける）

| 種別 | 既存（変更禁止） | 複数駅間版（新規） |
|---|---|---|
| 評価NN（回帰） | `direct_reward_model2.h5` | `direct_reward_model_multi.h5` |
| ゲート分類器 | `direct_reward_gate2.h5` | `direct_reward_gate_multi.h5` |
| スケーラ | `direct_reward_scaler2.pkl` | `direct_reward_scaler_multi.pkl` |
| マニフェスト | `direct_reward_manifest.json` | `direct_reward_manifest_multi.json` |
| モードNN | `mode_model.h5` / `mode_scaler.pkl` / `mode_manifest.json` | `mode_model_multi.h5` / `mode_scaler_multi.pkl` / `mode_manifest_multi.json` |
| 特徴量定義 | `reward_features.py` | `reward_features_multi.py` |
| 予測器 | `direct_reward_predictor2.py` | `direct_reward_predictor_multi.py` |
| NN学習 | `train_reward_network2.py` | `train_reward_network_multi.py` |
| LLM評価＋プロンプト | `evaluate_csv_with_llm.py` | `evaluate_csv_with_llm_multi.py` |
| 学習用CSV置き場 | `train_reward_csv_direct/` | `train_reward_csv_multi/` |
| LLM評価の入出力 | `評価用csv/` / `評価済ログ/` | `評価用csv_multi/` / `評価済ログ_multi/` |
| 先行列車CSV | `input/f_train/` | `input/f_train_multi/` |
| 標準走行曲線 | `input/sr_11.csv` | `input/sr_tozai_*.csv` |

**`histogram_loss.py` は純粋な計算モジュール**（HL-Gaussianのビン中心・教師分布・期待値読み出し）なので、
**変更しない限り共有してよい**。変更が必要になった場合のみ `histogram_loss_multi.py` に分岐する。

#### 9.1.4 回帰確認の手順（各フェーズの完了時に必ず実施）

```bash
# 1. 既存の学習が起動できること（数サイクルで止めてよい）
python apex2.py

# 2. 既存の報酬NNがロードでき、推論が通ること
python analyze_reward_nn_vs_llm.py

# 3. 既存の成果物ファイルが更新されていないこと（タイムスタンプ・ハッシュ）
md5sum direct_reward_model2.h5 direct_reward_scaler2.pkl direct_reward_manifest.json \
       mode_model.h5 mode_scaler.pkl mode_manifest.json
```
フェーズ0の着手前に上記3点のベースラインを取り、**フェーズごとに突き合わせる**。
3のハッシュが変わっていたら、その時点で上書き事故が起きている。

> **推奨**: 着手前に `git commit` して既存資産のスナップショットを固定する。
> `*.h5` / `*.pkl` がリポジトリ管理下にあるか確認し、無ければ別途バックアップを取る。

### 9.2 新規スクリプト一覧

既存ファイルは**変更しない**。`_multi` サフィックスで新規作成する。

| 新規ファイル | ベース | 主な変更点 |
|---|---|---|
| `actions_multi.py` | `actions.py` | 行動を5つに定義（`P1`/`P2`/`C`/`B2`/`B1`）。既存 `actions.py` は変更しない（§4.7.6） |
| `line_config.py` | 新規 | 路線メタデータ（CSVパス・進行方向・駅リスト・標準出発間隔140秒・標準停車時間）＋**車両パラメータ**を1箇所に集約。車両側は 15000系（走行抵抗係数A/B/C・引張力3領域モデル・制動3.5 km/h/s・**列車長200m**） |
| `track_multi.py` | `track.py` | 路線を引数で受ける。進行方向の吸収（座標反転・**勾配符号は保持**）、勾配フィルタ緩和（±40‰）、`curve.csv` 欠損許容。**速度制限（ATC現示）は既存どおり先頭位置で引く**（§6.2.1） |
| `train_multi.py` | `train.py` | Track と**車両パラメータ**をコンストラクタ注入可能にする。走行抵抗・制動減速度・引張力を1箇所に集約（現行は4箇所に重複。§6.5） |
| `required_speed_multi.py` | `required_speed.py` | **プロファイル対応**の `simulate_trip_time` / `brake_stop_distance` / `calculate_required_speed`、`v_limit_pattern`、惰行到達可能性判定（§7）。物理定数は `train_multi.py` と一致させる（制動3.5・走行抵抗A/B/C） |
| `environment_multi.py` | `environment2.py` | **本体**。複数駅間エピソード、停車フェーズ、累積ダイヤ、観測43次元、停車中の `forbidden_action`、後続列車、`v_std(x)` の参照、下り勾配での惰行禁止（§7.7）、**CBTC車間への列車長200m反映**（§6.6） |
| `apex_multi.py` | `apex2.py` | Actor/Learner/Tester のシナリオ生成をマルチ区間化、Double DQN、テストケース再定義 |
| `generate_forward_train_multi.py` | `generate_forward_train.py` | 複数駅間連続の先行／後続列車パターン生成 |
| `runcurve_plot_multi.py` | `runcurve_plot.py` | 複数駅の駅線・停車区間の描画、**標準運転曲線の重ね描き**、時間軸ダイヤグラム |
| `check_forward_observability.py` | 新規 | §5.3 の因果性チェック。シナリオを3分類して表出力 |
| `evaluate_multi.py` | 新規 | **最終評価用の全数実行**。Tester は代表20〜30ケースに絞り、全数はこちらで回す（§11.2） |
| `reward_features_multi.py` | `reward_features.py` | モードに `hold_at_station` / `spacing` を追加（既存の3クラス構成を壊さないため分離。§9.1.2） |
| `direct_reward_predictor_multi.py` | `direct_reward_predictor2.py` | `*_multi` のモデル・スケーラ・マニフェストを読む |
| `train_reward_network_multi.py` | `train_reward_network2.py` | 出力先を `*_multi` に変更（**既存モデルの上書き防止**。§9.1.2） |
| `evaluate_csv_with_llm_multi.py` | `evaluate_csv_with_llm.py` | **プロンプトを含めて複製**し、停車判断・後続列車・標準運転曲線・急勾配の評価軸を追加（§9.1.2） |

**流用できるもの（変更不要）**
- `model.py`（入力次元は引数、出力3のまま）／ `actions.py` ／ `segment_tree.py`
- `direct_reward_predictor2.py` / `histogram_loss.py`（フェーズ3で `state_info` 拡張時に更新）
- `apply_tasc_to_runcurve.py`（駅ごとに適用すればよい。呼び出し側の対応のみ）
- `analyze_qnet_coverage.py`（観測次元が変わるため次元・シナリオ値の更新が必要）

**`generate_standard_curve.py`** は区間ごとに実行して連結する運用にする。
ただし §6.5 で車両モデルを更新する場合、同スクリプトの「物理定数を `train.py` の実値と突き合わせ、
食い違えば例外を出す」仕組みも更新対象になる。

---

## 10. 実装手順（フェーズ別）

### フェーズ0: 車両モデルの決定と路線データ整備（**最重要**）

> **フェーズ0-1 を最初に片付けること。** ここが決まらないと標準運転曲線・惰行ポイントV・`DWELL_MAX` が
> すべて仮置きのままになり、後続フェーズが全部やり直しになる。

**0-0. 既存資産のベースライン取得（§9.1.4）**
- `git commit` で既存資産のスナップショットを固定する。`*.h5` / `*.pkl` が管理下にあるか確認し、無ければバックアップ。
- `md5sum` で既存の学習済みモデル7ファイルのハッシュを記録する。
- `python apex2.py` が起動することを確認する（数サイクルで止めてよい）。

**0-1. 車両モデルの実装（§6.5・仕様は確定済み）**
1. `line_config.py` に15000系の車両パラメータを定義する
   （走行抵抗 A=2.089 / B=0.0394 / C=0.000675、引張力3領域モデル F0=95.626・V1=40・V2=75、
   **制動2.5 km/h/s**、**列車長200m**）。
2. `train_multi.py` で注入できるようにする。**走行抵抗・制動減速度は1箇所に集約**（現行は4箇所に重複。§6.5）。
3. `required_speed_multi.py` の物理定数を `train_multi.py` と一致させる。
4. 最速走行を再計算し、**東陽町→木場 71.3s / 木場→門前仲町 77.3s**（§6.5の実測値）を再現することを確認する。
   併せて標準運転時間ちょうどの省エネ運転（東陽町→木場 79.9s・最高62.4 km/h・惰行開始100m地点）も再現できること。
5. **列車長200mの対応**（§6.6）: CBTC車間 `−250m`、制限速度の最後尾判定、衝突判定のずらし。

**0-2. 路線データのローダ整備**
5. `track_multi.py` を作成。座標反転（**勾配符号は保持**）・勾配フィルタ緩和・curve欠損を吸収する。
6. 単体確認: 任意位置での勾配・制限速度がCSVを手で引いた値と一致するか。特に区間境界を重点的に。
   検算値: 東陽町→木場の距離加重平均 −10.32‰、木場→門前仲町 +11.26‰。

**0-3. 目標速度算出の改修（§7）**
7. `required_speed_multi.py` を作成。プロファイル対応の `simulate_trip_time` / `brake_stop_distance`、
   `v_limit_pattern`、惰行到達可能性判定を実装する。
8. 単体確認: +3‰地点から計算した「惰行で駅に届くか」の判定が、+29.7‰ランプを織り込んだ結果になっているか。
   現行実装との差分を数値で確認する（§7.1 の破綻例が解消されること）。

**0-4. 標準運転曲線の生成**
9. `generate_standard_curve.py` を東西線の各区間で実行し、`rt`（80 / 85秒）で定時・省エネ運転が成立するか確認。
10. `v_std(x)` の参照テーブルと `sr_*.csv` を生成する（§7.6）。
11. 先行・後続列車の惰行ポイント候補Vを確定する。

**0-4b. 列車長200mの影響確認（§6.6）**
- 先行がC駅停車中のとき、自列車の停止限界が「C駅の250m手前」になることを確認する。
- **速度制限（ATC現示）側には列車長を適用しないこと**を確認する（§6.2.1・改訂4の誤りの再発防止）。
- `drive_monitor.py` の列車模式図を**列車長200m**へ更新する（CLAUDE.md の「実スケール・20m」記述も要更新）。

**0-5. 単一区間での動作確認**
12. 東西線の1区間で、既存の単一区間 `apex2.py` 相当の学習が回ることを確認する。
13. 報酬NNの出力が妥当か `analyze_reward_nn_vs_llm.py` で確認する。
    **`current_gradient` が学習データ範囲外（+12.5‰超）なので、分布外の程度を必ず定量化する**（§12 R5）。

> **各フェーズの完了時に §9.1.4 の回帰確認（既存 `apex2.py` の起動・モデルハッシュの不変）を必ず実施する。**

### フェーズ1: 複数駅間1エピソード化（停車時間は固定30秒・先行のみ）
1. `environment_multi.py`: 駅リスト・区間切替・累積ダイヤ・観測拡張（#31〜#40）。
   - 中間駅到着で `done` にせず、`departure_station` / `arrival_station` / `Train.TARGET_STATION` を更新。
   - `section_start_t`、停滞検出のチェックポイントを発車時にリセット。
   - 停車中は `DWELL_TIME_STEP` を使い、停滞検出を無効化。
   - **下り勾配での惰行禁止（§7.7）を実装する。**
2. `generate_forward_train_multi.py`: 先行の複数駅間パターンを生成。
3. `apex_multi.py`: Actor のシナリオ生成、Tester のテストケース、運転曲線の描画（複数駅対応・標準曲線重ね描き）。
4. Double DQN を導入。
5. 短時間学習で、**エピソード長分布・maxQ推移・区間切替時の観測連続性**を確認。

> **フェーズ1のゴール**:
> 1. 東陽町→木場→門前仲町 を通しで走り切り、両駅とも±1m以内に停止する
> 2. **テストケース0（先行・後続なし・遅延なし）で標準運転曲線に沿う**（§7.6 の要件）
>
> 発車判断はまだ学習していない（停車は常に30秒）。
> 併せて §5.4 の「CBTC干渉開始タイミング」を実走行ログから実測し、`DWELL_MAX` を確定させる。

### フェーズ2: 発車判断の導入（暫定ルール報酬・先行のみ）
1. 停車中の `forbidden_action`（発車／待機の2択、最低30秒・上限240秒）を実装。
2. §5.2 の暫定ルール報酬を実装。**環境側の報酬であることをコードに明記**し、フェーズ3で置き換える旨を書く。
3. `check_forward_observability.py` で、学習・検証シナリオが因果的に成立するか確認（§5.3）。
   シナリオを3分類し、**3種すべてを学習に含める**。
4. 学習し、「先行がC駅で長時間停車 → B駅で待つ」が出るか確認。

> **フェーズ2のゴール**: 中核目標（§1.1）の挙動が**構造的に学習可能**であることの実証。
> ここで出なければMDP設計に問題があるので、フェーズ3（コストの高いLLM再評価）に進んではいけない。

### フェーズ2.5: 後続列車の環境実装（暫定ルール報酬のまま）
1. `environment_multi.py` に後続列車を追加（記録再生＋簡易CBTC追従の頭打ち。§8.2）。
2. 観測 #41〜#43 を追加。
3. 暫定ルール報酬に `crowded`（後続を詰まらせている）項を追加。
4. 「自列車が待つ vs 後続が詰まる」のトレードオフが方策として観測できるか確認する。

> **フェーズ2.5のゴール**: 後続込みの環境が動き、総機外停車時間が指標として取れること。
> ここまでで**フェーズ3のLLM評価に必要な状態量が全部そろう**。

### フェーズ2.6: 行動空間の5ノッチ化（§4.7）
1. `actions_multi.py` で5行動を定義。`train_multi.py` の `step()` に `P2` / `B2` を追加
   （**P2の引張力クリップ `min(TF(v), R(v)+35)` と起動時アサーションを必ず入れる**）。
2. `model.py` の QNetwork 出力を5次元に（入力次元と同様に引数化）。
3. `forbidden_action` を5要素に。停車中は2択に絞る（§4.3）。
4. 暫定ルール報酬を5ノッチに対応させ、**3ノッチ版（P1a）との比較**を取る（§4.7.7）。
5. `runcurve_plot_multi.py` / `drive_monitor.py` のノッチ別配色を5色に。

> **フェーズ2.6のゴール**: 5ノッチで学習が回り、下り勾配（東陽町→木場）の維持帯が
> 実際に狭くなる（帯幅11.0→3.3 km/h・§4.7.3）ことを実走行で確認する。
> 3ノッチ版を残しておき、B1 vs P1a vs P1b のアブレーションを成立させる（§4.7.7）。

### フェーズ3: 報酬をLLM蒸留に置き換え（本番化・LLM評価は1回にまとめる）
> **保全（§9.1）**: `reward_features.py` / `evaluate_csv_with_llm.py` / `train_reward_network2.py` と
> 既存の学習済みモデル7ファイルには**一切触れない**。すべて `_multi` 側で行う。
>
1. `reward_features_multi.py`（新規）の `MODE_CLASSES_ACTIVE` に **`hold_at_station` と `spacing` を同時に追加**。
2. `evaluate_csv_with_llm_multi.py`（既存をプロンプトごと複製）に以下の評価軸を追加:
   - 駅停車中の発車判断の評価軸（`hold_at_station`）
   - 後続列車・間隔調整の評価軸（`spacing`）
   - **標準運転曲線からの逸脱の評価軸**（通常運転では沿うのが最良。§7.6）
   - 急勾配・制限速度変化への対応の評価軸（§7.4・§7.5）
   - **5ノッチの評価軸**（`P2`/`B2` の正しい使い所、ノッチ間距離によるノコギリ重み付け。§7.4.3(7-2)）
3. フェーズ2.5までの走行ログからデータセットを作成し、LLM評価を実行。
   **ここが最もコストの高い工程なので、状態量の設計が固まってから一度で済ませる。**
   **東西線条件（−16.3〜+29.7‰、制限45〜75km/h）のデータを必ず十分量含める**（§12 R5）。
4. `state_info` を拡張し、**評価NN・モードNNをセットで再学習**（`train_reward_network_multi.py`）。
   出力先は `direct_reward_model_multi.h5` 等（§9.1.3）。**既存の `direct_reward_model2.h5` は更新されないこと**を
   §9.1.4 の手順で確認する。
5. `analyze_reward_nn_vs_llm.py` / `analyze_reward_imbalance.py` で検証。
   **停車中サンプル・後続ありサンプル・急勾配サンプルの不均衡を必ず診断**
   （[[balanced-mse-negative-result]] の教訓も参照）。
6. 暫定ルール報酬を撤去し、本学習。

### フェーズ4: 評価と論文化
`evaluate_multi.py` で全シナリオを実行し、§11 の指標・図表を作成する。

---

## 11. 研究手順（実験計画）

### 11.1 比較する手法

| ID | 手法 | 内容 |
|---|---|---|
| **B0** | 数理モデル基準 | `generate_standard_curve.py` を各区間に適用し連結。省エネ・定時の理論上限（先行なし条件）。**テストケース0の正解** |
| **B1** | 単一区間独立最適化 | 現行 `apex2.py` を区間ごとに学習し、逐次実行。停車時間は固定30秒 |
| **B2** | 単一区間＋ルールベース発車抑止 | B1 に「先行が次駅で標準超過停車中なら発車しない」ルールを外付け |
| **P1a** | 複数駅間1エピソード（停車固定・**3ノッチ**） | フェーズ1の成果。B1との差＝エピソード構造の効果 |
| **P1b** | 複数駅間1エピソード（停車固定・**5ノッチ**） | フェーズ2.6の成果。P1aとの差＝行動空間の効果（§4.7.7） |
| **P2** | 複数駅間1エピソード＋発車判断 | フェーズ2/3の成果（**提案手法・先行のみ**） |
| **P3** | P2 ＋ 後続列車考慮 | フェーズ2.5/3の成果（**提案手法・完全版**） |

B2 を入れる意義: 「発車抑止は単純なルールで十分では？」という当然の指摘に答えるため。
P2 が B2 を上回るとすれば、**待つ／待たないの二値ではなく「どれだけ待つか」「待った後どう走るか」を
連続的に最適化できる点**にあるはずで、その仮説を検証する形になる。
特に §5.3 の「判断困難（H=100で先行の延着がまだ観測できない）」シナリオでは、
ルールベースは判断材料が無く機能しないはずで、ここが P2 の優位が最も出やすい。

### 11.2 評価シナリオ

```
先行のC駅停車時間 D_C  ∈ {30, 60, 90, 120, 180, 240} 秒      … 6
出発間隔 headway       ∈ {140, 120, 100} 秒                   … 3
先行の惰行ポイント V   ∈ {標準相当, 標準より遅い}             … 2
自列車の出発遅延       ∈ {0, 15, 30} 秒                       … 3
後続 back_headway      ∈ {なし, 140, 120, 100} 秒             … 4
                                              合計 432ケース
```
※**テストケース0**（先行なし・後続なし・遅延0）は上記と別に必ず含める。標準運転曲線との一致度で評価する。

**実行の分担**
- **Tester（学習中に定期実行）**: 代表 **20〜30ケース**に絞る。現行 `apex2.py` の22ケースと同規模に保つ。
- **`evaluate_multi.py`（最終評価）**: 全432ケースを固定シードで実行。手法間で完全に同一条件を揃える。

### 11.3 評価指標

| 分類 | 指標 |
|---|---|
| **標準運転曲線との一致** | **テストケース0での `v_std(x)` との RMSE・最大偏差**、力行エネルギー比、ノッチ切替回数比（§7.6の要件の直接評価） |
| **安全・機外停車** | **総機外停車列車数**／**総機外停車時間**（自列車＋後続。§8.2 の目的関数）、自列車の機外停車率、CBTC停止限界までの最小距離、異常接近の発生数 |
| **定時性** | C駅到着遅延、B駅発車遅延、**後続列車の遅延（自列車に起因する分）**、区間別の遅延 |
| **列車間隔** | 先行との車間距離の分散、後続との車間距離の分散、両者の対称性 |
| **省エネ** | 力行エネルギー（区間別・通算） |
| **乗り心地** | ノッチ切替回数、加速度変化率、無駄な加減速の回数 |
| **勾配・現示への対応** | +29.7‰ランプ進入速度・通過最低速度、75→70低下点での現示超過の有無、−16.3‰下りでの現示超過回数、現示低下点手前の減速の滑らかさ（加速度変化率） |
| **停止精度** | 木場・門前仲町の停止位置誤差（TASC適用前／適用後の両方） |
| **学習の健全性** | エピソード長分布、maxQ の推移、`analyze_qnet_coverage.py` の行動間ギャップ |

**主要な図表**
- 図1: 先行のC駅停車時間 D_C 対 **総機外停車時間**（手法別）— 提案手法の主張の核
- 図2: D_C 対 C駅到着遅延（手法別）— 「待つと遅れる」トレードオフの定量化
- 図3: D_C 対 B駅停車時間（P2/P3の学習結果）— **待機時間が状況に応じて連続的に変わるか**。
  §5.4 の設計計算の表と重ねると「理論上必要な待機時間」との比較になる
- 図4: **テストケース0の運転曲線 — 標準運転曲線（B0）とDQN（P1〜P3）の重ね描き**（§7.6の要件の可視化）
- 図5: D_C = 180秒 の代表シナリオでのダイヤグラム重ね描き（B1 vs P2 vs P3、自列車＋先行＋後続）
- 図6: 力行エネルギーとノッチ切替回数（手法別・箱ひげ）
- 表1: 全指標のサマリ（手法 × 指標）
- 表2: シナリオ3分類（発車抑止が正解／定時発車が正解／判断困難）別の成績

### 11.4 可視化
`drive_monitor.py` を複数駅・停車フェーズ・後続列車・**標準運転曲線の重ね描き**に対応させる。
時刻-位置のダイヤグラムで「自列車・先行・後続の3本の軌跡」が並ぶため、
B駅での発車抑止が効いているか、後続がどれだけ詰まったかが一目で分かる。
**フェーズ1完了時点で複数駅＋標準曲線対応、フェーズ2.5で後続対応**させることを推奨。

---

## 12. リスク一覧と対策

| # | リスク | 影響 | 対策 |
|---|---|---|---|
| ~~R0~~ | ~~現行車両モデルでは標準運転時間が達成不能~~ | — | **解決済み（2026-08-21）**。15000系10両で両区間とも11〜16%の余裕をもって成立（§6.5） |
| **R0'** | **列車長200mがコード上どこにも存在しない**（§6.6・確認済み） | CBTC車間が**200m甘くなる** | `line_config.py` に列車長を持たせ、**CBTC・衝突判定・後続の頭打ち・可視化の4箇所**に反映する。**速度制限（ATC現示）には適用しない**（§6.2.1） |
| **R0'''** | **報酬NNの再学習が既存モデル7ファイルを同名で上書きし、`apex2.py` が動かなくなる** | 既存手法が失われ、ベースライン比較（§11.1 B1/B2）が不可能になる | `_multi` 系で出力名を分離（§9.1.3）。各フェーズ完了時に `md5sum` で不変を確認（§9.1.4） |
| **R0''** | 引張力曲線の V1（定トルク終端40km/h）・V2（定出力終端75km/h）は**PDFに記載が無く一般値** | 中速域の加速性能が実車とずれる | 感度確認を行う（V1=38〜45、V2=65〜80）。標準運転曲線・所要時分への影響を見る（§13） |
| **R1** | **目標速度算出が勾配・制限をスカラー1つで近似**（§7.1・確認済み） | 惰行減速度を最大3.1倍過小評価し、駅間停車や遅延を招く | プロファイル対応へ改修（§7.2）。フェーズ0-3 |
| R2 | 停車中の待機でエピソードが延び、Q値が発散（設計メモ §29 の再発） | 学習崩壊 | `DWELL_MAX`=240秒の強制発車＋累積タイムオーバー＋毎サイクルのエピソード長監視（§4.5） |
| R3 | 停車中の3行動が同一結果になり過大評価が累積（設計メモ §26 の再発） | 方策の無差別化 | 停車中は `coasting` を禁止して2択にする（§4.3） |
| R4 | 座標が減少方向であることを見落とす | 全ロジックが破綻 | ローダで内部座標へ変換（§6.2）。**勾配符号は保持**（§6.3・確認済み） |
| R5 | **報酬NNの学習データが東西線の勾配範囲を全く含まない**（学習データ −6.7〜+12.5‰ vs 東西線 −16.3〜+29.7‰） | 急勾配区間の報酬が不正確 | フェーズ0-5で分布外の程度を定量化。**フェーズ3で東西線条件の評価データを必ず追加** |
| R6 | `track.py` の勾配フィルタ（`g <= 30`）に **+29.7‰ が0.3‰差で接している** | 区間拡張やデータ更新で静かに0.0に潰れる | `track_multi.py` で ±40‰ に緩和（§6.2） |
| R7 | **−16.3‰下りでは惰行しても加速する**（設計メモ §18 の前提が崩れる。15000系でも同様） | 制限速度超過を惰行で解消できない | `forbidden_action` を勾配依存にする（§7.7） |
| R7' | 物理パラメータ（走行抵抗・制動減速度）が**4箇所に重複**している | 一部だけ更新して静かに不整合になる | `_multi` 版で1箇所に集約する（§6.5 末尾に該当箇所を列挙） |
| R8 | headway=100 で先行の延着が発車判断時点に観測できない（§5.3） | 判断不能なシナリオで学習失敗 | 「情報獲得のための待機」として学習させる。観測 #36 を必ず入れる。`check_forward_observability.py` で事前分類 |
| R9 | 後続列車を記録再生のまま流すと自列車を追い越す | 観測 #41 が負値で暴れる | 簡易CBTC追従の頭打ちを必ず入れる（§8.2） |
| R10 | 停車中サンプル・後続ありサンプルがラベル不均衡 | 発車判断・間隔調整が学習されない | `analyze_reward_imbalance.py` で診断。データ生成時に該当シナリオを厚くする |
| R11 | エピソード長2〜3倍でサンプル効率が落ちる | 実験サイクルの遅延 | フェーズ1では停車固定でエピソードを短く保つ |
| R12 | 駅数分の0.1秒刻み区間でブートストラップが過剰 | Q値過大評価 | Double DQN 化（§8.3）。`analyze_qnet_coverage.py` で前後比較 |
| R13 | 停車中の遷移がリプレイバッファを圧迫（停車240秒＝240遷移） | 走行中の遷移が希釈される | `DWELL_TIME_STEP` を粗くする、またはセミMDP化（§4.3） |
| R14 | 評価シナリオ432ケースをTesterで回すと学習が止まる | 学習速度の低下 | Testerは代表20〜30ケース、全数は `evaluate_multi.py` で別途（§11.2） |
| R15 | 車両モデル・標準運転曲線・報酬NN の世代がずれる | 報酬が黙って壊れる | **3点セットの世代を揃えて運用**する（CLAUDE.md のモデル／スケーラ／マニフェスト運用と同じ考え方。§7.6） |

---

## 13. 未決事項（着手前に決めたいこと）

1. ~~東陽町の45km/h制限を列車長で判定するか~~ → **解決（2026-08-21）**。
   速度制限データは**CS-ATCの信号現示**であり先頭位置で判定する。列車長は考慮しない（§6.2.1）。
2. **引張力曲線の V1・V2**（§6.5）— PDFに記載が無いため一般値（40 / 75 km/h）を置いた。
   実車の特性データが入手できれば差し替える。無ければ感度確認（V1=38〜45、V2=65〜80）で影響を評価する。
3. ~~制動減速度~~ → **確定: `DECELERATE = -2.5 km/h/s`**（PDFの3.5は常用最大。駅停車は2.5）。
   CBTCパターンも同じ2.5で構築する（§6.5）。
4. **慣性係数**（§6.5）— 現行の `FACTOR_OF_INERTIA = 28.34467`（慣性係数≈1.0）を維持する方針だが、
   実車相当（1.08 → 約30.6）へ変えるか。変える場合は F0 も再計算する。差は勾配・抵抗の効き方で約7%。
5. **先行・後続の惰行ポイント V の具体値** — `generate_standard_curve.py` を15000系で実行して決める。
6. **γの引き上げ**（0.9975 → 0.999）を実験項目に含めるか。
7. **後続列車の「詰まり」をどこまで再現するか** — §8.2 の簡易CBTC頭打ちで十分か。
   まずは簡易版で始め、指標として不足したら精緻化する方針を推奨。
8. ~~維持帯の `T_min`~~ → **確定: 5秒**（2026-08-21）。ノコギリ判定のしきい値も7秒→5秒に統一する。
   7秒では東陽町→木場が81.0秒で定時未達となるため、5秒は必要条件（§7.4.3(5)）。
9. **門前仲町→茅場町を加えた4駅への拡張**を最終的なスコープに含めるか。
   §7.4.1 で事前検証済みであり、**定時到着は (a) 予見型のままでも成立する**（最速131.9秒・余裕5.8%）。
   拡張する場合に追加で必要になるのは以下:
   - **±35‰の急勾配** → `track.py` の勾配フィルタが+35‰を0.0に潰す（§6.2(2)）。`track_multi.py` では対処済み。
     内訳は −35‰が235m、**+35‰が380m**、−35‰が217m（内部座標 2.5775〜3.4097）。
     **380mの急勾配が丸ごと平坦扱いになる**ため、フィルタ緩和は必須
   - **(b) 追従型への切替**（必要なら）→ 追加データ不要で実装できる（§7.4.1）。余裕が5.8%→7.9%に増える
   - **曲線半径データ** → 所要時分にはほぼ影響しないが（§7.4.1）、力行仕事＝省エネ評価の忠実度が上がる。
     55km/h現示の区間が全体の50%を占めるため、**エネルギー比較を主張に使うなら用意したい**

   ※ 現在の3駅スコープでは上記いずれも**不要**。

---

## 14. フェーズ0の実施結果（2026-08-21）

### 14.1 作成したもの

| ファイル | 内容 |
|---|---|
| `line_config.py` | 路線メタデータ＋車両パラメータ。東西線（下りB線・ATC現示・標準運転間隔140秒）と15000系（走行抵抗A/B/C・引張力3領域・制動2.5・列車長200m・勾配補償35‰） |
| `track_multi.py` | 内部座標への変換（下りB線対応・**勾配符号は保持**）、勾配フィルタ緩和（範囲外は**例外**）、曲線欠損許容、ATC現示の先頭位置判定、現示低下点の先読み |
| `actions_multi.py` | 5行動の定義（P1/P2/C/B2/B1） |
| `train_multi.py` | 5ノッチの運動モデル。走行抵抗・制動・引張力を**1箇所に集約**（既存は4箇所に重複）。**P2の引張力クリップと起動時アサーション**を実装 |
| `generate_standard_curve_multi.py` | 標準運転曲線ソルバ。ATC先読み天井・制動曲線の逆積分・V_holdグリッド探索・x_coast二分探索 |
| `generate_eval_csv_multi.py` | LLM評価用の走行ログCSV生成（§14.3f） |
| `evaluate_csv_with_llm_multi.py` | LLM評価ランナー（並列・中断再開・応答検証つき。§14.3g） |
| `generate_forward_train_multi.py` | 先行／後続列車の走行パターン生成（惰行ポイント方式・複数駅間・登り勾配での再力行付き。§14.3d） |
| `required_speed_multi.py` | **目標速度の算出**。プロファイル対応の制動距離・ATC先読み天井・CBTC現示・維持帯・惰行到達可能性・モード別目標速度（§14.3c） |
| `prompt_multi.py` | LLM評価プロンプト（§7.4.3の設計を反映） |
| `optimal_curve_dp.py` | **検証用**。運転パターンを仮定しない動的計画法でエネルギー最小の理論下界を求める（§14.3b） |

**既存資産の保全（§9.1.4）は確認済み。** 学習済みモデル7ファイルのMD5は全て不変、
既存スクリプト（`track.py` / `train.py` / `environment2.py` / `apex2.py` / `evaluate_csv_with_llm.py` 等）も未変更。

### 14.2 標準運転曲線の生成結果 — **全3駅間で成功**

| 区間 | 距離 | 標準 | 到着 | 停止位置誤差 | V_hold | 最高速 | 力行仕事 | 制動仕事 | ノッチ切替 |
|---|---|---|---|---|---|---|---|---|---|
| 東陽町 → 木場 | 964.5 m | 80 s | **79.78 s** | **−0.000 m** | 58.75 km/h | 62.2 km/h | 88.9 kJ/t | 130.4 kJ/t | **4回** |
| 木場 → 門前仲町 | 1058.0 m | 85 s | **84.92 s** | **−0.000 m** | 74.00 km/h | 74.0 km/h | 247.5 kJ/t | 65.6 kJ/t | **4回** |
| （参考）門前仲町 → 茅場町 | 1801.8 m | 140 s | **139.90 s** | **−0.002 m** | 64.00 km/h | 64.0 km/h | 292.2 kJ/t | 219.6 kJ/t | **9回** |

**停止位置誤差はいずれもミリメートル級**であり、勾配・ATC現示が複雑な路線でも運転曲線が正しく引けることを確認した。

**区間ごとの性格の違いがそのまま現れた**（§6.5の予測と一致）:
- **東陽町→木場（下り −16.3‰）**: 力行98mで62.2 km/hまで上げた後、**惰行のまま重力で加速**しながら775mまで走り制動。
  力行仕事 88.9 kJ/t は他区間の1/3以下。下り勾配の省エネ運転の理想形。
- **木場→門前仲町（登り +29.7‰）**: 現示一杯の73 km/hまで力行し、+27‰ランプの入口で**勾配力行(P2)**を使って速度を保ち、
  以降は惰行で48 km/hまで落として制動。力行仕事は東陽町→木場の2.8倍。

### 14.3 ±35‰で中間ノッチが真の定速保持を行うことを確認（§4.7の狙いどおり）

延伸区間（門前仲町→茅場町）の実走行で、5ノッチの設計意図が確認できた。

| 区間 | 勾配 | 使用ノッチ | 挙動 |
|---|---|---|---|
| 550〜790 m | **−35.0‰** | **勾配ブレーキ(B2)** を15.7秒連続 | 速度54 km/hを**完全に平坦に保持** |
| 790〜1170 m | **+35.0‰** | **勾配力行(P2)** を継続 | 同じく54 km/hを平坦に保持 |

3ノッチではこの区間で惰行↔制動（または力行）を高速で往復するしかなく、
ノッチ切替が多発して乗り心地が崩れる。**中間ノッチの追加が最も効くのがこの局面**である。

なお当該区間のノッチ使用時間は 力行20.2s / 勾配力行24.5s / 惰行48.9s / 勾配ブレーキ25.5s / 制動20.8s であり、
**中間ノッチが全体の35%を占める**。

### 14.3b 「本当にエネルギー最小か」の検証と、運転曲線の作り直し（2026-08-21）

初版の門前仲町→茅場町は**ノッチ切替19回**で、制動と加速を繰り返す運転になっていた。
「これは本当に消費エネルギーが低いのか」を検証し、運転曲線を作り直した。

#### (1) 動的計画法で理論下界を確認した

運転パターンを一切仮定せず、位置×速度の格子上で
`E_力行 + λ·T` を最小化する動的計画法（`optimal_curve_dp.py`）を解いた
（λを二分探索して所要時間を標準運転時間に合わせるラグランジュ緩和）。

| 手法 | 所要 | 力行仕事 | 制動仕事 | ノッチ切替 |
|---|---|---|---|---|
| DP（力行仕事のみを最小化） | 141.35 s | **257.6 kJ/t** | 187.2 kJ/t | **40回** |
| 本ソルバ（作り直し後） | 139.90 s | 292.2 kJ/t | 219.6 kJ/t | **9回** |

**理論下界に対して力行仕事は約13%増だが、ノッチ切替は1/4以下**である。
DPは切替を一切考慮しないため40回も切り替える運転を選ぶ。乗り心地を考えれば実用解ではない。
基準曲線としては「多少エネルギーを譲ってでも切替が少なく惰行の長い運転」が望ましいため、本ソルバの解を採用する。

#### (2) 制動の大半は経路に内在するもので、削れない

制動仕事219.6 kJ/tの内訳はおおよそ次のとおり。
- **駅停車の制動**（50 km/hからの停止）: 約96 kJ/t — 不可避
- **−35‰の下りをATC現示55 km/hの下で走る**ための制動: 残り

**−35‰の急な下りが現示55 km/hの区間の中にある**ため、重力で得た速度を捨てるしかない。
これは運転方法の問題ではなく路線条件である。
なお現示が70 km/hに戻る区間（1170〜1390 m）では、**惰行のまま41.7 → 58.0 km/h まで回復**しており、
重力を無動力で回収できている（運転曲線の緑の上り勾配部分）。

> **本来の制限区間データがあれば改善の余地がある。** ATC現示は実際の制限区間の手前で切り替わるため、
> 現示55 km/hの区間は実際の制限区間より長い。実際の制限区間が短ければ、その分だけ高い速度で
> 走れて強制的な制動が減る。§7.4.1 の (b) 追従型モデルと、§13-9 の「本来の制限区間の速度データ」が
> ここで効いてくる（現時点の3駅スコープには −35‰ が無いため影響しない）。

#### (3) ソルバの3つの改良

**① 維持帯を固定幅から可変幅へ**（§7.4.3(5) と同じ規則をソルバにも適用）
初版は帯幅を1.5 km/h固定にしていたため、勾配によって相の継続時間が数百msまで縮み切替が多発した。
**両相が `T_min`（既定5秒）以上continueする最小幅**を、その地点の勾配・速度から都度算出する方式に変更。
真の定速ノッチがある勾配（±35‰でのB2/P2）では帯を最小限（0.6 km/h）にする。

**② エネルギーが同等なら切替の少ない解を選ぶ**
力行仕事が最小値の**2%以内**の候補のうち、**ノッチ切替が最少**のものを採る。
実測では木場→門前仲町で、力行仕事の差0.1%に対し切替が **4回 vs 20回** という候補が並んでいた。

**③ 候補の選定を最終刻みで行う**
探索は0.05秒刻み、最終解は0.01秒刻みで解いていたため、探索段階の切替回数が最終解と一致しなかった
（探索では4〜6回に見えていた候補が、最終刻みでは20回になる）。
上位候補を**最終刻みで解き直してから**選ぶように変更した。

#### (4) 作り直しの結果

| 区間 | 力行仕事 | 制動仕事 | ノッチ切替 | 惰行時間 |
|---|---|---|---|---|
| 門前仲町→茅場町（初版） | 290.3 kJ/t | 未計測 | 19回 | 48.9 s |
| 門前仲町→茅場町（**作り直し後**） | 292.2 kJ/t | 219.6 kJ/t | **9回** | **54.9 s** |

エネルギーはほぼ同等のまま、**切替が半減し惰行時間が増えた**。
対象3駅（区間0・1）はいずれも**切替4回**であり、「力行 → 惰行 → 制動」の素直な運転になっている。

### 14.3c 目標速度算出モジュールの実装（0-3・2026-08-21）

`required_speed_multi.py` を作成した（既存 `required_speed.py` は変更なし）。
§7.3 の三層構造と、モード別の要件を実装している。

#### (1) モード別の目標速度

| モード | 目標速度 | 実装 |
|---|---|---|
| `normal` | **`v_std(x)`** | `generate_standard_curve_multi.py` が出力したCSVを位置→速度のテーブルとして読み、線形補間。定時とエネルギー効率を同時に満たす基準曲線に沿わせる |
| `delay_recovery` | **`v_ceiling` 直下の維持帯** | `band()` が勾配・速度から `band_upper`／`band_lower`／使用ノッチ対を返す。帯幅は「両相が T_min=5秒 以上continueする最小幅」（§7.4.3(5)） |
| `anti_mid_stop` | **`target_speed_no_stop`** | 実効所要時間 = max(定刻残り, 先行クリア残時間 + 余裕15秒) に対し、「その速度を保って走るとちょうどその時刻に着く」速度を二分探索 |
| `spacing` | `target_speed_spacing` | 前後の車間の偏りを所要時間の補正に写して同様に算出 |
| 全モード共通の上限 | **`v_ceiling` = min(ATC先読みパターン, 先行によるCBTC現示)** | CBTC停止限界＝先行の**最後尾**から50m手前＝先行の先頭から250m手前（列車長200m。§6.6） |

#### (2) 「勾配・現示を考慮する」ことの実証

**制動距離が前方プロファイルで変わる**（既存はスカラー1つで駅まで一定と仮定していた）

| 位置（東陽町→木場） | 勾配 | 70 km/h からの制動距離 |
|---|---|---|
| 300 m | −16.3‰ | **321.0 m** |
| 500 m | −16.3‰ | 294.2 m |
| 700 m | −16.3‰ | **248.0 m** |

同じ −16.3‰ 地点でも 321.0 → 248.0 m と変わる。制動中に 707 m 地点で +2.0‰ へ変わることを積分が織り込むため。
スカラー近似では全て 321.0 m になり、駅側で **73 m 過大評価**して早すぎる制動を招く。

**維持帯のノッチ対が勾配で入れ替わる**

| 区間・位置 | 勾配 | 維持帯 | 帯幅 | 使用ノッチ対 |
|---|---|---|---|---|
| 東陽町→木場 300 m | −16.3‰ | 68.1〜71.4 | 3.3 km/h | **惰行 ↔ 勾配ブレーキ** |
| 木場→門前仲町 500 m | +27.0‰ | 66.8〜73.0 | 6.2 km/h | **勾配力行 ↔ 惰行** |
| 木場→門前仲町 700 m | +29.7‰ | 63.0〜**69.6** | 6.6 km/h | 勾配力行 ↔ 惰行 |

700 m 地点で天井が 71.6 km/h に下がっているのは、707 m の現示低下（75→70）を先読みしているため（§7.4）。

**機外停車防止の上限が先行クリア時間で下がる**（木場→門前仲町・発車100 m・残り70秒）

| 先行クリア残時間 | 0 s | 60 s | 90 s | 120 s | 180 s |
|---|---|---|---|---|---|
| `target_speed_no_stop` | 58.8 | 53.1 | **35.2** | 26.6 | **18.1** km/h |

**CBTC現示が停止限界までの距離で下がる**（停止限界＝先行の先頭から250 m手前）

| 先行の先頭まで | 300 m | 400 m | 500 m | 700 m |
|---|---|---|---|---|
| 停止限界まで | 50 m | 150 m | 250 m | 450 m |
| CBTC現示 | **31.4** | 54.9 | 73.7 | 75.0（線路条件） km/h |

**惰行到達可能性で「何km/h落ちたら加速」の閾値を置かずに済む**（§7.5）
木場→門前仲町の300 m地点（+4.0‰、この先に+27〜29.7‰のランプ）から惰行した場合:

| 速度 | 55 km/h | 65 km/h | 73 km/h |
|---|---|---|---|
| 判定 | **✗ ランプで失速＝駅間停車** | ○ 届く | ○ 届く |

閾値を書かずとも、プロファイル積分が「ランプを越えられるか」を直接答える。

#### (3) 高速化 — 二分探索の内側から積分を追い出す

素直に実装すると学習が律速する（毎ステップ呼ばれるため）。

| 関数 | 素直な実装 | 高速化後 | 倍率 |
|---|---|---|---|
| `required_speed` | 12.33 ms | **0.15 ms** | 82× |
| `target_speed_no_stop` | 56.26 ms | **0.088 ms** | 638× |
| `targets()` 一式 | 24.62 ms | **0.45 ms** | 55× |

高速化の要点:
- **制動テーブルに残り時間も持たせる** → 「Vから制動して停止するまでの時間」がテーブル参照になる
- **`V → 制動開始位置` の逆引き**を事前計算する
- **力行軌跡を1回だけ積分**し、候補Vごとに「Vに達する点」を引く（二分探索の内側から積分を追い出す）
- **保持時間を位置グリッド上の総和**で求める（`Σ dx / min(V, 現示)`・numpyでベクトル化）

`SpeedProfile` の構築コストは 6.6 ms で、区間に入るとき（reset・区間切替）に1度だけ払う。

#### (4) 勾配急変点での平滑化

維持帯のノッチ対を**その地点の瞬時勾配**で決めると、勾配が急変する地点で帯が飛んでノッチが振動する
（§14.4 と同じ問題）。`hold_notches()` は**前方50 mの距離加重平均勾配**で判定する。
実際、東陽町→木場の700 m地点は瞬時値が −16.3‰ だが、50 m先に +2.0‰ への変化があるため
平均は正側へ寄り、ノッチ対が「惰行↔勾配ブレーキ」から「勾配力行↔惰行」へ切り替わる。

### 14.3d 先行／後続列車の走行パターン生成（0-4残・2026-08-23）

`generate_forward_train_multi.py` を作成した（既存 `generate_forward_train.py` は変更なし）。
既存と同じ**惰行ポイント方式**を複数駅間へ延長したもの。

#### (1) テストケースは2種

| パターン | 惰行ポイント V | 由来 |
|---|---|---|
| `normal`（通常運転） | 東陽町発 **48.7** ／ 木場発 **74.0** km/h | 標準運転曲線の**惰行開始速度**（§14.2の解より） |
| `slow`（低速運転） | 東陽町発 **40.0** ／ 木場発 **55.0** km/h | 指定値 |

| パターン | 東陽町→木場 | 木場→門前仲町 | 標準比 |
|---|---|---|---|
| `normal` | 79.9 s（標準80 s） | 86.3 s（標準85 s） | ほぼ定時 |
| `slow` | 86.2 s | 95.2 s | **+6.2 s / +10.2 s の遅れ** |

停止位置誤差は全パターン **0.00 m**。
駅停車時間は 木場 D_B ∈ {30, 45}、門前仲町 D_C ∈ {30, 60, 90, 120, 180, 240} 秒の
組合せで 2×2×6 = **24ファイル**を `input/f_train_multi/` に出力する。

#### (2) 登り勾配での再力行（35 km/h）は**必須**だった

**登り勾配（>1‰）で速度が 35 km/h を下回ったら V まで再力行する**ルールを入れた。
木場→門前仲町の +27〜29.7‰ ランプで検証した結果、このルールが無いと成立しない。

| 惰行ポイント V | 再力行あり | 再力行なし |
|---|---|---|
| 74 km/h | 到着（ランプ最低速 46.3 km/h・再力行は発動せず） | 到着 |
| 65 km/h | 到着（ランプ最低速 35.0・再力行 12.8 s） | — |
| 55 km/h（slow） | 到着（ランプ最低速 35.0・再力行 11.6 s） | — |
| 45 km/h | 到着（再力行 13.6 s） | **✗ 481 m 手前で失速＝駅間停車** |
| 40 km/h | 到着（再力行 15.6 s） | **✗ 532 m 手前で失速** |
| 35 km/h | 到着（再力行 16.9 s） | **✗ 578 m 手前で失速** |

再力行ありでは全Vで停止位置誤差 0.00 m。ランプ通過時の最低速度がちょうど 35.0 km/h で
下げ止まっており、しきい値どおりに機能している。

> 下り勾配（東陽町→木場の −16.3‰）では惰行でも加速するため、
> ATC現示に近づいたら**勾配ブレーキ(B2)**で頭を抑える処理も入れてある。

### 14.3e プロンプトの全点検（2026-08-23）

`prompt_multi.py` を既存 `evaluate_csv_with_llm.py` のプロンプトと突き合わせて点検した。

#### (1) 機械的な整合（自動チェック）

| 項目 | 結果 |
|---|---|
| `FEATURE_KEYS`(46) が全て供給されるか（`targets()` 17 + 環境側 29） | ○ 欠落なし |
| プロンプト本文が参照するキーが `FEATURE_KEYS` に在るか | ○ 全45キー在り |
| `MODES`(5) と本文中の `mode = "..."` の一致 | ○ 一致 |
| 車両・路線依存の固定数値の残存（§7.4.3(8) の削除対象6種） | ○ 全て除去済み |
| 旧プロンプトの主要ルール16項目の引き継ぎ | ○ 全て引き継ぎ済み |

#### (2) 検出した問題と修正（5件）

**① 【重大】`v_ceiling` と `signal_speed` の混同**
「基準速度の三層構造」では *v_ceiling を超えたら reward=0.0* と書きながら、
「即reward0.0ルール」では *signal_speed 超過で 0.0* と書いており、**矛盾していた**。
しかも `targets()` が `signal_speed = v_ceiling`（先読み値）を返しており、両者が同一になっていた。

先読みパターンは現示低下点の手前で下がるため、**v_ceiling < signal_speed** となる局面がある。
実測（木場→門前仲町・現示低下点707 m）:

| 位置 | 線路条件のATC現示 | `signal_speed` | `v_ceiling` |
|---|---|---|---|
| 650 m | 75.0 | 75.0 | 75.0 |
| 700 m | 75.0 | **75.0** | **71.6** |
| 707 m | 75.0 | **75.0** | **70.2** |
| 720 m | 70.0 | 70.0 | 70.0 |

このとき 72 km/h で走っていても**ATC違反ではない**（現示は75）。
旧記述のままだと即 reward=0.0 になり、「予見的に減速せよ」という指導が
「違反」にすり替わって学習が壊れる。

→ **修正**: `signal_speed`（＝ min(生の線路現示, 先行CBTC)）と
`v_ceiling`（＝ min(先読みパターン, 先行CBTC)）を実装・記述の両方で分離した。
即0.0は `signal_speed` 超過のみ。`v_ceiling` 超過は**段階減点**とプロンプトに明記。

**② 【中】衝突判定の基準が列車長を織り込んでいなかった**
「先行列車との接近距離が40m」は列車長20m時代の記述。10両編成では基準が曖昧になる。
→ 「**先行列車の最後尾**と自列車の**先頭**の距離が40m」と明記し、
「CBTC停止限界は同50mなので、そこにぴったり停止するのは設計上の正解であり0.0にしてはならない」も併記。

**③ 【中】ノコギリ運転の条件3が同方向のノッチ変更まで含んでいた**
5ノッチ化で「ノッチを変更している」と書いたため、`力行→勾配力行`（出力を一段緩める操作）まで
ノコギリ判定の対象になっていた。旧プロンプトの条件3は**反転**のみが対象である。
→ 「加速側と減速側をまたぐ反転」に限定し、
**同じ側の中での段階変更（力行⇔勾配力行、制動⇔勾配ブレーキ）は減点しない**と明記。

**④ 【中】`v_std` / `target_speed_spacing` が None のとき例外**
標準運転曲線が未生成の区間や後続列車がいない場合に `{f['v_std']:.1f}` が TypeError になる。
→ `_f()` フォーマッタを導入し、None は「該当なし」と表示する。

**⑤ 【小】維持帯の数値にフォーマット指定が無く浮動小数点がそのまま出ていた**
→ 同じく `_f()` で1桁に整形。併せて「この勾配で速度を保てるノッチ（`hold_notch`）」も表示するようにし、
`FEATURE_KEYS` にも追加した。

#### (3) 引き継いだが**意図的に変えた**点（要記録）

| 項目 | 旧 | 新 | 理由 |
|---|---|---|---|
| 評価優先順位 | 制限速度順守 と CBTC順守 を分離（8項目） | **ATC信号現示順守**に統合（7項目） | 東西線の速度制限データはCS-ATC現示そのもの（§6.2.1）。`signal_speed` が両者の min |
| ノコギリしきい値 | 7秒 | **5秒** | §7.4.3(5)。7秒だと東陽町→木場が定時未達 |
| ノコギリのペナルティ | `prev_notch_duration` のみ | **`notch_jump` で重み付け** | 5ノッチでは加速度のジャンプが0.51〜4.51と9倍の開きがある（§4.7.4） |
| 通常運転の基準速度 | `required_speed` | **`v_std`（標準運転曲線）** | §7.6 |
| 遅延回復の基準速度 | `speed_limit` | **`v_ceiling`** | §7.4.3(4) |
| 下り勾配の判定 | 「−6‰より急」 | **`coast_accel` の符号** | しきい値は速度依存（40km/hで−4.74‰、75km/hで−8.84‰） |
| `checks` のキー | `speed_limit` / `cbtc` | **`atc` / `gradient`** | ①の統合と、勾配確認ステップの新設 |

> **`checks` のキーが変わったため、`evaluate_csv_with_llm_multi.py` のパーサは
> 既存 `evaluate_csv_with_llm.py` のものを流用できない。** 作成時に対応させること。

### 14.3f LLM評価用CSVの作成と、プロンプトの実行可否（2026-08-23）

#### (1) プロンプトの実行可否 — **テキストは完成。ランナーが未作成**

| 要素 | 状態 |
|---|---|
| プロンプト本文（`prompt_multi.py`） | ✅ 完成。§14.3e の5件を修正済み |
| 走行ログCSV（LLMへの入力） | ✅ 作成（本節(3)） |
| CSV→プロンプトの結合 | ✅ 全3894行で `build_prompt()` 成功を確認 |
| **`evaluate_csv_with_llm_multi.py`（実行スクリプト）** | ❌ **未作成。これが無いとLLMへ送れない** |

**ランナーは既存 `evaluate_csv_with_llm.py` から流用できない部分がある。**
- `checks` のキーが `speed_limit`/`cbtc` → `atc`/`gradient` に変わった
- `mode` が3種 → 5種（`spacing` / `hold_at_station` 追加）
- 数値列の型変換対象が増えた（`FEATURE_KEYS` 47個）

#### (2) 既存の評価済みデータは**再利用しない**（重要な判断）

`train_reward_csv_direct/`（39,442行）は複数駅間版へ**流用してはならない**。

| 理由 | 内容 |
|---|---|
| ノッチ体系が違う | 3ノッチ（力行/惰行/制動）vs 5ノッチ。`current_notch`/`prev_notch` の値が別物 |
| プロンプト世代が違う | 通常運転の基準 `required_speed`→`v_std`／遅延回復 `speed_limit`→`v_ceiling`／ノコギリ 7秒→5秒 |
| 勾配が分布外 | 学習データ −6.7〜+12.5‰ に対し東西線は −16.3〜+29.7‰ |
| **過去に実害が出ている** | メモリ [[label-contradiction-hunting-rule]]: プロンプト世代の混在が原因で中間値0.6〜0.7が過剰出力された |

→ 既存データは `train_reward_csv_direct/` に**そのまま保全**し（§9.1.1 変更禁止リスト）、
複数駅間版は完全に別系統のフォルダで運用する。

```
評価用csv_Tozai/            LLM評価の入力（走行ログ）      ← 本節で作成
評価済ログ_Tozai/            LLM評価の出力（mode/reward/reason付き）
train_reward_csv_direct_Tozai/  NN学習に使うCSVの集約先
```

#### (3) 作成した走行ログCSV（通常運転モード・2ファイル）

`generate_eval_csv_multi.py` で生成。先行列車なし・後続なし・遅延なしの **通常運転モード**が対象。

| ファイル | 行数 | 内容 |
|---|---|---|
| `tozai_normal_good.csv` | **1988** | 正例。標準運転曲線の再現／`v_std`追従／惰行ポイント±4km/hの変動（20走行） |
| `tozai_normal_bad.csv` | **1906** | 負例。早すぎる惰行・過剰力行・無駄な制動・ノコギリ(2s/3s)・制動開始の遅早・ちんたら運転・下り勾配での力行（19走行） |

**カバレッジ**

| 項目 | good | bad |
|---|---|---|
| 現示低下点への接近フェーズ | 222行 | 310行 |
| 現示超過（即reward0.0の対象） | 0行 | **22行** |
| 現示直下0〜2km/hの帯 | 16行 | 47行 |
| `v_ceiling < signal_speed`（先読みが効いている局面） | 15行 | 24行 |

**現示超過22行と現示直下47行は、旧データの既知の欠落を埋めるもの**である。
設計メモ §18 は「余裕0-2km/h帯＝5行、制限超過＝**0行**」がDQN失敗の一因だったと記録している。
また「現示低下点への接近フェーズ」は羽前成田線では構造的に発生せず学習データ0行だった
（メモリ [[ego-segment-route-characteristics]]）。本データで初めて532行が入る。

**残る不足 — 中間ノッチ（P2/B2）のデータが薄い**

| ノッチ | good | bad |
|---|---|---|
| 力行 | 403 | 502 |
| 惰行 | 888 | 685 |
| 制動 | 680 | 704 |
| **勾配力行(P2)** | **17** | 0 |
| **勾配ブレーキ(B2)** | 0 | **15** |

通常運転モードでは標準運転曲線がほとんど中間ノッチを使わない（§14.2 で P2 は0.8秒のみ）ため、
構造的に薄くなる。**中間ノッチは遅延回復モードの維持帯で多用される**ので、
そちらのデータ生成時に厚くする必要がある。通常運転モードだけで学習させると
P2/B2 の評価が未学習のまま残る点に注意。

#### (4) 作成中に見つかったモード判定の不具合（修正済み）

`required_speed` は「現在速度のままで間に合うなら現在速度を返す」仕様である。
そのため**過速している列車では現在速度がそのまま返り、遅延回復モードを誤発動させる**。

実測（東陽町→木場・34秒経過・残り46秒・現示75 km/h）:

| 現在速度 | `required_speed` | `schedule_speed` | 旧条件での判定 | 新条件での判定 |
|---|---|---|---|---|
| 62.0 km/h | 62.0 | 47.3 | normal | normal |
| **77.1 km/h**（過速） | **75.0** | **47.3** | **delay_recovery ✗ 誤発動** | normal ○ |

→ **`schedule_speed`（現在速度に依存しない「定刻に必要な巡航速度」）を新設**し、
プロンプトのモード判定条件を `required_speed` から `schedule_speed` へ変更した。
`required_speed` は「惰行への切替目安」として残す（両方をプロンプトに渡し、用途を明記）。

### 14.3g LLM評価ランナーの作成（2026-08-23）

`evaluate_csv_with_llm_multi.py` を作成した（既存 `evaluate_csv_with_llm.py` は変更なし）。
プロンプト本文は `prompt_multi.py` に分離済みなので、本スクリプトは
「CSVを読む → プロンプトを組む → LLMへ送る → 応答を検証して書き戻す」だけを担う。

#### (1) 既存ランナーから変えた点

| 項目 | 内容 |
|---|---|
| `checks` のキー | `speed_limit`/`cbtc` → **`atc`/`gradient`**（計8キー） |
| `mode` | 3種 → **5種**（`spacing` / `hold_at_station` 追加） |
| 数値列の型変換 | **34列**（`FEATURE_KEYS` 47個のうち数値のもの） |
| 入出力 | `評価用csv_Tozai/` → `評価済ログ_Tozai/llm_evaluated_dataset_multi.csv` |

#### (2) 追加した機能（3894行を扱うため）

- **`--dry-run`** … APIを呼ばずにプロンプトだけ確認する。全行でプロンプトが組めるかも検証する
- **`--workers N`** … 並列実行（API待ちはI/Oバウンドなので効く）。既存ランナーは逐次
- **`--resume`** … 中断した続きから。既存出力の (source_file, run_id, time) を突き合わせる
- **`--limit N`** … 先頭N行だけ処理（試験用）
- **応答の検証**（`_validate`）… 下記のいずれかに該当したら**リトライ扱い**にする
  - `mode` が5種のいずれでもない
  - `reward` が数値でない／0.0〜1.0の外
  - `checks` に8キーが揃っていない
  - **`immediate_zero_rule` が NG なのに `reward != 0.0`**（プロンプトの明示ルールとの整合）
- **`checks` を出力CSVに保存**する（既存は破棄していた）。ラベルの診断に使える

検証ロジックは5ケースのテストで期待どおり動作することを確認済み。

#### (3) 作成中に見つかった不具合（修正済み）

ドライランで、**発車時（速度0）に `required_speed` と `target_speed_no_stop` が 0.0 になる**ことが判明した。
`_hold_time()` が `V <= 0` のとき0を返しており、「0 km/hで保持したまま到着できる」という
計算になっていたため。→ `V <= 0.5` のときは無限大を返すよう修正し、
`_time_to_station()` にも同じガードを入れた。修正後は速度0でも `required_speed = 45.0 km/h`
（＝その地点の現示上限）が正しく出る。

各走行の1行目と駅停車行が該当するため、放置すると
「発車時は目標速度0＝力行不要」という誤ったラベルが混入していた。

#### (4) 現在の状態

| 要素 | 状態 |
|---|---|
| プロンプト本文 | ✅ |
| 走行ログCSV（3894行） | ✅ |
| 全行でのプロンプト生成 | ✅ 3894/3894 成功 |
| ランナー（並列・再開・検証つき） | ✅ |
| `.env`（`LLM_API_URL` / `LLM_API_KEY`） | ✅ 設定済み |
| **LLM評価の実行** | ⏸ 未実施（外部APIを呼ぶため実行前に確認が必要） |

実行コマンド:
```bash
python evaluate_csv_with_llm_multi.py --dry-run          # APIを呼ばず確認
python evaluate_csv_with_llm_multi.py --limit 20         # 20行だけ試す
python evaluate_csv_with_llm_multi.py --workers 6        # 本番
python evaluate_csv_with_llm_multi.py --resume           # 中断からの再開
```

### 14.4 実装中に判明した3つの落とし穴（**環境実装でも同じ対処が必要**）

標準運転曲線ソルバの実装で、いずれも「素直に書くと壊れる」箇所が3つあった。
**`environment_multi.py` でも同じ対処が要る**ため記録しておく。

**(1) 惰行フェーズの天井張り付きにヒステリシスが要る**
「天井に触れたら制動、離れたら惰行」を素で書くと、弱い制動でも1ステップで天井を割るため
**0.1秒周期でノッチが振動**した（実測: ノッチ切替87回）。一度制動に入ったら一定幅（1.5 km/h）下がるまで
保持するラッチが必須。→ プロンプト §7.4.3(7) の「天井に張り付く」も、この幅を持った運転を前提に評価する。

**(2) 「加速度ちょうど0」のノッチを候補から除外してはならない**
定速保持のノッチ対を「加速度が正／負」で厳密に選ぶと、−35‰でのB2（加速度ちょうど0）が
**両方の候補から外れて**その両隣（惰行と制動）を高速で往復した（実測: ノッチ切替124回）。
等号を含めて「絶対値が最小のノッチ」を優先する必要がある。

**(3) 下り勾配の惰行で、天井に達する前から制動してはならない**
「惰行で加速するなら制動」とだけ書くと、下り勾配で**重力の助けを捨てて延々と制動し続ける**
（実測: 東陽町→木場が122.8秒＝標準80秒に対し大幅超過、力行仕事も1.9倍）。
制動するのは**天井に達したときだけ**である。

いずれも「勾配が急で惰行が加速する路線」で初めて顕在化する問題であり、
羽前成田線（最急下り −6.7‰）では起きなかった。

### 14.5 補足 — V_hold グリッドの上限

`speed_cap`（V_hold探索の上限）を「区間内の**最小**現示」にすると、
東陽町の出発直後13.3mだけの45 km/h現示に引きずられて解が見つからなくなる。
区間内の**最大**現示を上限とし、局所的な現示は走行シミュレーション内で
`min(v_hold, ceiling(x))` として別途クランプする。

### 14.6 次の作業

- **0-5**: 東西線の1区間で既存の単一区間 `apex2.py` 相当の学習が回るか確認する。
  併せて報酬NNの `current_gradient` が学習データ範囲外（§12 R5）である程度を定量化する。
- **フェーズ1**: `environment_multi.py` の作成（複数駅間エピソード・停車フェーズ・観測拡張）。
  §14.4 の3つの落とし穴に同じ対処を入れること。

---

## 付録A: `environment_multi.py` の主要な差分（実装時の参照用）

```
reset(line="tozai", station_indices=[0,1,2], ego_delay, forward_csv, headway,
      backward_csv=None, back_headway=None, ...):
    self.stations = line_config から取得した駅リスト（東陽町・木場・門前仲町）
    self.section_idx = 0
    self.departure_station = stations[0];  self.arrival_station = stations[1]
    self.schedule = 累積標準ダイヤ [T_arr(B)=80, T_dep(B)=110, T_arr(C)=195]
    self.is_dwelling = False;  self.dwell_elapsed = 0.0
    self.t = ego_delay
    self._grade_profile / self._limit_profile をキャッシュ（§7.2）
    self._v_std = 標準運転曲線の参照テーブルをロード（§7.6）

step(action):
    先行列車の位置更新（記録再生・現行どおり）
    後続列車の位置更新（記録再生 → 自列車の後方250mで頭打ち）     # F2.5（列車長200m+停止限界50m）

    if self.is_dwelling:
        # --- 停車フェーズ ---
        time_step = DWELL_TIME_STEP (1.0秒)
        if action == acceleration and dwell_elapsed >= DWELL_MIN:
            発車 → self.is_dwelling = False
                   self.section_idx += 1
                   arrival_station / Train.TARGET_STATION / プロファイルキャッシュ / v_std を更新
                   section_start_t / 停滞チェックポイントをリセット
        else:
            dwell_elapsed += time_step
        reward = 停車中の評価（F2=ルール, F3=NN）
        # 停滞検出は無効
    else:
        # --- 走行フェーズ（現行 environment2 とほぼ同一）---
        ...
        if 停止窓内に停止:
            reward = (llm_reward − 0.5)          # 到着イベント（time_stepスケールなし）
            if self.section_idx == 最終区間:  done = True          # 正常終了
            else:  self.is_dwelling = True;  self.dwell_elapsed = 0.0   # 中間駅

time_step プロパティ:
    if self.is_dwelling: return DWELL_TIME_STEP
    return 0.1 if 駅手前100m以内 else 1.0

forbidden_action プロパティ:
    if self.is_dwelling:
        coasting      = True                              # 常に禁止（行動縮退の回避）
        acceleration  = (dwell_elapsed <  DWELL_MIN)      # 最低停車時間内は発車不可
        deceleration  = (dwell_elapsed >= DWELL_MAX)      # 上限で強制発車
        return [coasting, acceleration, deceleration]
    # --- 走行中 ---
    if self.speed > self.current_speed_limit:
        acceleration = True
        if 惰行加速度 > 0:  coasting = True                # §7.7 下り勾配での惰行加速
    if 先行を追い越した:  acceleration = True; coasting = True
    ...
```

## 付録B: 東西線 対象区間のデータ要約

```
駅（下り B線・キロ程は進行方向に減少）
  東陽町   16.0118 km   rt=80s    → 内部座標 0.0000 km
  木場     15.0473 km   rt=85s    → 内部座標 0.9645 km
  門前仲町 13.9893 km             → 内部座標 2.0225 km
  （茅場町 12.1875 km  ※今回は対象外）

CS-ATC信号現示（内部座標・**先頭が越えた時点で切り替わる**）
  0.0000〜0.0133  45 km/h   ← 東陽町の出発直後 13.3m のみ（先頭判定では事実上効かない）
  0.0133〜0.1218  70 km/h
  0.1218〜1.6718  75 km/h   ← 木場を挟む長い区間
  1.6718〜2.3618  70 km/h   ← 門前仲町を含む（唯一の低下点。門前仲町の350.7m手前・先行制動36.0m）

勾配（進行方向基準・符号そのまま）
  東陽町→木場     −5 → −14.2 → −16.3 → +2 ‰        平均 −10.32‰  標高差 −10.0m
  木場→門前仲町   +2 → +3.5 → +4 → +27 → +28 → +29.7 → +3 ‰
                                                     平均 +11.26‰  標高差 +11.9m
    ※ +27〜+29.7‰ のランプは 348.9 m（内部座標 1.3620〜1.7109 km）

標準ダイヤ    運転間隔 140秒（ラッシュ時2分20秒）／ 駅停車時間 30秒
曲線データ    なし（曲線抵抗0として扱う）

最速走行（制動2.5・ATC現示厳守・全力行・駅にちょうど停止）
                        現行(山形28t)     15000系10両      標準    余裕
  東陽町→木場            79.1 s             71.3 s          80 s   +8.7 s (10.8%)
  木場→門前仲町          97.0 s ✗           77.3 s          85 s   +7.7 s ( 9.0%)

標準運転時間ちょうどの省エネ運転（実測）
  東陽町→木場   79.9 s  最高 62.4 km/h  惰行開始 100 m 地点（下り勾配のため区間の90%を惰行＋制動）
  木場→門前仲町 84.9 s  最高 74.7 km/h  惰行開始 373 m 地点（登り勾配のため現示一杯まで加速が必要）

曲線抵抗の感度（区間一律に加えた場合）  R=800m→力行仕事+3% / R=400m→+6〜7%（標準時間は依然達成可）
  ※比較対象の羽前成田→白兎も実質曲線なし（ego区間の距離加重平均 0.05 kg/t）
```

## 付録B2: 車両モデル（東京メトロ15000系・10両編成）

```
編成          10両 × 20,000 mm = 列車長 200 m（5M5T）
性能(定員時)   起動加速度 3.3 km/h/s ／ 常用減速度 3.5 km/h/s ／ 非常 5.0 ／ 設計最高 110 km/h

走行抵抗       R(v) = 2.089 + 0.0394 v + 0.000675 v²   [kg/t]      ※指定値
引張力         F(v) = F0              (v ≤ 40)          定トルク域
               F(v) = F0 · 40/v       (40 < v ≤ 75)     定出力域
               F(v) = F0 · 40·75/v²   (v > 75)          特性域
               F0 = 3.3 × 28.34467 + 2.089 = 95.626 [kg/t]  （起動加速度3.3を満たす値）
               ※V1=40 / V2=75 はPDFに記載が無く一般値。感度確認の対象
制動           DECELERATE = −2.5 km/h/s（走行抵抗・勾配抵抗は別途加算）
               ※PDFの3.5は常用最大・5.0は非常。駅停車は2.5。CBTCパターンも2.5で構築する
慣性係数       FACTOR_OF_INERTIA = 28.34467 を維持（≈1.0。現行との連続性を優先）

均衡速度       +29.7‰ 力行 84.7 km/h ／ +27.0‰ 力行 87.3 km/h
               −16.3‰ 惰行 118.8 km/h（＝制限75km/hでは惰行しても加速し続ける）
惰行加速度     −16.3‰: +0.44 / +0.37 / +0.29   （30 / 50 / 70 km/h）
               +29.7‰: −1.19 / −1.25 / −1.34
制動距離(0‰)   40→84.5m ／ 60→187.2m ／ 70→252.5m ／ 75→288.4m
               −16.3‰ で +27%（70 km/h → 321.0 m）／ +29.7‰ で −28%（→ 181.8 m）
               同じ70km/hでも勾配により 181.8〜321.0 m と 1.77倍の開きがある

CBTC          停止限界 = 先行の先頭位置 − 200 m(列車長) − 50 m = 先行先頭の 250 m 手前
```

## 付録C: 参照すべき既存ドキュメント

- `docs_先行列車対応_設計メモ.md`
  - §5 駅間停車防止モードの加速上限評価 — §7.3 の `target_speed_no_stop`
  - §13 未来情報リークの解消（因果性の原則）— §5.3 の根拠
  - §15 target_speed_no_stop の先読み化 — §5.3 の対処案A
  - §18 制限速度超過時に惰行を許可した経緯 — **§7.7 で前提が崩れる**
  - §26〜§30 TASC導入時の学習崩壊 — §4.3（行動縮退）・§4.5（Q値発散）の根拠
  - §32 先行列車の惰行ポイント方式 — §8.1 の前提
- `CLAUDE.md`
  - 「設計上の重要事項（time_step と報酬・割引のスケーリング）」— §4.4・§4.6 の前提
  - `generate_standard_curve.py` の項 — §7.6 の `v_std(x)` の生成元
- メモリ
  - [[ego-segment-route-characteristics]] — 羽前成田線で制限接近フェーズが構造的に発生しなかった件（§7.1・§7.4）
  - [[retrain-both-nns-on-feature-change]] — §4.2・フェーズ3
  - [[balanced-mse-negative-result]] — フェーズ3のラベル不均衡対処
