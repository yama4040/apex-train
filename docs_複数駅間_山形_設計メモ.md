# 複数駅間DQN（山形鉄道・羽前成田 → 白兎 → 蚕桑）設計メモ

作成日: 2026-08-25

既存の単一区間手法（`apex2.py` 系）と、東京メトロ東西線の複数駅間版（`*_multi.py` 系）に続く
**第3の系統**として、既存路線（山形鉄道フラワー長井線）で複数駅間の最適化を行うための一式。

---

## 0. 絶対条件 — 既存資産の保全

**既存の `apex2.py` 関連スクリプトは一切変更していない。** 新規ファイルのみで構成する。

変更していないファイル（読み取り専用で import するのみ）:

| ファイル | 本系統での扱い |
|---|---|
| `train.py` | 物理モデル（3ノッチ・山形28t）をそのまま使う |
| `track.py` | 路線データ（勾配・曲線・制限速度）をそのまま使う |
| `actions.py` | 行動定義（力行/惰行/制動）をそのまま使う |
| `model.py` | `QNetwork` をそのまま使う（入力次元だけ引数で変える） |
| `segment_tree.py` | 優先度付き経験再生の `SumTree` をそのまま使う |
| `histogram_loss.py` | HL-Gaussian のビン定義を共有する |
| `required_speed.py` | 物理定数・引張力・走行抵抗を取り込む（関数は新規側で差し替え） |
| `generate_standard_curve.py` | `StandardCurveSolver` / `TrackLookup` を再利用する |
| `environment2.py` / `evaluate_csv_with_llm.py` / 学習済みモデル7ファイル | **一切参照も変更もしない** |
| `input/Station.csv` ほか既存の入力CSV | **読むだけ。書き換えない** |

データ置き場も分離してあり、既存の `評価用csv/` `train_reward_csv_direct/` とは
**絶対に混ぜない**（プロンプト世代・列構成・モード定義が異なる。過去にラベル矛盾の実害が出ている）。

---

## 1. 対象区間と設計値

```
[羽前成田 21.112km] --180秒-- [白兎 23.29km] --停車30〜300秒-- [蚕桑 24.667km]
      t=0                        t=180 着 / 210 発            t=340 着（標準ダイヤ）
```

| 項目 | 値 | 根拠 |
|---|---|---|
| 対象駅 | `input/Station.csv` の index 11 / 12 / 13 | 羽前成田・白兎・蚕桑 |
| 羽前成田→白兎 | 2178 m / **標準運転時間 180秒** | Station.csv の `rt` と同じ。既存の標準運転曲線（V=65）と一致 |
| 白兎→蚕桑 | 1377 m / **標準運転時間 130秒** | **本系統の設定値。** Station.csv の `rt`=180秒は最短118.8秒に対し余裕61秒で緩すぎ、駅停車の遅延が下流に効かない。`input/Station.csv` は書き換えず `config_ymulti.RUNNING_TIMES` で持つ |
| 標準停車時間 | 30秒 | CLAUDE.md「各列車の駅停車時間は30秒とする」 |
| 最低停車時間 `DWELL_MIN` | 30秒 | 乗降のためこれ未満では発車できない |
| 最大停車時間 `DWELL_MAX` | **300秒** | §4 参照 |
| 標準出発間隔 | 120秒 | 既存 `apex2.py` と同値 |
| 列車長 | 20 m | CLAUDE.md |
| CBTC停止限界 | 先行の**最後尾**から50m ＝ 先行の**先頭から70m** | §3 参照 |
| 行動空間 | **3ノッチ**（力行/惰行/制動） | 既存の単一区間の結果とそのまま比較できる |

### 路線の特性（実測）

| 区間 | 勾配 | 備考 |
|---|---|---|
| 21.112〜21.65 | −2.1 〜 −6.7‰ | 発車直後は下り |
| 22.3〜22.5 | +12.5‰ | |
| 22.7〜23.3 | +6.1 → +9.2‰ | 白兎手前の上り |
| **23.3〜24.3** | **+11.4‰ が約1km** | **白兎→蚕桑の主要区間。惰行では失速する** |
| 24.3〜 | −2.3‰ | 蚕桑手前は緩い下り |

- 制限速度は全区間 **70km/h 一定**（`speed_limit.csv` の 19.66km→70 が 30.13km まで続く）。
  したがって「制限速度区間に接近中」フェーズは構造的に発生しない。
- 曲線は 23.571〜23.77（R=1000）と 24.372〜24.462（R=400）にある。
  **蚕桑手前の R=400 は制動距離に効く**（§3）。

### 標準運転曲線（`standard_curve_ymulti.py` の出力）

| 区間 | 定速保持速度 | 運転パターン | 力行仕事 | 停止位置誤差 |
|---|---|---|---|---|
| 羽前成田→白兎（180秒） | 65.25 km/h | 力行(0-51s) → 惰行(51-172s) → 制動(172-180s) | 1.368 kWh | +0.022 m |
| 白兎→蚕桑（130秒） | 54.50 km/h | 力行(0-65s) → 惰行(66-114s) → 制動(115-130s) | 1.761 kWh | +0.018 m |

通算 340.0 秒（標準停車30秒込み）で累積標準ダイヤと一致する。

---

## 2. MDP設計

### エピソード

```
[羽前成田 停止] --走行--> [白兎 停止] --停車（発車判断）--> [白兎 発車] --走行--> [蚕桑 停止] = done
```

- 中間駅（白兎）に停止窓（−10m〜+5m・0.5km/h以下）で入ったら **`done` にせず停車フェーズへ遷移**する。
- 停車フェーズへ入った時点で `arrival_station` を蚕桑へ進める。
  停車中の判断対象は「次の駅間で機外停車するか」なので、状態量が次区間を指している必要がある。

### 行動（停車中の読み替え）

| 状況 | 力行 | 惰行 | 制動 |
|---|---|---|---|
| 走行中 | 力行 | 惰行 | 制動 |
| 停車中 | **発車** | **禁止** | **待機（停止保持）** |

停車中に惰行を許すと待機と完全に同一の結果になり、Q学習の max 演算子が過大評価バイアスを
累積させる（`docs_先行列車対応_設計メモ.md` §26 と同型の問題）。**必ず2択に絞る。**

`forbidden_action`（`[惰行, 力行, 制動]`）:

```
停車経過 + 1秒 <  DWELL_MIN   → 惰行・力行を禁止（待機のみ）
DWELL_MIN 〜 DWELL_MAX        → 惰行を禁止（発車 / 待機の2択）
停車経過 + 1秒 >= DWELL_MAX   → 惰行・制動を禁止（強制発車）
```

**「+1秒」で判定する理由**: ステップ後の停車経過で見ないと、最短でも `DWELL_MIN + 1` 秒の
停車になり、標準停車30秒ちょうどが作れない（実測で31秒になっていた）。

### 制御周期

| 状況 | `time_step` |
|---|---|
| 通常走行 | 1.0 秒 |
| 次駅の100m以内 | 0.1 秒（停止精度に必要） |
| **駅停車中** | **1.0 秒固定**（`DWELL_TIME_STEP`） |

停車中は `station_remaining_distance ≈ 0` なので、そのままだと0.1秒刻みに落ちて
停車300秒が3000ステップに膨れる。明示的に1.0秒へ戻している。

### 報酬

```
走行中       reward = (評価値 − 0.5) × (time_step / 1.0)
駅到着イベント reward = (評価値 − 0.5)          ← time_stepスケールを外して満額
駅停車中     reward = (評価値 − 0.5) × (1.0 / 1.0)
```

- 評価値は 0.0〜1.0。既定は LLM を蒸留した報酬NN（`direct_reward_predictor_ymulti`）の出力のみ。
- 到着イベントは中間駅（白兎）でも満額で与える。停止精度は「時間あたりのレート」ではなく
  1回のイベントの評価なので、駅前0.1秒のスケール（×0.1）を掛けると±1m精度への勾配が
  ノイズに埋もれる（既存 `environment2.py` の終端報酬と同じ扱い）。
- 報酬NNが未学習の間は `rule_reward_ymulti.py`（暫定ルール報酬）で動かせる。
  **これは CLAUDE.md の「報酬はNN出力のみ」制約に対する一時的な例外**であり、
  LLM評価が済んだら必ずNNへ置き換える。

### 観測（40次元）

| # | 内容 |
|---|---|
| 1–8 | 速度 / 駅までの距離（広域・ズーム）/ 残り時間 / 同一操作の継続時間 / 直前行動 one-hot 3 |
| 9–14 | 制限速度 / CBTC現示 / required_speed / target_speed_no_stop / **v_std** / **v − v_std** |
| 15–16 | 必要制動距離 / 停止余裕マージン |
| 17–22 | フェーズ one-hot（**駅停車中**・加速・巡航・制限接近・減速・停車完了） |
| 23–28 | 現在勾配 / 前方の勾配変化（距離・値）/ 前方の制限変化（距離・値）/ **惰行到達可能性** |
| 29–33 | 先行までの距離 / 先行の速度 / 先行クリア残時間 / **先行の停車経過** / **先行が次駅発車済みか** |
| 34–38 | **停車中フラグ** / **停車経過** / **先行クリア残 − 停止限界到達時間** / 残り駅数 / 終着までの残り時間 |
| 39 | 直前ノッチの継続時間 |
| 40 | モード `hold_at_station` フラグ |

太字が複数駅間版で追加した特徴量。

### 終了条件

| # | 条件 | 中間駅（白兎） | 終着駅（蚕桑） |
|---|---|---|---|
| ① | タイムオーバー（累積標準ダイヤ340秒 + 360秒） | — | `done`（失敗） |
| ② | 停止窓内に停止 | **停車フェーズへ遷移**（`done`にしない）＋到着イベント報酬 | `done`（成功） |
| ③ | 先行への異常接近（先行の先頭から40m未満） | `done`（失敗） | `done`（失敗） |
| ④ | オーバーラン（+5m超で走行中） | `done`（失敗） | `done`（失敗） |
| ⑤ | 駅の手前で停止（信号待ちを除く） | `done`（失敗） | `done`（失敗） |
| ⑥ | 停滞（ちんたら運転） | `done`（失敗） | `done`（失敗） |
| ⑦ | 停車時間 `DWELL_MAX` 到達 | 強制発車（`done`にしない） | — |

**既存 `environment2.py` から直した点（いずれも複数駅間で顕在化した実バグ）**

1. **失敗判定を到着判定より先に行う。**
   先行が次駅に停車したままそこへ突っ込むと位置は停止窓に入るため、
   到着を先に判定すると「追突したのに到着成功」になる（実測で発生）。
2. **異常接近を距離だけで判定する。**
   既存は「停止したときだけ」判定していたため、駅に停車中の先行へ走行したまま
   突っ込むケースを検出できなかった。
3. **信号待ちの判定を列車長込みにする（`held_by_signal`）。**
   停止限界が先行の先頭から70mになったため、既存の「先行の100m手前」という境界では
   正しく停止した列車が先行の前進とともに枠外へ出て「駅間停車」と誤判定された
   （実測: 先行が30m進んだ瞬間に失敗扱い）。判定は120m以内、または現示がほぼ0のとき。
4. **信号開通後の再起動猶予（`SIGNAL_CLEAR_GRACE_S` = 5秒）。**
   現示が開いたその瞬間に「駅の手前で停止している」と判定すると、
   正しい信号待ちが必ず失敗に終わる。
5. **停滞検出は信号待ち中にチェックポイントを打ち直す。**
   信号待ちの時間が「前進していない時間」として蓄積し、現示が開いた直後に
   停滞と誤判定されるのを防ぐ。
6. **発車直後の猶予（`DEPART_GRACE_S` = 3秒）。**
   駅を発車した瞬間は定義上 0km/h であり、既存では発車1歩目に惰行・制動を選ぶと
   即座に失敗終了していた。複数駅間では中間駅の発車判断の直後にも同じ崖が現れる。

### 割引率

`gamma ** (env.time_step / base_time_step)` で実時間あたりの割引を揃える（既存と同じ）。
既定は1秒あたり γ=0.9975。

### Double DQN

**既定で有効**（`--no-double` で従来動作）。複数駅間ではエピソードが2倍以上長く、
駅手前100mの0.1秒刻みが駅の数だけ増えるためブートストラップ連鎖が伸び、
vanilla max の過大評価バイアスが蓄積しやすい（CLAUDE.md「残る副作用」）。

---

## 3. CBTC停止限界と制動距離

### 列車長を織り込む

```
停止限界（自列車の先頭）= 先行の先頭 − 列車長20m − 停止限界50m = 先行の先頭から 70m 手前
```

既存 `environment2.py` は列車長の概念が無く50mしか引いていなかった（20m甘い）。
本系統では `config_ymulti.CBTC_HEAD_MARGIN_KM = 0.070` として効かせる。
異常接近の判定は40mとし、停止限界（70m）にぴったり止まる**正解挙動**を失敗にしない。

| 先行までの距離 | CBTC指示速度 |
|---|---|
| 70 m | 0.0 km/h |
| 100 m | 23.4 km/h |
| 200 m | 49.0 km/h |
| 300 m | 65.5 km/h |
| 500 m | 70.0 km/h |

### 制動距離は逆積分した制動曲線を使う（`brake_curve_ymulti.py`）

`train.Train.req_stop_dist` と `required_speed.brake_stop_distance_m` は
**制動開始地点の勾配・曲線が停止まで一定**と仮定している。蚕桑の制動開始点の直前には
R=400m の曲線があり、この仮定では制動距離を **5.4m 過小評価**する。
実際に先行列車パターンの生成で **5.75m の過走**が起きた。

駅から逆向きに0.01秒刻みで積分すればプロファイルがそのまま入るので、数cm精度になる。
停止位置が固定なのでキャッシュでき、参照コストは O(1)。

| 速度 | 蚕桑への制動距離（逆積分） | 一定勾配近似 | 差 |
|---|---|---|---|
| 50 km/h | 135.75 m | 135.81 m | −0.07 m |
| 65 km/h | 226.52 m | 221.13 m | **+5.38 m** |
| 70 km/h | 261.17 m | 255.51 m | **+5.66 m** |

**先行列車が移動する場合（CBTC現示）は逆積分できない**ため、現在地点の勾配・曲線が一定という
近似を使う（既存 `environment2.cbtc_signal_speed` と同じ扱い）。停止限界に70mの余裕があるので実害はない。

---

## 4. `DWELL_MAX` = 300秒 の根拠

機外停車を避けるために必要な白兎の停車時間は

```
条件: 先行の蚕桑発車時刻 < 白兎発車時刻 + （白兎発 → 蚕桑の停止限界までの所要 ≒ 118秒）
```

| 先行のパターン | 出発間隔 | 必要な白兎停車時間 |
|---|---|---|
| 惰行65・白兎30秒・蚕桑180秒 | 120秒 | 94秒 |
| 同上 | 90秒 | 124秒 |
| 同上 | 60秒 | 154秒 |
| 惰行50・白兎60秒・蚕桑180秒 | 60秒 | **257秒** |

最悪ケースが257秒なので **300秒**とした（240秒では機外停車を避けられない組合せが残る）。
タイムオーバーの余裕は 360秒（累積標準ダイヤ340秒 + 360秒 = 700秒）とし、
`DWELL_MAX` まで待ってから走り切っても打ち切られないようにしてある。

**Q値発散の防波堤**（`docs_複数駅間最適化_計画.md` §4.5 と同じ3点セット）
1. `DWELL_MAX` による強制発車（構造的に無限延長を不可能にする — 最も重要）
2. タイムオーバーを累積ダイヤ基準で必ず効かせる
3. ゼロ中心化された報酬 + 割引（待機に自然な機会費用がある）

---

## 5. 駅停車中の評価基準（ユーザー指定）

```
mode = "hold_at_station"（駅停車中は必ずこのモード。走行中のノッチ評価は一切適用しない）

前提: 最低停車時間（30秒）までは行動の余地がないため一律 0.5（中立）

通常時（先行なし／先行が通常どおり）
  発車       → 0.9   （標準停車30秒での定時発車が最良）
  待機を継続 → 0.35 から、標準停車からの超過に応じてなめらかに減点し、
                超過30秒（＝停車1分）で 0.0（最低評価）

先行が次の駅で長く停車する要素がある場合
  （= 先行の観測遅延 > 0、または「先行クリア残時間 > 今発車して停止限界に着くまでの時間」）
  待機を継続 → 0.85  ← 標準停車超過による減点は適用しない
  発車       → 0.1   （機外停車を招く）
  発車は先行が運転を再開した後に行う

判断できない場合（先行が次駅に到着して間もない）
  待機を継続 → 0.35〜0.60（待てば情報が増えるので短時間の待機は許容。長引くほど下げる）
  発車       → 0.5
```

### 因果性について（未来情報リークの回避）

「先行が蚕桑で長く停まっている」ことを自列車が白兎で**実際に観測できるか**を確認した。

自列車の標準発車時刻（t=210）に先行の延着を観測できる条件は
`白兎→蚕桑の運転時間 + 標準停車 ≦ 出発間隔` すなわち `130 + 30 = 160 ≦ H`。
標準出発間隔は120秒なので、**定時運行の先行では標準発車時刻に延着を観測できない。**

これは未来情報リークを避ける以上、本質的に避けられない不確実性である。
そこで以下の3つで扱う。

1. **「待てば情報が増える」構造をそのまま学習させる**（推奨・計画書 §5.3 案B）。
   観測 #32（先行の停車経過時間）と #33（先行が次駅を発車済みか）を観測に入れてあるので、
   「少し待って先行の様子を見てから決める」という情報獲得のための待機を DQN が獲得しうる。
2. **先読み機構**（`ASSUMED_DELAYED_DWELL_S`）。先行が前駅を30秒以上遅延して出発していれば
   次駅の停車も延びると想定する。先行の出発遅延は自列車が観測可能なので因果的である。
3. **自列車の出発遅延を 0〜60秒でランダムにする**（既存 apex2 は0〜20秒）。
   自列車が遅れているほど先行の延着を観測しやすくなり、発車判断が学習可能なシナリオが増える。

### 暫定ルール報酬での確認結果

| シナリオ | 標準30秒で発車 | 塞ぎに合わせて待機 |
|---|---|---|
| 先行なし | **+104.0**（到着 337秒） | — |
| 出発間隔120・蚕桑30秒（通常） | **+104.0**（337秒） | +81.8（100秒停車・407秒） |
| 出発間隔120・蚕桑180秒（急病人） | +86.3（機外停車607歩・441秒） | **+108.8**（100秒停車・414秒） |
| 出発間隔60・蚕桑180秒 | +9.2（機外停車1953歩・605秒） | **+114.0**（260秒停車・623秒） |

**塞がれているときは待機が、塞がれていないときは定時発車が、それぞれ最大の報酬になる**という
意図どおりの構造になっていることを確認した。

---

## 6. 目標速度の算出（`required_speed_ymulti.py`）

既存 `required_speed.py` は「現在地点の勾配が駅まで一定」と仮定するが、
白兎→蚕桑は +11.4‰ の上りが1km続いたあと −2.3‰ に反転するため、
**惰行の減速度が −0.581 → −0.097 km/h/s と 6倍変わる**。この仮定では惰行の減速量を大きく誤る。
本モジュールは**勾配プロファイルを位置ごとに参照して積分する**。

### 惰行到達可能性（この路線で本質的）

上り勾配が長いため、惰行に入る速度が低すぎると駅の制動開始点まで届かず駅間停車する。

| 区間 | 位置 | 惰行のみで駅に届く最低速度 |
|---|---|---|
| 羽前成田→白兎 | 残り1742m | 60.0 km/h 以上 |
| | 残り1089m | 53.1 km/h 以上 |
| | 残り436m | 34.5 km/h 以上 |
| 白兎→蚕桑 | 残り1102m | 54.7 km/h 以上 |
| | 残り689m | 35.5 km/h 以上 |
| | 残り275m | 6.4 km/h 以上 |

これを `coast_reachable` / `coast_arrival_speed` として観測・プロンプト・報酬に渡す。
**3ノッチしかないため、+11.4‰ での定速保持は力行と惰行の繰り返しになる。**
これは車両の制約による必然なので、プロンプトでノコギリ運転の対象外と明記してある。

### 計算コスト

RL の1ステップで `required_speed` と `target_speed_no_stop` を各1回呼ぶ。
シミュレーション刻み 0.5秒・二分探索14回で **1.0〜2.2 ms/回**、`env.step()` 全体で **2.27 ms/step**
（1エピソード約900歩なら約2秒）。既存 `required_speed.py`（3.59 ms/回）より速い。

---

## 7. 先行列車パターン（`generate_forward_train_ymulti.py`）

出力: `input/f_train_ymulti/coast{V}_b{D_B}_c{D_C}.csv`（**312種**）

- 惰行ポイント V ∈ 40〜65 km/h（1km/h刻み・26種）
- 白兎の停車時間 D_B ∈ {30, 45, 60} 秒
- 蚕桑の停車時間 D_C ∈ {30, 60, 120, 180} 秒
  （**120 / 180 が「急病人救護など」の長時間停車シナリオ**＝自列車は白兎に留まるべき局面）

運転は自列車と同じ省エネ運転（惰行ポイント方式）。上り勾配で惰行だけでは制動開始点に
届かない場合は V まで再力行する（この規則が無いと +11.4‰ で失速して駅間停車する）。

検証結果: **停止位置誤差 最大 0.142 m / 平均 0.009 m、駅間停車 0件**。

先行の出発遅延はCSVでは表現しない（全パターン t=0 出発）。
遅延は `出発間隔 headway = 標準出発間隔120秒 − 先行遅延` に換算して環境へ渡す
（既存と同じ headway 換算モデル）。

---

## 8. 運転モードの判定

`normal` / `delay_recovery` / `anti_mid_stop` / `spacing`（枠のみ） / `hold_at_station`

**モード分類NNを持たず、`reward_features_ymulti.decide_mode()` のルールで決定的に決める。**
既存 `direct_reward_predictor2._infer_mode` はモードNNのargmaxを併用していたが、
条件が成立し続ける区間でも `normal ↔ delay_recovery` が反転する問題が実測されている。
プロンプトに書くモード定義とこの関数を1対1で対応させることで、
LLMのラベルと実行時の判定が食い違わないようにした。

```
優先順位: 駅停車中 ＞ 駅間停車防止 ＞ 遅延回復 ＞ 通常運転
```

後続列車は本フェーズでは扱わないため `spacing` は非アクティブ（one-hotの枠だけ確保）。
LLM評価ランナーは `mode="spacing"` の応答を**検証エラーとして拒否**する。

---

## 9. ファイル一覧（すべて新規）

| ファイル | 役割 |
|---|---|
| `config_ymulti.py` | 区間・ダイヤ・停車・先行パターン・出力先の設定 |
| `brake_curve_ymulti.py` | 勾配・曲線を織り込んだ制動曲線（駅から逆積分） |
| `standard_curve_ymulti.py` | 2区間の標準運転曲線と `v_std` ルックアップ表の生成 |
| `required_speed_ymulti.py` | プロファイル積分の目標速度・CBTC現示・惰行到達可能性 |
| `generate_forward_train_ymulti.py` | 先行列車パターン（白兎・蚕桑の2駅で停車）312種 |
| `reward_features_ymulti.py` | 状態スキーマ（49列）・特徴量（72次元）・モード判定 |
| `environment_ymulti.py` | 複数駅走行＋駅停車フェーズを持つ強化学習環境（観測40次元） |
| `rule_reward_ymulti.py` | 暫定ルール報酬（フェーズ2用・プロンプトの実行可能な下書き） |
| `prompt_ymulti.py` | LLM評価プロンプト（3ノッチ・複数駅間・発車判断） |
| `generate_eval_csv_ymulti.py` | LLM評価用の走行ログCSV生成（正例7・負例5の方策） |
| `evaluate_csv_with_llm_ymulti.py` | LLM評価ランナー |
| `train_reward_network_ymulti.py` | 報酬予測NNの学習（HL-Gaussian＋ゲート） |
| `direct_reward_predictor_ymulti.py` | 学習済みNNの推論器 |
| `runcurve_plot_ymulti.py` | 運転曲線・ダイアグラムの描画 |
| `apex_ymulti.py` | Apex DQN 学習エントリポイント（Double DQN既定） |

### データ置き場（既存と分離）

| ディレクトリ／ファイル | 内容 |
|---|---|
| `standard_curve_ymulti/` | 標準運転曲線（CSV・PNG・`v_std_*.csv`・meta.json） |
| `input/f_train_ymulti/` | 先行列車パターン312種 |
| `評価用csv_Yamagata/` | LLM評価前の走行ログ |
| `評価済ログ_Yamagata/` | LLM評価済みデータセット |
| `train_reward_csv_direct_Yamagata/` | 報酬NN学習に使うCSV置き場 |
| `data_ymulti/` | 学習ログ・重み・運転曲線PNG |
| `direct_reward_{model,gate,scaler,manifest}_ymulti.*` | 学習済み報酬NN |

---

## 10. 実行手順

```bash
# ① 設定と路線諸元の確認
python config_ymulti.py
python brake_curve_ymulti.py
python required_speed_ymulti.py

# ② 標準運転曲線（v_std）の生成  ※環境がこれを参照するので最初に必要
python standard_curve_ymulti.py

# ③ 先行列車パターンの生成（312種）
python generate_forward_train_ymulti.py --jobs 6

# ④ LLM評価用の走行ログCSVを生成
python generate_eval_csv_ymulti.py --list      # 方策とシナリオの一覧
python generate_eval_csv_ymulti.py --rows 4500

# ⑤ LLM評価（.env に LLM_API_URL / LLM_API_KEY が必要）
python evaluate_csv_with_llm_ymulti.py --dry-run      # APIを呼ばずプロンプトを検証
python evaluate_csv_with_llm_ymulti.py --limit 20     # 少数で試す
python evaluate_csv_with_llm_ymulti.py --workers 6    # 本番

# ⑥ 評価済みCSVを学習用ディレクトリへ置いて報酬NNを学習
cp 評価済ログ_Yamagata/llm_evaluated_dataset_ymulti.csv train_reward_csv_direct_Yamagata/
python train_reward_network_ymulti.py

# ⑦ Apex DQN 学習
python apex_ymulti.py                 # 報酬NNがあれば自動でNNを使う
python apex_ymulti.py --reward rule   # 報酬NNが無い段階の動作確認
python apex_ymulti.py --actors 4 --no-double
```

⑤⑥がまだの段階でも、④までを済ませれば `--reward rule` で学習ループを回して
環境・DQNの構造を検証できる。

---

## 11. 残課題・今後

1. **LLM評価の実行**（⑤）。`.env` の設定が必要なため未実行。
   `--dry-run` でプロンプトが全行組めることは確認できる。
2. **報酬NNの学習**（⑥）。⑤の完了後。学習後は
   `analyze_reward_imbalance.py` 相当の診断で**駅停車中のラベル分布と校正**を必ず確認すること
   （停車中のサンプルは走行中より少ないため不均衡の診断が必須）。
3. **後続列車の導入**。状態スキーマに `b_*` 列と `backward_info` の枠を確保済みで、
   `spacing` モードも one-hot の枠を取ってある。導入時に列を足さずに済むようにしてある。
4. **TASC（停止位置制御）の後処理**。既存 `apply_tasc_to_runcurve.py` は単一区間・
   `environment2` 前提なので、複数駅間版は別途必要。
5. **運転曲線モニター**（`drive_monitor.py`）の複数駅間対応。
   `apex_ymulti.py` の Tester が出す CSV は列構成が異なるため、現状は読めない。
