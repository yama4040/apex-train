# 先行列車を考慮した運転曲線最適化 — 設計メモ（2026-07-20）

単一列車の駅停車がほぼ達成されたため、次フェーズとして先行列車（将来的には後続列車も）を考慮した運転最適化に取り組む。本メモは方式検討の結論と、実装前に詰めるべき論点をまとめたもの。

## 1. 目標（CLAUDE.mdより）

### 先行列車のみ
- 先行列車が遅延していても**駅間に停車せず**次駅まで走行（停車しない速度で惰行する方策）
- 接近しすぎた場合はCBTC停止限界（先行の50m手前）までに停止できる
- 信号開通後は駅に向かって加速し、駅停車に向けて減速できる

### 先行・後続の両方
- どちらかが遅れている場合、**列車間隔が一定になるよう速度調整**（早めの惰行など）

### 共通
- 間隔保持のための遅延はやむを得ないが最小限に
- 駅間停車は極力回避
- ノコギリ運転なし・省エネ・乗り心地（単一列車時と同様）

### 設定
- 各列車の駅停車時間30秒／CBTC停止限界50m（先行最後尾〜自列車先頭）／1両編成・列車長20m

## 2. 採用方式: 1回LLM呼び出し＋二重蒸留

### 検討した案
- 案A: 1回のLLM呼び出しでモード判定＋評価を同時出力
- 案B: LLM2回呼び出し（1回目モード判定→2回目モード入力で評価）
- 案C: モード判定をPythonルール化＋LLMは評価のみ

### 結論（採用）
**1回のLLM呼び出しで {mode, reward, reason} を同時出力し、蒸留NNを2つに分ける**:

```
【データ生成】 状態 → LLM（1回） → {mode, reward, reason}
【蒸留】      モードNN:  状態(46次元) → mode（4クラスsoftmax）
              評価NN:    状態 + mode one-hot(4)（=50次元） → reward（ゲート＋回帰の2段構成を維持）
【RL実行時】  状態 → モードNN → argmaxモード → 評価NNへ入力 → 報酬
```

### 採用理由
1. **研究仮説との一貫性**: 「LLMは文脈から状況判断できる」という仮説を、評価だけでなく状況判断（モード決定）の蒸留にまで貫徹できる（階層的蒸留）。案Cはこの仮説の半分を放棄する
2. **実行時整合**: RL実行時のモードはモードNN（毎ステップ推論可能）から供給され、学習時（LLMモードラベル→評価NN教師入力）と同一経路。LLMにモード判定させつつ実行時に再現できる唯一の構成
3. **コスト**: 案Bは評価時間・API失敗面が2倍（実測90秒/行前後）。1回呼び出しなら×1のまま
4. **診断性**: モードNNの精度（混同行列）を独立に測定でき、報酬の誤りを「モード判定」と「評価」に切り分けられる。ゲート分類器の成功実績あり、4クラス分類は回帰より易しい

### 運転モード（フェーズ1は3種で開始／将来4種）
通常運転 / 遅延回復モード / 駅間停車防止モード（＋フェーズ2で間隔調整モードを追加）
（定義・優先順位はLLMプロンプト側に明文化。判定はLLM、模倣はモードNN）

**決定事項（2026-07-22）:**
- **モード数**: 間隔調整モードは後続列車前提でフェーズ1ではデータがほぼ0のデッドクラスになるため、フェーズ1は**3モードで学習**する。ただしモードone-hotの次元は**4で確保**しておき、フェーズ2で間隔調整を追加してもモードNNの再学習のみで済ませ、評価NNの入力次元（50次元）は据え置く。
- **優先順位（同時成立時の採用順）**: 安全(制限速度/CBTC) > 間隔調整 > 駅間停車防止 > 遅延回復 > 通常。フェーズ1の実効順は **安全 > 駅間停車防止 > 遅延回復 > 通常**。

## 3. 詰めるべき論点

### ①【最大の未決】後続列車が環境に存在しない
- 現状 `backward_info="後続列車なし"` 固定。間隔調整モードの半分は後続前提
- **フェーズ分割を推奨**: フェーズ1＝先行のみ（駅間停車回避・CBTC・解放後回復）→ フェーズ2＝後続追加
- 後続の実装は先行列車方式（CSV軌道の直接再生・7/17実装）を流用可能。開始条件（自列車の後ろへいつ発車させるか）の設計が必要

### ② 誤りの連鎖（カスケード）
- モードNNの境界誤判定→評価NNが誤ったモード前提で確信的に採点
- 対策: 評価NN入力はハードone-hot（LLMラベル／argmax）で学習・実行を統一。モードNNの混同行列で弱い境界を特定し、そこへグリッドデータを追加する運用（既存手法のモード版）

### ③ モードのチャタリング
- 0.1秒刻みで境界付近のモードが毎ステップ入れ替わると報酬・Q入力が暴れる
- 対策候補: (a)実行時のみ最低保持時間（3〜5秒）、(b)softmaxのEMA平滑
- ただし両方とも「LLM模倣からの逸脱」を含むため、**最初の評価バッチでLLMモードラベルの軌道沿い安定性を実測してから決定**する

### ④ 既存データ（約26,000行）のモードラベル
- 選択肢: (a)全再評価（純度最高・コスト大）／(b)先行なし行は自明なので機械付与（先行なし×delay小→通常等）し、**先行あり行（約2,500行）＋新規データのみLLM評価**
- 現実解は(b)。ただし機械付与の許容範囲は要方針決定

### ⑤ プロンプト設計
- 出力JSONに `"mode"` を追加。モード定義（4択・優先順位）→そのモード基準での採点、をStep化
- **既存ルールとの衝突解消が本丸**: 現行は「CBTC現示に沿ってブレーキ→駅間手前停止でも高評価」だが、新目標は「停止しないで済む早め惰行が上位」。駅間停車防止モードでは優先関係を再定義する必要がある
- モード別評価基準の同載で分量増（9,000字→1.2万字級）→ ルール混線リスク。**モードごとの実APIスモークテスト必須**（7/20のダイブ対応は3回の反復ガードが必要だった）

### ⑥ NN・周辺への波及
- 評価NN: 46→50次元（モードone-hot 4）。train/predictor/analyzeの3ファイル同期＋世代互換がまた増えるため、**特徴量リストをスケーラーと一緒にJSON保存しpredictorが読む「マニフェスト方式」への改修を推奨**（次元判定のif分岐が増え続けている）
- モードNN: 新規学習スクリプト（ゲート分類器と同型の小規模分類器で可）
- QNetwork（25次元）へのモード入力: モードは状態の関数なので情報的には冗長。**まず入れずに始め、学習が遅ければ追加**（入れる場合はnum_states・テスターCSVヘッダ38→42列等に波及）
- 評価済ログのCSVスキーマに `mode` 列追加

### ⑦ テスト・検証系
- テスターに遅延先行列車シナリオを追加（`f_train_delay5/10_stop*.csv` は生成済みだが**現在未使用**）
- analyze_reward_nn_vs_llm のルールベース検証にモード次元を追加
- 細部: 列車長20m（車間＝先行最後尾−自列車先頭）が現実装では点列車扱い。車間計算に20m補正を入れるかの確認

## 4. 実装順序（推奨）

1. プロンプト改訂（mode出力＋モード定義＋モード別評価基準、既存CBTCルールとの優先関係整理）
2. 数十行の実APIスモークで **LLMのモード判定品質と軌道沿い安定性を実測**（チャタ対策の要否判断）
3. モードNN学習スクリプト新設＋評価NN50次元化（マニフェスト方式導入）
4. environment2 に後続…ではなくフェーズ1: モードNN推論の組み込み（predictor経由）
5. 先行ありデータの評価（既存2,500行＋新規グリッド）→ 二重蒸留 → 検収プローブ（モード混同行列＋モード別報酬マップ）
6. テスターシナリオ拡充 → DQN学習

## 5. モードの定義メモ（2026-07-22 改訂）

### 前提：2種類の「遅延」指標
モード判定・評価には性質の異なる2つの遅延指標を用いる（[[hover-exploit-and-reward-calibration]]の残タスクとも関連）。

- **駅出発時遅延量（新規・エピソード中固定）**: 標準運転時間180秒に対し、駅出発時点でどれだけ遅れているか。`environment2.py`の`reset(delay=...)`引数から確定できるため、エピソード開始時に`self.departure_delay`として保持し、走行中は固定値として扱う。→ **遅延回復モードの判定に使用**（エピソード単位で安定＝チャタリングしない）。
- **走行中遅延 `current_delay`（既存・連続値）**: 計画ダイヤを過ぎてからの経過秒（残り時間が正の間は0）。走行中に先行へ詰まって遅れが拡大していく状況の検知・スコアリングに使用。

### モード定義（フェーズ1）
優先順位（高い順）: **安全(制限/CBTC) > 駅間停車防止 > 遅延回復 > 通常**。上位モードの条件が成立していれば下位モードには入らない。

1. **通常運転モード**
   - 条件: 駅出発時遅延 < 30秒 かつ 先行列車に塞がれていない（先行なし、または先行との距離・CBTC現示に十分な余裕がある）。
   - 方策: 省エネルギー最優先。早め惰行を許容し、無駄な加減速・ノッチ切り替えをしない。乗り心地重視。

2. **遅延回復モード**
   - 条件: 駅出発時遅延 ≥ 30秒 かつ 駅間停車防止モードが発動していない（＝先行に塞がれていない）。
   - 方策: 省エネルギー性より遅延回復を優先する。惰行を遅らせ制限速度近くまで力行を維持する運転を高評価とする。
   - 制約: 制限速度の超過は不可。ノッチを規定時間以上キープしないのこぎり運転も不可。
   - **通常モードとのスコア差別化（要）**: 通常なら高評価となる「早め惰行・低め巡航」を、このモードでは減点する。制限近くの巡航維持・積極的な力行を高評価とする。この差を明示しないと評価NNが2モードを区別できない（論点②カスケードの逆リスク）。

3. **駅間停車防止モード**
   - 条件: **駅出発時点**で先行列車の状態（位置・速度・在線/出発状況）を見て、このまま通常走行すると駅間に停車してしまうと物理的に予測できる場合。典型例: 駅出発時点で先行列車がまだ次駅に到着しておらず駅到着ブレーキ中／次駅を出発していないため、通常走行だと追いついてCBTCに引っかかり機外停車に至る。優先度は遅延回復より上。
   - **判定タイミング（決定 2026-07-22）**: **駅出発時点で発動を判定し、原則エピソード中保持**（解放条件を除く。下記参照）。「先行が駅到着ブレーキ扱い中→機外停車する前に早めの惰行を高評価」というLLMの物理予測を、駅出発時の先行スナップショットに基づいて行う。発動は一方向（発動→解放）なのでチャタリングは発生しない。
   - **必要な特徴量（論点D・G の解決方針）**: 下記「駅出発時スナップショットの項目」を参照。発動用の固定スナップショットと解放用の走行中現在値の双方をプロンプト・状態に載せる。
   - 方策: 早め惰行で駅間停車を回避する方策を獲得する。停止が避けられない場合は安全（CBTC停止限界50m以内での停止）を最優先とする。
   - **解放条件（決定 2026-07-22）**: **駅出発時に発動・先行クリアで解放**のハイブリッド。駅出発時点の先行スナップショットで発動し、走行中に先行列車が次駅を出発して十分に前方がクリア（CBTC現示に余裕）になった時点で通常/遅延回復モードへ解放する。純粋な駅出発時固定だと、先行が30秒停車後に発車して先行してしまい追いつかないケースで早め惰行が無駄な遅延になる（過保護）ため。
   - **モードNN上の含意**: 遅延回復モードが駅出発時遅延（エピソード固定値）のみをキーにするのに対し、駅間停車防止モードは①発動用＝駅出発時の先行スナップショット（固定特徴量）と②解放用＝走行中の先行クリア指標（CBTC現示速度／先行距離）の**両方**をキーにする。どちらも状態（46次元）に含める必要がある。解放後は駅出発時遅延に応じて通常/遅延回復へフォールスルーする。

### 駅出発時スナップショットの項目（確定 2026-07-22）
モード判定に用いる入力。**駅出発時に確定する固定スナップショット**（発動用）と**走行中の現在値**（解放用・既存）に分かれる。

**A. 駅出発時固定スナップショット（新規追加・エピソード中固定）**
1. **先行列車の遅延秒数** — 遅れ＝正の符号。先行が遅れているほど詰まる。取得方法は先行CSV（`f_train_delay*`）のメタデータ/ファイル名規約から（後工程で実装）。
2. **先行列車の距離** — 車間の定義は CBTC 基準（先行最後尾−自列車先頭、列車長20m補正）に統一（論点⑦）。
3. **次駅を発車済みか** — 対象は「自列車の次の停車駅」。ブール。先行が塞いでいるかの核となるフラグ。
4. **標準運転間隔** — 実装の `fowerd_train_time_offset`（先行が自列車より何秒先に出発したか）。apex2.pyでは `headway=random.uniform(30,120)`。エピソードごとに確定する有意な値。
5. **（既決）自列車の駅出発時遅延量** — 遅延回復モード判定と共通。実効車間 ≈ 標準運転間隔 ＋ 自列車遅延 − 先行遅延 の関係で、機外停車リスク判定に自列車遅延も効くため、モード判定Stepに併せて渡す。

**B. 走行中の現在値（既存・解放用）**
- 現在の先行距離・速度（`forward_info`）、CBTC現示速度（`signal_speed`）。駅間停車防止モードの解放（先行クリア）判定と、走行中の安全担保（上位優先）に使用。

**物理的関係**: 駅出発時の実効車間 ≈ 標準運転間隔 ＋ 自列車遅延 − 先行遅延。この値が小さいほど機外停車リスクが高い。距離単体（先行が駅到着ブレーキ中だと距離は大きいこともある）より、この時間ベースの関係が効く点をプロンプトで明示する。

### LLM推論のStep構造（確定 2026-07-22）
1回のLLM呼び出し内で2段階に分ける（[[hover-exploit-and-reward-calibration]]の段階評価化を踏襲）。
- **前段（モード判定）**: 優先順位（安全 > 駅間停車防止 > 遅延回復 > 通常）に従い、上記スナップショット＋走行中現在値から1モードを選び `mode` を確定。駅間停車防止は「駅出発時は詰まる予測か（発動）」＋「今は先行がクリアしたか（解放）」の両方を判断。
- **後段（評価）**: 選んだモードの評価基準に従って `reward` と `reason` を算出。
- **出力**: `{mode, reward, reason}`。

### 駅間停車防止モードの加速上限評価（target_speed_no_stop）（確定 2026-07-22）
「機外停車せずどこまで加速してよいか」の数値基準を、既存 `required_speed.py` の `calculate_required_speed` を転用して算出する。現行の required_speed は制約が「定刻までに駅到着」だが、これを「**先行がクリアするまで駅に着かない（先行に追いつかない）**」へ差し替える。

```
先行クリア残時間 = 先行列車が自列車の次駅を発車するまでの残り秒数
                  （先行CSVから算出。駅停車時間は先行CSVに織り込み済み）
実効所要時間     = max(定刻残り時間, 先行クリア残時間 + 安全マージン)
target_speed_no_stop = 「その速度から惰行すると実効所要時間ちょうどに次駅到着する速度」を二分探索
                      （※現在速度に依存しない状況ベースの上限。§7の【重要修正】参照。
                        先行が塞いでいない＝実効所要時間が定刻残り時間と同じ時は従来 required_speed に縮退）
```

- 先行が長く塞ぐほど実効所要時間が伸び、target_speed_no_stop は下がる＝低速で惰行トロトロ進めば機外停車しない、という**加速上限**が出る。
- 先行がクリア（先行クリア残時間 ≤ 定刻残り時間）になると自動的に通常の required_speed と一致 → **解放後の再加速が自然に高評価**（CLAUDE.md「信号開通後は駅に向かって加速」を表現）。

**プロンプトへの入力（決定）**: `required_speed`（定刻ベース・従来通り）と `target_speed_no_stop`（先行考慮）の**両方**を渡し、モードで使い分ける。
- 通常運転モード / 遅延回復モード → `required_speed`（従来通り）
- 駅間停車防止モード → `target_speed_no_stop`

**駅間停車防止モードの採点基準**:
| 自列車の操作 | 評価 |
|---|---|
| `target_speed_no_stop` を明確に超える方向への力行 | 減点（機外停車を招く過剰加速） |
| 上限付近まで（超えない範囲で）の加速・維持、超過なら早め惰行で上限へ収束 | 高評価 |
| 上限を大きく下回る過度な低速・惰行しすぎ（ちんたら） | 減点（無駄な遅延。停止回避マージンは許容） |
| CBTC現示が下がり減速している最中のブレーキ操作 | 高評価（衝突回避・その瞬間の正しい操作） |
| 機外停車して先行待ちしている**状態**（速度0・CBTC=0） | 中立 ~0.5（安全だが回避すべきだった事態。§6参照） |

**要決定の残パラメータ**: 実効所要時間の「安全マージン」（先行クリア直後に着かず少し余裕を持たせる秒数）。プロンプト実装・スモークテスト時に調整。

**実装上の含意**: 先行クリア残時間は走行中の連続値（先行位置の関数）。モードNN／評価NNが target_speed_no_stop を再現するには、先行クリア残時間または target_speed_no_stop を状態に含める必要がある（論点⑥マニフェスト方式で吸収）。

### 将来（フェーズ2）: 間隔調整モード
先行・後続列車が存在しどちらかが遅延している場合、次駅での列車到着間隔を一定に保つよう早めに惰行する（例: 2分間隔運行で後続が遅延したら2分10秒間隔程度で到着できるよう調整）。優先度は駅間停車防止より上。後続列車が環境に存在しないフェーズ1では判定・学習の対象外。

### 残る未決事項
- **論点D（先行在線情報の不足）→ 解決・項目確定**: 現状 `environment2.py:181` の `forward_info` は先行の距離・速度のみ。「駅出発時スナップショットの項目」の通り、先行遅延・先行距離(出発時)・次駅発車済みフラグ・標準運転間隔の4項目（＋自列車駅出発時遅延）を固定特徴量として追加する。残る後工程の実装課題は、先行遅延秒数の取得方法（先行CSVのメタデータ/ファイル名規約）と、追加特徴量による次元増（論点⑥マニフェスト方式）。
- **論点G（発動トリガー）→ 解決方針決定**: 駅出発時点の先行スナップショットに基づくLLMの物理予測で判定（モード3参照）。
- **駅間停車防止モードの解放条件 → 決定**: 駅出発時に発動・先行クリアで解放のハイブリッド（モード3参照）。
- **論点F（しきい値ギャップ・チャタリング）**: 駅出発時遅延1〜29秒は通常モード扱い（省エネ優先＝小遅延は積極回復しない方針）とする。駅出発時遅延をエピソード中固定にしたことで遅延回復モードのチャタリングは原理的に発生しない。

## 6. プロンプト再編方針（既存CBTCルールとの統合）（2026-07-22）

論点⑤の本丸。現行プロンプト（`evaluate_csv_with_llm.py`）の「先行列車がいる場合はCBTC遵守が絶対最優先、機外停車すら高評価」という思想（`:220-221` `:269` `:277`）と、新目標「早め惰行で機外停車を回避するのが上位」の衝突を解消する方針。

### 基本原則: per-step評価を守れば衝突は解ける
現行の大原則「現在のノッチ操作のみを評価（過去のミスも将来の結果も評価しない）」（`:101`）を守ると、両者は衝突しない。**評価するフェーズが違うだけ**である。

| フェーズ | 現在の正しい操作 | モードによる差 |
|---|---|---|
| 加速・巡航（速度・余裕あり） | 基準速度以下で惰行/巡航 | **モード差はここに集約**。基準速度を required_speed（通常）→ target_speed_no_stop（駅間停車防止）に切替。過剰加速を減点・早め惰行を高評価 |
| 減速・CBTC接近時（先行に迫る） | CBTC沿いブレーキ | 全モード共通で高評価（安全・衝突回避）。その瞬間のブレーキ操作自体は正しい |

→ **「機外停車したブレーキ操作」ではなく「機外停車に至る前の巡航フェーズでの過剰加速」を減点する**。これでper-step原則を壊さず、既存の安全ルール（即0.0・CBTC確認Step）は不変のまま維持できる。

### モードによる基準速度の切替
- 通常/遅延回復モード → `required_speed`（定刻ベース・従来通り）
- 駅間停車防止モード → `target_speed_no_stop`（先行考慮・§5参照）

加速フェーズの「力行継続=高評価」は駅間停車防止モードでは target_speed_no_stop で頭打ち。巡航フェーズの「惰行=高評価／基準割れ=ちんたら」も基準速度をモード別に読み替える。

### 機外停車待機状態の評価（決定）: モード別
- **先行への衝突**: 全モードで reward=0.0（不変）。
- **CBTC現示低下中の減速ブレーキ操作**: 全モードで高評価（動いている状態からの正しい衝突回避操作）。
- **機外停車して先行待ちしている状態（速度0・CBTC=0）**:
  - 通常/遅延回復モード → 現行通り高評価（避けられない急減速への正しい対応）。
  - 駅間停車防止モード → **中立 ~0.5 に下げる**。「安全だが回避すべきだった事態」。動機付けを **早め惰行で流して進む(>0.5) > 機外停車待機(0.5) > 衝突(0.0)** の単調にする。ゼロ中心化後 +0.5/step の積み上げによる hover 搾取（[[hover-exploit-and-reward-calibration]]）回避とも整合。
  - → 現行 `:269`「CBTC=0なら reward=1.0」を駅間停車防止モードでは ~0.5 に変更。

### ちんたら運転ルールのモード対応
現行のちんたら減点は「先行列車がいない（または600m超）」を条件にしており（`:174` `:190-194` `:249`）、駅間停車防止モード（先行が塞ぐ）では大半が発火しない。加えて駅間停車防止モードでは target_speed_no_stop までの低速走行は正当（ちんたらではない）。**target_speed_no_stop を明確に下回る過度な低速のみ**ちんたら扱いとする。

### 先行列車解放時の特例との整合
現行「先行列車解放時の特例」（`:292-297`、先行クリア後の再加速は適切）は、駅間停車防止モードの解放（target_speed_no_stop が上昇し再加速が高評価になる）と自然に整合。特例は維持。

### Step構造へのモード判定の挿入（案）
モード判定Stepを安全系Step（即0.0・制限速度・CBTC）の後、フェーズ評価の前に挿入し、フェーズ評価はモード別基準速度を使う。出力に `mode` を追加。
```
Step1 即0.0ルール確認 / Step2 制限速度 / Step3 CBTC
Step4 モード判定（優先順位: 安全 > 駅間停車防止 > 遅延回復 > 通常）
Step5 停止位置達成可能性 / Step6 フェーズ評価（モード別基準速度）
Step7 ノコギリ / Step8 運転間隔 / Step9 定時性・省エネ（モード別重み）
Step10 総合評価
```
出力: `{mode, reward, reason, checks}`（`mode` はchecksと別のトップレベル項目として追加。checksの8キーは不変）。

## 7. 実装進捗（案A: 通常⇄駅間停車防止を先行実装、遅延回復の差別化は後回し）

### 完了
- **A-1 共通算出関数（`required_speed.py`）**: `calculate_no_stop_target_speed()` を追加。`実効所要時間 = max(定刻残り時間, 先行クリア残時間 + 安全マージン)` を `calculate_required_speed` に渡す薄いラッパ。安全マージン `NO_STOP_SAFETY_MARGIN_S = 15.0`（暫定・スモークで調整）。検証済み: 先行クリア残150/250秒で上限が58.1→31.5→30.0と単調低下、クリア済み(0秒)でrequired_speedと一致。
- **A-2 プロンプト改訂（`evaluate_csv_with_llm.py`・加算的）**: 既存の通常モード挙動を温存しつつ以下を追加。
  - 「# 運転モード」セクション（3モード定義＋優先順位）
  - Step4「運転モード判定」を挿入（Step10まで繰り下げ）、出力に `mode`（normal/delay_recovery/anti_mid_stop）を追加
  - 「# 駅間停車防止モードの評価基準」セクション（target_speed_no_stop基準の採点・機外停車待機=中立0.5・解放）
  - 駅停車完了フェーズのCBTC=0→1.0ルールに、駅間停車防止モードの機外停車待機は0.5とする例外注記
  - `current_status` に先行スナップショット5項目＋target_speed_no_stopを表示
  - featuresに新CSV列（下記）を読み込み、target_speed_no_stopを算出（先行クリア残時間0なら通常のrequired_speedに縮退）
  - 出力CSVヘッダ・out_rowに新列＋modeを追加、`call_llm_for_eval` が `(reason, reward, mode)` を返すよう変更
  - 生成プロンプト約13,100字（先行塞ぎシナリオで生成・構文検証OK）
- **CSV列名（確定・apex2 Testerと共有）**: `forward_train_delay` / `forward_clear_remaining_time` / `forward_departed_next` / `standard_headway` / `target_speed_no_stop` / `mode`
- **プロンプトレビューでの修正（2026-07-23）**:
  - 機外停車と駅停車の区別を明示（速度0かつ次駅まで概ね30m以上＝機外停車0.5／所定停止位置付近＝駅停車は位置誤差で段階評価）
  - 「# フェーズ別評価基準」冒頭にモード読み替え注記（anti_mid_stopでは基準速度をtarget_speed_no_stopに読み替え・矛盾時は駅間停車防止セクション優先・加速フェーズの力行継続=高評価は上限超過に不適用）
  - **閾値の確定（開始値・A-4で調整）**: モード判定=target_speed_no_stopがrequired_speedより5km/h以上低い／過剰加速減点=target_speed_no_stop+5km/h超／ちんたら減点=target_speed_no_stop-10km/h超下回り

- **既存データ移行（完了 2026-07-23）**: `train_reward_csv_direct/` の既存59ファイル（計28,354行）を行単位で移行。
  - 先行なし行（24,100行）→ 新6列を付与して残置（`mode="normal"`、スナップショット4列=既定値、`target_speed_no_stop`=`required_speed`）。列順は評価CSVヘッダと一致。
  - 先行あり行（4,254行・10ファイル分）→ `退避_先行あり_旧基準_20260723/` に旧スキーマのまま退避（旧基準ラベルのため二重蒸留から除外、新プロンプトで再生成予定）。
  - 判定は forward_info に「前方」を含むか（reason中の「前方」は誤検出しない）。総数保存を確認済み。学習スクリプトは `feature_cols` で列を明示選択するため追加列で破綻しない。移行スクリプトはscratchpad。

### A-3 データ生成（実装済み・スモーク実行待ち 2026-07-23）
- **先行クリア残時間の算出**: 先行はCSV位置を直接再生（反応しない）。先行が自列車の次駅(arrival_station)を発車するCSV時刻 τ_depart を先行軌道から算出（`environment2._compute_forward_depart_time`、reset時にエピソード定数として保持）。実例: delay10_stop60は白兎(23.29)に t≈177着・**t≈238発**。駅停車時間は先行CSVに織り込み済み。
- **environment2.py**: `forward_clear_remaining_time = max(0, τ_depart − (t + headway))`（走行中の現在値プロパティ）、`forward_departed_next`（未発車/発車済み）を追加。検証済み。
- **apex2.py**: Tester/Actorで全10種先行CSV（`ALL_F_TRAIN_CSVS`）を使用。Actor headwayを20-120sに拡張、Testerは全10種×[30,60,90]s。TesterのLLM-CSVに5列（forward_train_delay/forward_clear_remaining_time/forward_departed_next/standard_headway/target_speed_no_stop）を追加出力。先行遅延はファイル名から取得（`parse_forward_train_delay`）。
- **【重要修正】target_speed_no_stopを現在速度非依存に変更**: 当初 `calculate_required_speed` をそのまま流用したが、同関数は「現在速度のまま惰行で間に合えば現在速度を返す＝早着を許容」する仕様のため、列車が加速するほど上限も上がり**過剰加速を検知できない**不具合が判明（実測: 過剰加速後に target が current に追随）。修正版は「その速度から惰行すると実効所要時間ちょうどに次駅到着する速度」を二分探索する状況ベースの上限（`required_speed.py`）。先行が塞いでいない時は従来 required_speed にフォールバック（先行なし行の target=required は不変）。検証済み: 塞ぎ中は current≫target で過剰加速を全区間検知。

### A-4 スモークテスト（実施済み 2026-07-23）
軽量生成スクリプト（scratchpad）で3シナリオ×10行=30行を生成しLLM評価（失敗0）。
- シナリオ: A=塞ぎ(delay10_stop60,hw25)×過剰加速方策 / B=同×早め惰行方策 / C=先行なし×対照。
- **検証OK**: ①モード判定が正確（塞ぎ→anti_mid_stop、先行なし→normal、対照C全10行normal）②過剰加速→CBTC現示超過→即0.0 ③衝突(先行40m未満)→0.0 ④target機構がCBTC非拘束の早期巡航で機能（B13: 先行615m先・cur44>tgt33→0.1）⑤通常モード温存（C=0.9-1.0）。
- **知見**: (1)強い塞ぎではCBTC違反が先に発火、target機構は先行が遠い早期巡航で補完的に効く（設計通り）。(2)スモークの早め惰行方策が力行↔惰行でノコギリ判定→テスト方策の問題でありプロンプトは正常（むしろノコギリルールが機能）。(3)**過剰加速後の「減速中の惰行」がstate基準で減点され、修正惰行のクレジットが弱い**（プロンプトの「上限超過からの惰行=高評価」が十分効かず）。ただしRL的には超過状態への減点→そこに至る加速の回避、かつ超過時は惰行(小減点)>力行(CBTC違反0.0)で差別化されるため、desired behaviorは創発する見込み。→ **要判断: (a)現状維持 / (b)プロンプトで修正惰行にクレジット明示**。
- 生成/評価スクリプトはscratchpad。1行あたり約25秒（本番データ生成の規模見積りに影響）。

### A-4 追加スモーク（平滑な早め惰行方策）とプロンプト修正（2026-07-24）
ノッチ最低保持8秒を強制した平滑方策で中/強塞ぎ2シナリオ×8行=16行を生成・評価。
- **検証OK**: 理想的な早め惰行（上限速度内・平滑）が高評価（rows 0-4,7-11 が 0.9-1.0）。プロンプトが desired behavior を正しく高評価すると確認。
- **発見した誤減点2件を修正**（desired behaviorを誤って減点していた）:
  - **Fix X（力行の除外）**: 「target−10km/h超下回り＝低速減点」が力行（上限へ加速中）にも適用され、加速中を誤ってちんたら減点していた（row5: 力行cur49<tgt64→0.3、row13: 力行cur44<tgt62→0.2）。→ 力行で上限へ速度回復中は低速減点の対象外と明記。
  - **Fix Y（required基準の排除）**: anti_mid_stopで required_speed を下回るのは当然なのに、LLMがrequired基準で「遅延リスク」と誤減点（row12: 惰行cur42≈tgt43→0.5）。→ このモードは required との比較を用いず target_speed_no_stop のみを基準とすると冒頭に明記。
- **既知の許容（課題1・現状維持）**: over-speed中の惰行（cur>tgtで惰行）はstate基準の減点。RL的に過剰加速回避の圧力になるため現状維持。
- **修正の効果を再評価で確認済み**: row5(力行)0.3→1.0、row13(力行)0.2→0.9（Fix X）、row12(惰行cur≈tgt)0.5→1.0（Fix Y）。誤減点3行が正しく高評価に。over-speed惰行のrow6/row14は0.3のまま（課題1・据置＝狙い通り）。
- **プロンプトの最終確認 完了（A-4クローズ）**: モード判定・安全・衝突・通常温存・理想早め惰行の高評価・過剰加速抑制がすべて検証済み。案A（プロンプト実装）は完了。

## 8. 本番データ生成（先行あり）（2026-07-24）

### 方針（ユーザー決定）
- 先行なしデータ（移行済み24,100行・mode=normal）はそのまま使用。
- 先行ありは新規生成しLLM評価。**3ファイル（3PC並列）× 各1000〜1500行**。生成方法はシナリオを意図的に網羅し、学習結果を見て判断。

### 生成物
`評価用csv_先行あり_20260724/PC1〜3/eval_forward_part{1,2,3}.csv`（各**1240行**・計3720行）。列は評価用スキーマ（先行スナップショット5列＋reward列。modeはLLM評価時に付与）。
- **グリッド**: 先行CSV10種 × headway[20,35,50,70,100]s × 方策3種（aggressive過剰加速/ideal理想早め惰行/explore探索ε=0.5）= 150エピソード、各30行サンプリング、シャッフルして3分割。生成器はscratchpad（`gen_eval_dataset.py`、seed=42）。
- **分布**: 実質塞ぎ行（clear>0 かつ req−tgt≥5）40%、発車済み1136/未発車2306/空(low50)278、ノッチ 力行1483・ブレーキ1490・惰行747、フェーズ 巡航1593・減速1736・加速391、衝突候補(先行40m未満)あり。
- **required_speed.py修正**: `calculate_no_stop_target_speed` で先行クリア残時間≤0（クリア済み/先行なし）は安全マージンに関わらず required_speed に縮退するよう修正（駅接近で残り時間<15sのときマージンでtargetが不当低下するアーティファクトを解消）。再生成後、クリア済み1414行すべてで target=required を確認。

### 次段（②以降）
- 3PCで `evaluate_csv_with_llm.py` を各ファイルに実行（1行約25秒＝1ファイル約8時間）→ mode付き評価済みデータを得る。
- 先行なし24,100行と統合 → 二重蒸留（モードNN＋評価NN50次元・マニフェスト方式）。anti_mid_stopクラスは相対的に少数（塞ぎ約1500行）のためクラス重み付け要検討。

## 9. 二重蒸留NNの構築（完了 2026-07-25）

LLM評価済みデータ（計27,675行: normal 25,958 / anti_mid_stop 1,493 / delay_recovery 224）から2つのNNを蒸留。

### 共有モジュール化（設計メモ§3⑥マニフェスト方式の本命）
- **`reward_features.py`（新規）**: 特徴量エンジニアリングを1モジュールに集約し学習・推論で共有（従来の二重管理による不一致を解消）。状態特徴量 **52次元**（旧46＋先行スナップショット6: `target_speed_no_stop` / `speed_margin_to_target`(=cur−target・採点の要) / `forward_clear_remaining_time` / `forward_train_delay` / `forward_departed_flag` / `standard_headway`）。`MODE_CLASSES`=4次元one-hot（spacing予約）、実学習は3クラス。

### モードNN（`train_mode_network.py` 新規）
- 状態52次元 → 3クラスsoftmax。balancedクラス重み（normal 0.36 / delay_recovery 41.2 / anti_mid_stop 6.2）。
- 成果物: `mode_model.h5` / `mode_scaler.pkl` / `mode_manifest.json`。
- **結果（テスト5,535行）**: accuracy 0.99・macro-F1 0.97。anti_mid_stop recall 0.98/precision 0.92、delay_recovery recall 1.00。混同行列で誤りは境界の少数のみ。

### 評価NN（`train_reward_network2.py` 改修）
- 入力 = [正規化状態52 | mode one-hot 4] = **56次元**。既存の2段構成（ゲート分類器＋回帰器）維持。学習時はLLMのmodeラベルでteacher forcing。スケーラは状態52次元でfit、one-hotは正規化後に付加。
- 成果物: `direct_reward_model2.h5` / `direct_reward_gate2.h5` / `direct_reward_scaler2.pkl` / `direct_reward_manifest.json`。
- **結果**: 合成モデル テストMAE 0.0804、報酬リーク3.0%（0.0ラベルの平均予測0.026）、非0.0ラベルMAE 0.110。

### 推論・環境統合
- **`direct_reward_predictor2.py`（改修）**: 2段推論（状態→モードNN argmax→mode one-hot→[状態|one-hot]→評価NN）。共有モジュール使用。`last_mode`で判定モードを保持。モデル/モードNN欠損時は後方互換（0.0/normal固定）。
- **`environment2.py`（改修）**: state_infoに先行スナップショット5項目を追加（`target_speed_no_stop`はcalculate_no_stop_target_speedで算出、`forward_train_delay`は先行CSVファイル名から取得）。
- **エンドツーエンド検証**: 先行なし→normal/報酬1.0、塞ぎ上限内加速→anti_mid_stop/0.8、塞ぎ過剰加速→anti_mid_stop/0.2、塞ぎ上限付近惰行→anti_mid_stop/0.7。environment2経由でも過剰加速時にスケール後報酬が負まで低下しLLM挙動を再現。

### 学習曲線（それぞれ出力・2026-07-25）
- モード分類器: `train_mode_network.py` → `learning_curve_mode.png`（loss＋accuracy）。
- 評価回帰器: `train_reward_network2.py` → `learning_curve_direct.png`（Huber loss＋MAE、既存）。

### 分析ツール改修（`analyze_reward_nn_vs_llm.py`・2026-07-25）
二重蒸留に対応。`main()`＝新しい`run_distillation_analysis`（旧回帰/分類の比較は`main_legacy`に退避、新回帰NNの旧経路は56次元と不整合のため無効化）。
- **① モード分類の正しさ**: モードNN予測 vs LLMモードラベルの混同行列・分類レポート（`mode_nn_confusion.png`）。実測: accuracy 0.99、anti_mid_stop recall 0.99/precision 0.92。
- **② モード別の評価模倣**: 評価NN出力 vs LLMラベルをモード別MAEで集計。**教師強制（LLMモード入力）と完全パイプライン（モードNN→評価NN）の両方**を出力しカスケード影響を可視化。実測: MAE normal 0.067 / delay_recovery 0.077 / anti_mid_stop 0.098、カスケード影響 平均0.0002（モードNN高精度のため報酬にほぼ波及せず）。モード別ヒートマップ（`evalnn_heatmap_mode_*.png`）・分布比較・グループ別誤差・ルールベース検証も出力。

### 残・波及
- QNetwork（25次元）へのmode入力は未追加（§3⑥「まず入れずに始め、学習が遅ければ追加」の方針）。次段はapex2.pyでのDQN学習（mode対応報酬関数を使用）。

## 10. 不足部分の追加データ生成（2026-07-25・DQN学習中に空きPC2台で評価予定）

現行データセットの手薄領域を分析（anti_mid_stop 1,493 / delay_recovery 224 / 機外停車待機22）し、ギャップを狙った2ファイルを生成（各≤700行）。生成器はscratchpad `gen_eval_gap.py`（CPU限定でDQN学習と非競合）。
- **不足の特定**: anti_mid_stopの中間報酬帯0.4〜0.7が計29行のみ（MAE 0.098の主因）／機外停車待機22行のみ／delay_recovery薄い。
- **File A（`評価用csv_先行あり_gap_20260725/PC_A/`・344行）**: 中間報酬帯 anti_mid_stop。中程度の塞ぎ×hover_over方策（上限+5〜8km/h）で、塞ぎ中(未発車)で上限を中程度超える巡航行に絞り込み。中程度超過(tgt+3〜12) **215行**を確保。
- **File B（`.../PC_B/`・490行）**: 機外停車待機（強い塞ぎ×cbtc_follow方策で先行50m手前停止・低速行優先で17行）＋ delay_recovery（非塞ぎ大headway×ego遅延30-50×over_drive方策で132行）。
- 評価は空き2PCで `python evaluate_csv_with_llm.py 評価用csv_先行あり_gap_20260725/PC_A 評価済ログ_gapA`（PC_Bも同様）。評価後、既存データセットへ統合し二重蒸留を再学習してMAE改善を確認する。

### 追加3ファイル（2026-07-26・生成器 scratchpad `gen_eval_more.py`、CPU限定）
現行の手薄領域をさらに補う3ファイル（各≤500行）。`評価用csv_先行あり_more_20260726/PC1〜3/`。
- **File1 midrange（190行）**: 中間報酬帯 anti_mid_stop。全行未発車で中程度超過131行（offset刻み4/7/10）。
- **File2 release（387行）**: 解放transition。先行クリア後の再加速（発車済み×力行）147行＝CLAUDE.md「信号開通後の加速」。cbtc_follow方策。
- **File3 diverse（358行）**: delay_recovery 50行＋未サンプルheadway(28/45/60/85)での anti_mid_stop 多様性。
- 評価: 各PCで `python evaluate_csv_with_llm.py 評価用csv_先行あり_more_20260726/PC{1,2,3} 評価済ログ_more{1,2,3}`。

## 11. Testerのテストパターン再定義＋運転曲線のモード別配色（2026-07-26）

### テストパターン再定義（apex2.py Tester、16パターン）
- 通常（遅延なし・先行なし）／先行なし×自列車遅延[15,30,60]／先行あり×先行遅延[0,30,60,90]×次駅停車[30,45,60]（自列車遅延なし）。
- **先行遅延はheadway換算モデル**: 羽前成田の標準出発間隔120秒から `headway = 120 − 先行遅延`（0→120, 30→90, 60→60, 90→30）。通常走行の先行CSV `f_train_delay0_stop{30,45,60}` を流用（新CSV不要）。`forward_train_delay = 120 − headway` を `environment2.reset(forward_train_delay=...)` で明示指定（Actorは従来通りファイル名から取得＝影響なし）。Actorは変更せず（Tester のみ）。

### 運転曲線のモード別配色（apex2.py）
- 各ステップの `env.reward_predictor.last_mode` を記録し、速度-位置図・ダイヤ図の自列車曲線を**モード別に配色**（通常=赤／遅延回復=緑／機外停車防止(anti_mid_stop)=オレンジ／間隔調整=紫）。`plot_curve_by_mode`（連続同一モードをまとめて描画）。凡例にモード別ラベル。
- 検証済み: 先行あり塞ぎシナリオで anti_mid_stop=オレンジが明瞭に描画、塞がれ後の遅延で delay_recovery=緑も出現。

### 既知の制約（受容・2026-07-26）
- **先行なし＋自列車遅延（Sim2）は delay_recovery（緑）にならず normal（赤）のまま**。原因: ①`current_delay`は残り時間が正の間は0（標準運転時間超過まで遅延と認識しない）②データ移行で先行なし＋遅延行をすべてmode=normalとしたためモードNNが先行なしのdelay_recoveryを学習していない。delay_recovery（緑）は主に先行ありで塞がれて遅延した後に出現する。将来、遅延回復モードを正式対応（駅出発時遅延の特徴量化＋再ラベル＋モードNN再学習）する際に解消予定。

### 反映タイミング
- **変更は次回 apex2.py 起動時に反映**。現在学習中のプロセスは旧コード（旧34パターン・単色赤）で動作中。次のrunから新16パターン・モード配色になる。

## 12. DQN失敗の診断と対策データ（2026-07-26）

### 診断（run 20260725230521, cycle 13750）
強い塞ぎ（ci31: delay10_stop60_hw30）でDQNが失敗：ノッチ反転45回（ノコギリ）・上限超過115行（過剰加速）・先行最接近19.7m（追突）・平均report報酬0.11。
- **根本原因**: 過剰加速115行のモードは全てanti_mid_stop（判定は正しい）だが、**報酬NNが「上限超過中のブレーキ/惰行」状態に0.8-0.9を出す**（81/115が≥0.7）。ブレーキ＝安全・修正行動として高評価するため、「力行で過剰加速→ブレーキ」のサイクルが報酬的に得になり、かつノッチ保持約8秒でノコギリ判定（7秒閾値）を僅かに回避 → **過剰加速ノコギリの搾取**。

### 対策データ（3ファイル・1フォルダ `評価用csv_対策_20260726/`、生成器 scratchpad `gen_eval_fix.py`）
失敗領域を密に再ラベルし報酬勾配を鋭くする。強い塞ぎ delay10_stop{45,60}×小headway(25/32/40)で生成。
- **fix_1_over_accel_power（381行）**: 力行で上限超過（過剰加速の負例）→ 力行過剰加速のpenalty強化。
- **fix_2_forward_approach（800行）**: 先行接近(40-120m)・CBTC現示超過（機外停車寸前の負例）＝**報酬NNが0.8を出す漏れの核心状態**。LLM再ラベルが「データで直るか／プロンプト修正が要るか」の判定材料。
- **fix_3_ideal_coast（800行）**: 上限内で滑らかに追随（理想早め惰行の正例）→ 正しい挙動の明確な目標。
- 評価: `python evaluate_csv_with_llm.py 評価用csv_対策_20260726 評価済ログ_対策`（計1,981行）。評価後、既存データに統合し二重蒸留を再学習。
- **未解決の懸念**: fix_2 のブレーキ×上限超過状態をLLMも高評価する場合、修正ブレーキを高評価しすぎるプロンプト設計（§6 課題1）が根因となり、プロンプト側の対応（上限超過状態への減点強化 or ノコギリ判定閾値の見直し）が別途必要。

### 対策データ投入・再学習後の分析（2026-07-28）
評価NNのみ再学習（モードNNは未再学習）。データセット 30,737行（anti_mid_stop 1,493→**3,777**）。ci31過剰加速115行を再診断：
- **力行で上限超過: 0.8→0.20**（fix_1が奏功・LLMラベルも平均0.16）✅ 過剰加速のpenalty強化に成功。
- **惰行で上限超過: 0.27**（LLMラベル0.30）✅。
- **ブレーキで上限超過: 依然0.69**。LLMラベルが**二峰性**（0.0が594行＝危険 / 0.9-1.0が266行＝適切ブレーキ）で回帰器が平均化。
- **真の残存漏れ = cur>signal+2（CBTC現示超過）なのにブレーキ中で高評価(≥0.7)の72状態**（例: cur38/sig26/先行82m、gate≈0）。
- **根本原因（データで直らない）**: プロンプトの不整合。即0.0ルールは「signal_speed超過→0.0」だが、CBTCセクションは「ブレーキ中の信号対応」を許容的に扱うため、LLMが cur≫signal のブレーキを0.7前後にラベル。→ **プロンプト修正が必要**: 「cur > signal + 2km/h は notch に関わらず 0.0（危険）、高評価は cur≒signal±2 のブレーキに限る」を明確化し、対策データを再評価。
- **併せて**: モードNNを増えたデータ（anti_mid_stop 3,777）で再学習推奨。

### プロンプト修正（CBTC現示超過の整合化）＋再学習データ（2026-07-28）
残存漏れ（cur≫signalのブレーキが0.7）に対し、`evaluate_csv_with_llm.py` のCBTCルールを3箇所整合化：
- **`current_speed > signal_speed + 2km/h` はノッチに関わらず reward=0.0**（ブレーキ中でも現示超過は危険＝CBTC違反）。高評価は `cur ≒ signal ±2` のブレーキに限る。過剰加速で現示を超えた後のブレーキも0.0（早め惰行で超えないようにすべきだったため）。
- 修正箇所: 即0.0ルール「signal_speed超過」／CBTCセクション評価ルール／次駅減速フェーズ・先行列車が居る場合。
- **再学習データ 3ファイル（1フォルダ `評価用csv_CBTC修正_20260728/`、生成器 scratchpad `gen_eval_cbtc.py`）**: 強い塞ぎ delay10_stop{45,60}×小headway(22/28/35)で cur=signal+2 の境界を densely 生成。
  - `cbtc_1_over_brake`（500行）: ブレーキ×CBTC超過 → 0.0（漏れの核心）
  - `cbtc_2_over_power_coast`（65行）: 惰行×CBTC超過 → 0.0（力行×超過は物理的に稀で0行）
  - `cbtc_3_follow_and_coast`（500行）: 追随(cur≒signal±2ブレーキ)250 ＋ 現示下の早め惰行250 → 高評価
- 評価: `python evaluate_csv_with_llm.py 評価用csv_CBTC修正_20260728 評価済ログ_CBTC修正`。評価後、既存データへ統合し**評価NN・モードNNを再学習** → ci31のブレーキ×CBTC超過が0.0側に矯正されるか（漏れ解消）を確認。
- **A-4 スモークテスト**: 数十行の実APIでモード判定品質・軌道沿い安定性・target_speed_no_stop基準の採点挙動を実測。

### 注意点
- 遅延回復モードの判定は暫定で走行中 `delay`（current_delay）を代理指標に使用（駅出発時遅延の固定特徴量は未実装）。評価基準は通常モードと同一のため案Aでは無害。将来 §5 の駅出発時遅延に差し替え＋力行高評価の差別化を行う。
## 13. 先行の次駅停車遅延の因果化（未来情報リークの解消）（2026-07-28）

### 問題（ユーザー指摘）
`先行クリア残時間` を `τ_depart`（先行の実発車時刻）＝先行CSVの全軌道から算出していたため、
**自列車が先行の未来の次駅延着（停車45/60秒）を、先行が次駅に着く前から知っている**（未来情報リーク）状態だった。
現実には自列車は「先行が標準停車30秒を超えて停まり続けるのを観測して初めて」延着を知る。

### 決定（相談 A/A/A）
- **相談1-A**: 先行の次駅到着時刻は先行の実軌道から取る（巡航中の到着は滑らかに予測可能＝観測情報）。`τ_depart見積 = τ_arrive + 標準30秒`。延着は先取りしない。
- **相談2-A→B案（2026-07-28 変更）**: 当初は先行が標準30秒超で停車中を継続値(sentinel)で低速誘導する案だったが、**sentinelは撤廃**。標準超過の延着は `forward_observed_delay`（観測遅延）で表現し、「加速不要・惰行継続が適切」の判断は**プロンプトの観測遅延ルールに委ねる**（LLMが延着を直接判断＝研究仮説と整合）。延着観測中は `先行クリア残時間=0`（target は required に戻る）。
- **相談3-A**: 「先行の次駅での観測遅延[秒]」を明示的にプロンプト＋NN特徴量に追加。**B案では判断の主軸**。

### 実装（`environment2.py`）
- `_compute_forward_arrive_time`（先行の次駅到着時刻）を追加。`forward_arrive_time` をreset時に保持。
- `forward_clear_remaining_time` を因果化: `max(0, (τ_arrive+30)−f_tau)`（到着＋標準30秒で見積もり）。標準超過後は0（B案・sentinelなし）。
- `forward_observed_delay` プロパティ追加（標準30秒超の停車秒数・観測情報）。state_infoに追加。
- `forward_departed_next` は実発車ベース（観測可能）を維持。
- 検証済み: delay10_stop60・headway60で、到着前は標準30秒前提（旧リーク値178→因果147）、標準超過後は先行クリア残=0＋観測遅延0→28に増加、実発車で観測遅延も0に解放。
- **プロンプト（B案）**: 駅間停車防止モードの評価基準に「観測遅延 > 0 → 加速（力行）は不要・惰行継続/低速維持を高評価」ルールを追加。モード判定材料にも「観測遅延 > 0」を anti_mid_stop の根拠として追加。target_speed_no_stopを根拠に加速可と判断しないよう明記。

### 波及（コード変更済み・要データ再生成＋再学習）
- `reward_features.py`: 状態特徴量に `forward_observed_delay` を追加（**52→53次元**）。
- `evaluate_csv_with_llm.py`: features読込・プロンプト表示・出力列に追加。target_speed_no_stopは因果的forward_clearから再算出される。
- `apex2.py` Tester: LLM-CSVに `forward_observed_delay` 列を追加、`forward_clear_remaining_time` は因果値。
- 定数: `STANDARD_DWELL_S=30`（sentinelは撤廃）。

### 将来モード（別途）
先行の予定停車時間（例: 時間調整で60秒停車）を事前に自列車へ渡すモードを追加予定。その際は `forward_depart_time`（実発車・延着込み）を可視化し、「低速出発 vs 駅待機」を判断させる。現状は因果版のみを特徴量に使用し、実発車時刻は内部に保持（将来モードで切替）。

### 残（データ・学習）※要相談
- 既存の先行あり評価済みデータはリーク値のtarget/クリアで採点されており、因果版と不整合＋観測遅延列なし → **因果版で再生成＋LLM再評価が必要**。
- 先行なしデータ（24k）は `forward_observed_delay=0` 列を機械付与（軽微移行）。
- モードNN・評価NNを53次元で再学習。

### 因果版データ再生成（2026-07-29・相談4=A）
先行あり評価済みデータ（リーク版7,647行）を退避し、因果版を新規生成（生成器 scratchpad `gen_eval_causal.py`、CPU・DQNと非競合）。先行なし24,100行は `forward_observed_delay=0` を付与して残置。
- 出力 `評価用csv_先行あり_因果_20260729/PC1〜3/`（計1,863行）。headway換算（先行遅延=120−headway）。
  - **PC1 causal_A_broad（440行）**: 広域グリッド（先行遅延0/30/60/90×停車30/45/60×over_drive/cbtc_follow）
  - **PC2 causal_B_boundary（853行）**: 過剰加速・CBTC現示超過(598行)・先行接近の境界/失敗領域
  - **PC3 causal_C_obsdelay（570行・B案の核心）**: 先行が次駅で延着停車中（観測遅延>0）に自列車が加速(力行70＝負例)／惰行・減速(500＝正例)する状態
- 評価: 各PCで `python evaluate_csv_with_llm.py 評価用csv_先行あり_因果_20260729/PC{1,2,3} 評価済ログ_因果{1,2,3}`。
- 評価後、先行なし24,100行と統合 → **モードNN・評価NNを53次元で再学習** → 観測遅延ルール（先行延着中の加速=減点/惰行=高評価）が学習されたか、ci診断で確認。

## 14. QNetwork入力へ target_speed_no_stop＋モードone-hot を追加、前後比較（2026-07-31）

### 動機
run 20260730155242（25次元）の分析で、**中程度の塞ぎ（ci7〜12）では発車直後が通常運転モードで制限70km/hギリギリまで加速**する現象を確認（ユーザー指摘）。原因は、発車時点は先行がまだ遠く `target_speed_no_stop ≈ required_speed ≈ 70` となりモードNNがnormal判定→DQNが制限まで加速する後追い（reactive）構造。QNetworkの入力（25次元）に **target_speed_no_stop もモードも含まれていない**ため、方策は「機外停車を避ける加速上限」を直接観測できていなかった。

### 実装（25→30次元）
- `environment2.py` `normalized_state`: 26番目 `target_speed_no_stop/80`、27〜30番目 **モードone-hot(4)**（normal/delay_recovery/anti_mid_stop/spacing）を追加。`MODE_INDEX` 定数追加。target は step() の報酬側と同一ロジック（`required_speed.py`）で観測内でも算出。
- モードのタイミング整合: step() は「行動適用後の状態」で `predict_reward`→`last_mode` 確定→`normalized_state` 返却の順のため、`self.current_mode`（reset直後は'normal'、step末で更新）は**返す観測と同一状態のモード**（ラグなし）。
- `apex2.py`: `num_states` 25→30。Tester state CSV に `norm_target_no_stop`＋`mode_*`4列を追加（38→43列）。
- `analyze_qnet_coverage.py`: `NUM_STATES=30`。先行なしシナリオのため target≒制限速度・mode=normal固定で補完。
- 起動時に重みロードしないため新規runは一から30次元で学習（旧25次元重みとは非互換）。新run = `data/20260731112624/`。

### 前後比較（中間評価・両run同一 cycle 9850／報酬NNは同一53次元）
※新run(30次元)は学習途中（cycle 9850、旧run最終25900の約38%）。公平化のため**両runを同一cycle 9850で比較**。

- **集計報酬（窓平均 cycle 8000〜9850, n=38）はほぼ同等**：
  - history（全16ケース合計）: 旧 714.1 / 新 731.0（+2%・ノイズ域）
  - history_0（ci0通常）: 旧 74.4 / 新 74.6（同等）
  - history_f（先行あり最終報酬）は±0.5と過ノイズで比較不能
  - → **入力5次元追加による収束速度の悪化は見られない**（同budgetで同等以上）。
- **発車時の過剰加速（中塞ぎ ci7〜9）は前後で変わらず**：ci7の運転曲線は新旧ほぼ同一（発車Normalで68km/hまで加速→AntiMidStop→追走→到達）。序盤最高速も両run 43.5km/h。**入力追加だけでは発車過剰加速は解消しない**。
  - 原因の再確認: ci7(hw90)は発車時 target≈70（先行が遠くクリア余裕大）。target を入力に入れても、target自体が高いため方策は正しく高い上限を追う。問題は方策の観測不足ではなく **target_speed_no_stop が発車時に高い（reactiveで先読みしない）こと**。
- **中〜強塞ぎでの細部の差（cycle 9850時点・収束途上のため暫定）**：
  - ci10〜12: 序盤最高速 43.5→41.8km/h（わずかに抑制）。
  - ci13〜15（最強塞ぎ hw30）: 新runは先行を57〜61km/hで追走（旧runは37km/h前後で保守的にクロール）。ただし巡航でのtarget超過は 旧3行→新43行（超過量 max 5→13.4km/h）と増、末尾に小さな赤（Normal）のノコギリ。到達可否・保守性は収束変動域で確定不可。

### 所見・結論
- **入力追加は「必要な土台」**：方策が上限速度とモードを直接観測できるようになったが、**発車時の過剰加速（本題）は入力追加単体では解消しない**ことを確認。これは観測の問題ではなく **報酬／上限速度の定義（reactive）の問題**。
- 集計報酬は同budgetで同等以上＝**副作用（収束悪化）なし**。土台として残す価値はある。

### 残課題・次の対策（要検討）
1. **target_speed_no_stop の先読み化**：発車時でも「今70まで加速すると後で先行に追いつく」ことを織り込む（実効所要時間の見積りに先行の減速・停車を先取り、または安全マージン増）。これが発車過剰加速の本丸。
2. **報酬側での是正**：中塞ぎの発車〜巡航で required 近傍まで加速する行を、省エネ観点で軽く減点する正例/負例を追加。
3. delay_recovery は学習データ0件のため mode one-hot 28番目は常時0（無効入力）。遅延回復シナリオのデータ生成が別途必要。
4. 確定比較は新run(30次元)が旧runと同等サイクル（〜25000）に達してから再実施。

## 15. target_speed_no_stop の先読み化（先行の出発遅延に基づく想定停車時間）（2026-07-31）

### 動機（§14の「本丸」への対策・ユーザー提案）
§14で、発車直後の過剰加速は「観測の問題ではなく target_speed_no_stop が発車時に高い（reactive）」ことが原因と判明。これを因果性を保ったまま先読み化する。

### 提案（ユーザー）
- **(A) 先読み**: 先行が前駅（＝自列車の出発駅）を30秒以上遅延して出発している場合、混雑の波及で次駅停車も延びる可能性が高い。よって想定次駅停車時間を **45秒（仮値）** に引き上げて機外停車しないための速度（target）を算出する。
- **(B) 観測**: 先行に近づくと標準30秒を超える延着が観測可能になるので、その観測値を反映して target を算出・評価する。

いずれも「先行の出発遅延」「次駅での観測遅延」という**自列車が観測可能な量**のみを使い、未来の実発車時刻は使わない（§13の因果性を維持）。

### 実装（`environment2.py` `forward_clear_remaining_time` プロパティ）
想定停車時間 `assumed_dwell` を次のように見積もり、`clear = max(0, arrive_time + assumed_dwell − now)` とする。
- 先行が実発車済み（観測）なら `clear=0`（塞ぎ解消）。
- (A) `assumed_dwell = 45秒 if forward_train_delay ≥ 30秒 else 30秒`（定数 `ASSUMED_DELAYED_DWELL_S` / `DELAY_DWELL_THRESHOLD_S`）。
- (B) 観測遅延>0なら `assumed_dwell = max(assumed_dwell, 30 + 観測遅延 + 先読みマージン15秒)`（定数 `OBSERVED_DWELL_LOOKAHEAD_S`）。
  - マージンが無いと `30+観測遅延 = now−arrive` となり `clear=0`（＝今退避）に潰れ、観測中に target が required へ戻ってしまうため必須。
- `forward_clear_remaining_time` は単一プロパティで、RL報酬・apex2 Tester・データ生成器すべてが参照するため一括で波及する。

### 検証（新コード・発車時 target [km/h]）
| シナリオ | 新 | 旧 | 備考 |
|---|--:|--:|---|
| ci4 先0 hw120 | 70.0 | 70.0 | 変化なし（先行遠く塞ぎ無し・正） |
| ci7 先30 hw90 | 70.0 | 70.0 | **変化なし**：45秒想定でも clear+15(137)<定刻残(180)＝先行が定刻前に退避し機外停車リスク無し。過剰加速に見えても実は妥当 |
| ci10 先60 hw60 | 64.3 | 69.1 | −5・道中も低く維持 |
| ci13 先90 hw30 | 57.0 | 60.0 | −3・道中53〜57で持続 |

- Regime B（先90×停60）: 先行が白兎で標準超過停車中、観測遅延13→30に伴い target 39.7→9.2 と低下維持（マージン15でclear=15を確保）。
- **要点**: 本対策は「塞ぎが実際に効く強い遅延（先行遅延60以上）」のみ target を下げ、先行が定刻前に退避する ci7 等は正しく据え置く。発車過剰加速の是正は「入力追加(§14)」ではなく本対策（target定義の先読み化）が担う。

### 波及・残（要データ再生成＋再学習・要相談）
target/clear の**意味が変わる**ため、これらを特徴量に持つモード/評価NN、およびLLM評価ラベルとの整合が崩れる。完全採用には以下が必要：
1. プロンプト更新（`evaluate_csv_with_llm.py` の観測遅延節）：Regime Bで target 自体が観測遅延を反映するようになったため、「target は required に戻っているので観測遅延を優先せよ」の但し書きは陳腐化。target を直接使う形へ改訂。
2. **先行ありデータの再生成＋LLM再評価**（先行なし24,100行は target=required で不変・影響なし）。
3. **モードNN・評価NNの再学習**（53次元・新target値）。
4. **DQN再学習**（新報酬NN＋30次元入力の新target）。現行30次元run（20260731112624）は旧targetのため要再開。
5. 定数（45秒/30秒閾値/15秒マージン）は仮値。データ生成前に確定する。

## 16. 遅延回復モードのプロンプト設計（草案・実装保留／機外停車防止モード完成後に実装）（2026-08-02）
※本節は情報整理と懸念点のまとめ。プロンプトは未変更。機外停車防止モードのパイプライン完了後に実装する。

### 目的（ユーザー整理）
- 駅出発時点で遅延≥30秒 → 制限速度ぎりぎりまで力行し惰行へ（省エネより遅延回復を優先）。
- 勾配活用: 前方勾配（特に下り）で制限速度に到達/維持できるなら惰行を高評価（力行不要）。
- ヒステリシス帯（目安）: 制限速度−3km/hで惰行活用、制限速度−5km/hで再加速、制限速度を超える前にブレーキ。
- ブレーキの勾配考慮: 下り勾配が終わる直前に減速しすぎると平坦部で再加速が必要になり非効率 → 勾配終了を考慮した減速。
- 乗り心地: 既存のノコギリ運転ルール（7秒・反転・合理的理由なし）をそのまま適用。
- 現在速度が「制限速度−5km/h」で再加速（目安）。

### 現行との差分（基準速度の変更）
- 現行 delay_recovery はプレースホルダ（基準=required_speed・通常と同じ・[evaluate_csv_with_llm.py:145-148]）。本設計で基準を **speed_limit（制限速度）付近の回復目標** に変更。
- 加速フェーズの評価が通常モードと**反転**: 通常＝早め惰行が高評価／遅延回復＝制限付近までの力行継続が高評価・早すぎる惰行は減点。
- egoの走行区間は全区間70km/h・制限変化なし（[[ego-segment-route-characteristics]]）＝実質「70km/h付近維持」。プロンプトは speed_limit で一般化。

### 評価基準の骨子案（anti_mid_stopに倣う）
- 加速フェーズ: 制限速度付近までの力行継続＝高評価。早すぎる惰行＝減点（遅延拡大）。
- 巡航フェーズ（帯・目安値）: current≥制限−3で惰行→高評価／下り勾配で惰行が制限に届く・維持→高評価／current≤制限−5で再加速→高評価／制限超過前のブレーキ→高評価、超過は即0.0（安全）。
- ブレーキの勾配考慮: 下り勾配の終了地点が近い局面での過度な減速は減点（平坦部での再加速誘発）。
- 乗り心地: ノコギリ運転ルールそのまま適用。

### 利用可能な素材（current_status）
- current_gradient（現在勾配‰）、next_gradient_info（「Xm先に上り/下り勾配Y‰あり」or「この先目立った勾配なし」）、speed_limit、signal_speed、current_speed、delay、required_speed（比較用）、holding_time/prev_notch/prev_notch_duration（ノコギリ判定）。

### 懸念点・詰めるべき箇所
1. **ヒステリシス帯が狭い（制限−5〜制限−3＝実質2km/h）**: 1.0秒ステップ・70km/h域の制御粒度では精密維持が難しく、頻繁な力行⇔惰行切替＝ノコギリ誘発の恐れ。→ 帯幅見直し（例: 制限−8〜制限）や勾配適応を検討。
2. **ノコギリルールとの緊張（最重要）**: 帯追従は本質的に振動的。平坦/下りは惰行の自然減速が緩く coast 相が7秒以上続き問題ないが、**上り勾配では惰行減速が速く短周期化→ノコギリ減点**。→ 上り勾配では「惰行に移らず力行保持」を許容/高評価する条件を明記する必要。※egoの区間は上り勾配（最大+12.5‰）が下り（最大−6.7‰）より急・多く、実務上はこちらが支配的。
3. **勾配終了距離の特徴量が無い**: 「下り勾配が終わる直前は減速しすぎない」判断には『現勾配が平坦(0‰)に戻るまでの距離』が必要。しかし `_get_next_gradient_info` は next_grade==0 を除外するため「下り勾配が Xm 先で平坦化」をLLMが観測できない。→ 特徴量追加（現勾配の終了距離）orフィルタ緩和が必要。
4. **勾配物理のLLM推論の信頼性**: 「惰行で制限に届くか」「下りでどれだけ増速するか」は物理予測でLLMは定性推論のみ＝不確実。→ Python側で補助値をprecomputeする案（例: 現勾配で惰行した場合の到達速度、推奨再加速/ブレーキ点）を検討。どこまで計算で渡すか要決定（target_speed_no_stopと同じ思想）。
5. **モード判定データの不足**: delay_recovery は学習データ0件で mode NN が出力不可。プロンプト実装後、**遅延出発×先行なしの回復走行データ生成→LLM評価→mode/評価NN再学習が必須**（機外停車防止と同じパイプライン）。
6. **モードのフラッピング**: delay 回復で30秒未満になると mode が delay_recovery→normal に切替。30秒閾値付近で振動の恐れ。→ ヒステリシス（発動30秒・解除15秒等）を検討。
7. **フェーズ境界**: 帯追従（巡航）と駅停車減速（駅手前400m）の切替を明確化。「制限超過前のブレーキ」と「駅停車ブレーキ」を混同しない。
8. **優先順位・遷移**: anti_mid_stop ＞ delay_recovery を維持（塞がれていれば回復不可）。先行がクリアしたら delay_recovery に遷移して回復、を明記。
9. **required_speed の扱い**: 基準を speed_limit に変えるが required_speed は比較・回復必要性判断のため残すか。遅延回復では required も制限付近になり実質同等になる可能性。

### 実装時の段取り（機外停車防止モード完成後）
1. プロンプトに「# 遅延回復モードの評価基準」節を追加（基準=speed_limit・帯・勾配・ノコギリ）。懸念1〜4を確定。
2. （必要なら）勾配終了距離の特徴量追加＋補助precompute。
3. 遅延回復データ生成（遅延出発×先行なし）→LLM評価。
4. mode/評価NN再学習（delay_recovery を実データで学習）→DQN再学習。

### 改訂1（懸念①〜④へのユーザー回答反映・2026-08-02）
**基本方針**: 遅延回復のため、ノコギリでなく制限速度未満なら**力行（加速）を優先・高評価**。惰行は超過回避と勾配活用のときのみ。

**巡航フェーズ 評価基準（改訂）**
- 平坦・上り勾配（超過リスクなし）:
  - 制限速度未満での力行（加速）→ 高評価（ただしノコギリ・制限超過は不可）。
  - 惰行からの**再加速を高評価**とする条件: `current_speed ≤ 制限速度−10km/h` **または** 現在の惰行の継続が7秒経過（＝ノコギリでない／prev_notch_duration≥7相当）。
  - 惰行は「制限速度に到達しそうで超過回避のため」なら高評価。制限−10を下回っても惰行を続けるのは回復機会損失として減点。
  - 制限速度超過は即0.0（超過前のブレーキ/惰行は高評価）。
- 下り勾配（超過リスクあり）:
  - **力行（加速）は不可/減点**（重力で超過の危険）。7秒経過していても下り勾配では加速しない。
  - **制限速度−5km/h付近での惰行を最高評価**（重力で速度維持しつつ超過余裕を確保）。
  - 速度が制限へ近づく→超過前のブレーキを高評価。
- ノコギリ運転ルールはそのまま適用（上記の7秒条件はこれと整合）。

**③ 勾配終了距離をプロンプト入力に追加する**（決定）。「現在の勾配区間が終わるまでの距離」＋「終了後の勾配値（0含む）」。
　用途: 下り勾配の終了直前に減速しすぎない（平坦部での再加速誘発を回避）／下り終了後は平坦・上り基準に戻る。
　実装: `_get_next_gradient_info`/numeric を拡張（next_grade==0 の除外をやめる or 別フィールド新設）。**データ生成前に追加必須**。

**④ Python補助precomputeは今回見送り**。current_gradient＋勾配終了距離のみでLLMが「惰行で超過するか」を判断できるか、生成データ品質で検証する。不十分なら補助値precomputeに戻す。

**確定した数値**: 再加速帯=制限−10km/h ／ 下り惰行の理想=制限−5km/h ／ ノッチ保持閾=7秒。

### 追加の検討事項（2026-08-02）
1. **緩い下り勾配の扱い（新規・要注意）**: 「下りでは加速しない」は"惰行で速度維持/増速する急な下り"を前提とする。ego区間の下りは−2.1‰と−6.7‰で、緩い−2.1‰は走行抵抗＞重力で**惰行すると減速する**。一律適用すると減速しすぎ→回復を損なう。→「超過リスクのある勾配」を勾配値の閾値（例: −5‰以下の急な下りのみ加速禁止）で切るか、生成データで挙動確認。
2. **勾配終了特徴量の正確な定義**: 「現勾配が終わるまでの距離」と「終了後勾配値」。上り→平坦、下り→平坦の両方を表現できること（現実装は次勾配0を除外するため不可）。
3. **実装順序**: 勾配終了特徴量の追加 → プロンプト作成 → 遅延回復データ生成 → LLM評価 → mode/評価NN再学習 → DQN再学習。特徴量はデータ生成前に必須。
4. **発車直後の加速フェーズとの一貫性**: 遅延回復では発車直後も「力行継続＝高評価」で一貫（早め惰行は減点）。
5. （既存）モードフラッピング（delay=30秒付近のヒステリシス）、帯追従（巡航）と駅停車減速（駅手前400m）のフェーズ境界、anti_mid_stop→delay_recovery 遷移、delay_recovery学習データ0件→生成＋mode NN再学習。

### 改訂2（下り勾配しきい値の物理算出と決定・2026-08-02）
遅延回復モードの「下り勾配では加速しない（超過リスク）」を適用する勾配のしきい値を、train.pyと同一物理（走行抵抗 2.39+0.0224v+0.00062v²[kg/t]、勾配抵抗=勾配値[‰]、曲線=0、0.01秒積分）で算出。

**60km/h・惰行・200mの速度変化（抜粋）**
| 勾配 | Δ速度/200m | | 勾配 | Δ速度/200m |
|---:|---:|---|---:|---:|
| 0‰(平坦) | −2.53 | | −6.0‰ | +0.01（境界） |
| −2.1‰ | −1.63 | | −6.7‰ | +0.30 |
| −5.0‰ | −0.40 | | −10.0‰ | +1.65 |

**釣り合い勾配（惰行で加速に転じる境界）は速度依存**（= −走行抵抗(v)）: 50km/h→−5.1‰ / 60km/h→−6.0‰ / 70km/h→−7.0‰。

**ego区間の実態**: 下りは最急でも−6.7‰。70km/h付近では−6.7‰でΔ−0.11（ほぼ維持・微減速）＝暴走加速しない。−2.1‰はΔ−1.76（減速）。ただし惰行の減速は緩む（省エネ価値は有効）。

**決定: 下り勾配のしきい値 = −6‰**（ユーザー決定・安全側マージン）。
- 勾配 < −6‰（−6‰より急な下り）→ **加速リスク下り**扱い: 力行は減点（7秒経過でも不可）、制限速度−5km/h付近での惰行を最高評価、超過前ブレーキ。
- 勾配 ≥ −6‰（−6‰以浅の下り・平坦・上り）→ 通常の帯追従（制限−10 or 惰行7秒経過で再加速＝力行許可）。
- ego区間では−6.7‰のみが該当（境界ぎりぎり）、−2.1‰は非該当（通常帯追従）。
- ※プロンプトには current_gradient[‰] と勾配終了距離（改訂1・③）を渡し、LLMがこのしきい値で判断する。

### 改訂3（遅延回復モードの判定信号の是正・プロンプト＋特徴量実装・2026-08-02）
実装着手時に判明した重要な設計是正。
- **問題**: mode判定条件を「delay≥30」としていたが、`current_delay = max(0, −remaining_time)` は標準運転時間(180s)を
  超過して初めて正になる**遅行指標**で、走行中はほぼ0。ego_delay（出発遅延）は開始クロック t=delay をずらすだけ。
  → 回復運転中の巡航行が delay=0 となり delay_recovery でなく normal と誤ラベルされる（ci1〜3が全区間normalだった原因）。
- **是正**: 判定を **required_speed ≒ speed_limit（制限速度−3km/h以上）** を主信号に変更（先行指標）。required_speed は
  延着時に制限速度で頭打ちになり、ego_delay≥60では**全区間 required=70 を維持**する（区間内では取り戻せない）ことを実測確認。
  delay>0 は補助指標として併用。これはユーザー意図（延着→制限まで力行）を走行中ずっと正しく捉える。
- **実装済み**: プロンプトのmode判定条件を required≒制限に変更。勾配終了距離を next_gradient_info に追加
  （下り/上り勾配が平坦化する距離を表示・`_get_next_gradient_info` の next_grade==0 除外を解消。数値版は元々0を含むためNN影響なし）。
- **データ生成**: `評価用csv_遅延回復_20260802/`（生成器 scratchpad `gen_eval_delay.py`、先行なし・ego_delay{45,60,75,90}で
  required≒制限を担保）。A=回復運転(正例)/B=ちんたら低速(負例)/C=ノコギリ＋急な下り(−6.7‰)の力行(負例)/惰行(正例)。

## 17. run 20260802180403（先読みtarget・30次元）精査とCBTC遵守データ（2026-08-03）

### 精査結果（cycle 21950・遅延回復導入前）
- **Qテーブル**: 発散なし(max|Q|68)。**巡航域の行動間ギャップ0.95**（25次元runの≈0から改善＝巡航のこぎり構造的解消）。貪欲方策も一貫。駅直前ギャップ0.54と低め。
- **のこぎり**: 巡航（開放区間）は全ケース≤5で解消。残るは①ci13の−6.7‰急下り帯のV字振動、②駅手前停止位置の微振動（ci9=101回・全て25km/h未満＝免除相当・マクロ滑らか）。
- **先行接近**: 弱〜中塞ぎ(ci0-11)はCBTC違反0・適切追走・定時到達＝優秀。**最強塞ぎ(ci12-15)でCBTC現示超過78-142回**（即0.0違反）。**ci14/15は先行手前12mまで接近（衝突域<40mに83行）**＝先読みtargetが駅手前でrequiredへ戻り、CBTC信号を無視して先行へ突入。25次元run（206m手前で過保護停止）から過剰接近へ振れた。

### 対策1: CBTC遵守データ（`評価用csv_CBTC遵守_20260803/`・生成器 scratchpad `gen_eval_cbtc_stop.py`）
先行が次駅占有(未発車)する ci12-15 相当シナリオ（delay0_stop45/60/30 × hw30/60）。
- A(comply・正例500): CBTC限界50mで停止・待機（待機231行）＋現示追随ブレーキ。
- B(overspeed・負例500): 現示超過で追走（超過500行・衝突域284）。
- C(creep・負例500): 限界割込み低速接近（割込500行・衝突域322）。
→ 「50mで待機(良) vs 突っ込む(悪)」を densely 学習。

### 遅延回復モードの学習改修（完了）
実装に必要なコード改修は **`reward_features.extract_gradient_info` の1点のみ**だった。新形式
「Xm先で下り/上り勾配Y‰が終わり平坦になる」を `(flag=1, dist, val=0)` にパース追加（環境の数値版は既に0対応）。
その他（MODE_CLASSES_ACTIVE=3・one-hot・train_mode_networkの欠損クラス補完・QNet mode枠）は既存で対応済み。
→ 遅延回復の評価済みデータを train_reward_csv_direct に統合すれば mode/評価NN再学習→DQN再学習が回せる。

## 18. 遅延回復モードが「制限まで加速→即減速」になる問題の診断と対策（2026-08-04）

### 症状（run 20260804013724 cycle 11000 ci3=自遅延60）
モード判定は効いているが、制限速度70km/hまで加速した直後に急減速し、再加速を繰り返すノコギリになる
（t=120で69.2 → t=125で61.9 → t=130で48.4 → 再加速）。

### 診断（プロンプトか、データ不足か → 両方＋環境の設計問題。報酬NNの方向性は正しい）
報酬NNは同一状態でノッチだけ変えると **力行0.7〜0.9 ≫ 惰行0.3〜0.6 ≫ ブレーキ0.0〜0.2** を返しており、
「力行を高評価」という方向は正しく学習できていた。問題は制限速度という「崖」の扱いが未学習だったこと。

**因果連鎖（実測）**
1. プロンプトの遅延回復節に**力行を止める数値基準がなかった**（「制限速度に到達しそうな局面での惰行→高評価」
   と曖昧。再加速側は制限−10/7秒と数値があるのに非対称）。
2. → LLMが68〜69km/hの力行を高評価（学習データ 余裕0-2km/h帯: 力行0.37 / 惰行0.30）。
3. → **制限付近の学習データがほぼ空**: 余裕0-2km/h帯=**5行**、制限超過=**0行**、delay_recovery全1529行の
   **92%が制限−10km/hより下**（60km/h未満）。
4. → 報酬NNが「力行good」を制限まで外挿（69.2km/hで力行0.70 > 惰行0.50）。
5. → DQNが70.1km/hで制限超過 → llm_reward=0.00（即0.0ルール）。
6. → **`forbidden_action` が超過時に力行と惰行の両方を禁止**していたため、**急ブレーキ(2.5km/h/s)しか
   選べない**。惰行(約0.25km/h/s)なら1秒で解消する微小超過が、大減速の引き金になっていた。
7. → 超過復帰のデータが0行＋Q_decel過大評価により、12ステップ連続ブレーキ(69→35km/h)。
8. → 再加速（報酬0.8-0.9）→ 繰り返し＝ノコギリ。

**副次的所見**: モードNNは速度70丁度でnormalへ反転する（69まではdelay_recovery）。delay_recovery学習データの
92%が60km/h未満という偏りが原因で、制限付近データを厚くすれば同時に改善する見込み。

**ユーザー提案（required=制限にびたづきのときだけ適用）の評価**: 論理は正しい（requiredが制限未満なら
その速度で惰行しても遅延しないためnormalで十分）。ただし ci3 は required が全区間70.0に張り付いており
現行条件（≥制限−3）でも提案条件（=制限）でも該当するため、**本症状は改善しない**。モード適用範囲ではなく
適用後の制限付近の振る舞いが原因。モード境界の明確化としては採用価値あり（今回は未実装）。

### 対策（実装済み・ユーザー決定）
1. **環境**: `forbidden_action` を「制限超過時は力行のみ禁止・惰行は許可」に変更（先行追越時は従来どおり
   力行/惰行とも禁止＝安全側を維持）。微小超過を穏やかに解消でき、急ブレーキの強制がなくなる。
2. **プロンプト**（遅延回復モードの評価基準）: 力行停止の数値基準を追加し再加速側と対称化。
   - 理想は「**制限速度−3 〜 制限速度−10 の帯を保つ**」（上端で惰行・下端で再加速）。
   - **制限速度−3km/h以上での力行は超過を招く過剰加速として減点**、同帯での惰行を高評価。
   - **超過してしまった後の復帰は惰行で穏やかに戻すのを高評価、強いブレーキは減点**（落としすぎ・乗り心地）。
   - 物理検証: 帯60↔67km/hは惰行約30秒・力行約15秒でどちらも7秒以上＝ノコギリにならない。
3. **データ**: `評価用csv_遅延回復_制限付近_20260804/`（生成器 scratchpad `gen_eval_nearlimit.py`）
   - A(band・正例): 制限−10〜−3の帯運転。B(overrun・負例): 制限−3以上での力行継続→超過。
   - C(recover): 超過直後の状態を `train.set_states` で人工注入し、**惰行復帰(正例)** と
     **急ブレーキ復帰(負例)** を対比生成（70.8→70.1→69.4…穏やか vs 70.8→68.1→65.3→…51.8）。
   - 正例生成ではランダム探索からブレーキを除外（探索ブレーキが軌跡を壊し帯に到達できないため）。

### 残・次の手順
LLM評価 → train_reward_csv_direct へ統合 → **`python retrain_nns.py`（両NNセット再学習＋整合チェック）**
→ DQN再起動。※環境の forbidden_action を変更したため、DQNは必ず再起動が必要。

## 19. 遅延回復モードの判定をルールベース化（2026-08-05）

### 背景（run 20260804122227 ci3・cycle 12200）
モード分類NNは `required_speed` 以外の無関係な特徴量（holding_time・ノッチ種別など）にも反応し、
条件が成立し続けている巡航区間でも delay_recovery ↔ normal を反転させていた
（ci3の60km/h台: normal 66ステップ / delay_recovery 15ステップ）。normal に落ちると力行の報酬が
0.7→0.6程度に下がり惰行(0.5)との差が縮むため、68.8km/hで惰行に入り64km/hまで落として粘らず、
遅延を60秒中30秒しか回復できなかった。プロンプトのモード定義（required ≥ 制限−3）と実行時の挙動が
一致していないことが根本問題。

### 変更内容（`direct_reward_predictor2._infer_mode`）
優先順位は従来どおり **安全 ＞ anti_mid_stop ＞ delay_recovery ＞ normal**。
1. **anti_mid_stop はモード分類NNの判定を維持**（先行の塞ぎは状態量の組合せで決まるため。recall 99.4%）。
2. NNが anti_mid_stop でない場合、**delay_recovery / normal をルールで決定**する。
   - 条件1: `speed_limit − required_speed ≤ 0.5km/h`（びたづき＝定時到達に制限速度近くの力行が必要）
   - 条件2: `signal_speed ≥ speed_limit − 5km/h`（CBTC現示に余裕＝先行に速度を抑えられていない）
   - 両方成立で delay_recovery、それ以外は normal。

**条件2を入れた理由**: びたづき単独では、先行に接近して現示が下がっている状況まで delay_recovery に
なってしまう。実データでも「びたづきかつLLMがnormal判定」の行は97%が先行あり・63%がCBTC制限下、
逆に delay_recovery 判定行は85%がCBTC余裕だった。
**フェーズ（減速中か否か）は判別軸にならない**ことも確認（delay_recovery行の40%は減速フェーズ）。
一度フェーズガードを実装したが delay_recovery の recall が98%→58%に落ちたため CBTC現示ガードに差し替えた。

### 検証
- **閾値の選択は影響しない**: 制限−0.5〜−3の帯は学習データ全体の1%のみで、「びたづき」と
  プロンプト記載の「≥制限−3」は99%一致する。よってプロンプト文面の変更は不要。
- **新プロンプト評価データ(4,997行)との一致率**: 84.8%（ガードなし80.6% / フェーズガード77.5%）。
  内訳 delay_recovery recall 84.5% / anti_mid_stop 99.4% / normal 66.4%。
  参考: モードNN単体は92.2%だが、これは自身の学習ラベルに対する in-sample の値であり、
  かつ軌跡上での反転（本問題）を捉えない指標である。
- **ci3のモード反転が多数→1回**に激減。60km/h台は100% delay_recovery（変更前は81行中normal 66）。
- **先行ありケース(ci11〜15)で危険な誤適用0件**（先行200m以内 or CBTC制限下での delay_recovery なし）。

### 注意
- 旧プロンプト時代（08-01以前）の学習データは delay_recovery の定義が存在せず、びたづき行13,365件が
  normal とラベルされている。実行時はルールで delay_recovery を与えるため、評価NNの学習時ラベルと
  推論時のモードにズレが残る。新プロンプトで評価したデータ（制限付近データ等）が増えれば解消に向かう。
- **本変更を反映するにはDQNの再起動が必要**（apex2.py は起動時に予測器をロードするため）。
