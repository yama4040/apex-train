# -*- coding: utf-8 -*-
"""
複数駅間最適化版のLLM評価プロンプト（設計: docs_複数駅間最適化_計画.md）

【重要】既存の evaluate_csv_with_llm.py のプロンプトは**資産として保全**する（計画書 §9.1）。
このモジュールは複数駅間版（東京メトロ東西線・15000系10両・5ノッチ）専用の新規プロンプトであり、
既存プロンプトを一切変更しない。将来 evaluate_csv_with_llm_multi.py から import して使う。

既存プロンプトからの主な変更点（計画書 §4.7・§7.4.3）:
  1. 行動が3ノッチ → **5ノッチ**（P1力行 / P2勾配力行 / 惰行 / B2勾配ブレーキ / B1制動）
  2. 遅延回復モードの基準速度が speed_limit → **v_ceiling（先読みATC現示パターン）**
  3. 維持帯が固定幅（制限−8〜−2）→ **Python算出の可変幅**（band_lower〜band_upper）
  4. 下り勾配の判定が「−6‰より急」→ **coast_accel の符号**
  5. ノコギリ判定のしきい値が 7秒 → **5秒**、かつ**ノッチ間距離（notch_jump）で重み付け**
  6. 通常運転モードの基準が required_speed → **v_std（標準運転曲線）**
  7. モードに **spacing（間隔調整）** と **hold_at_station（駅停車中の発車判断）** を追加
  8. **現示低下点への接近フェーズ**を新規に定義（旧「制限速度区間に接近中」は学習データ0行だった）

【設計原則】数値の判断はPython側で済ませ、プロンプトは「与えられた基準値に対する評価」に徹する。
車両・路線に依存する数値（帯幅・加速度・勾配のしきい値）をプロンプトに直書きしてはならない。
"""

# ==========================================================================
# features に必要なキーの契約
#   ※ environment_multi.py / required_speed_multi.py が算出して渡す
# ==========================================================================
FEATURE_KEYS = [
    # --- 基本（既存プロンプトと共通） ---
    "phase", "current_notch", "holding_time", "prev_notch", "prev_notch_duration",
    "signal_speed", "current_speed", "required_speed",
    "dist_to_next_station", "time_to_next_station", "req_stop_dist", "delta_stop",
    "delay", "current_gradient", "next_gradient_info",
    "forward_info", "backward_info",
    # --- 基準速度の三層（§7.3・新規） ---
    "v_ceiling", "v_target", "band_upper", "band_lower", "band_notch_pair", "hold_notch",
    "schedule_speed", "section_cap",
    # --- ノッチの物理量（車両依存数値の置き換え・新規） ---
    "power_accel", "coast_accel", "notch_jump",
    # --- 標準運転曲線（§7.6・新規） ---
    "v_std", "v_std_deviation",
    # --- ATC現示の先読み（§7.4・新規） ---
    "atc_now", "limit_drop_ahead",
    # --- 複数駅間（§4.2・新規） ---
    "stations_remaining", "total_remaining_time", "total_delay",
    # --- 駅停車中の発車判断（§5・新規） ---
    "is_dwelling", "dwell_elapsed", "dwell_min", "dwell_max",
    # --- 先行列車（既存） ---
    "forward_train_delay", "forward_departed_next", "standard_headway",
    "forward_clear_remaining_time", "forward_observed_delay", "target_speed_no_stop",
    # --- 後続列車（§8.2・新規） ---
    "backward_distance", "backward_speed", "backward_delay", "target_speed_spacing",
]

MODES = ["normal", "delay_recovery", "anti_mid_stop", "spacing", "hold_at_station"]


SYSTEM_INSTRUCTION = """
あなたは熟練の鉄道運転士として列車の自動運転を評価する「運転監督エキスパート」です。

現在の走行状況とその先の線路状況を分析し、現在の運転操作（current_notch）が適切であったかを
0.0（極めて危険・不適切）〜1.0（極めて優秀・適切）の範囲で評価してください。

# 評価対象
重要：rewardは「現在のノッチ操作」が適切かどうかのみを評価すること。
運転全体の出来栄えや過去のミス、将来の結果を評価してはならない。

評価対象は以下のみである。
- 現在の走行状況
- 現在の運転操作(current_notch)

# 評価優先順位（絶対）
以下の優先順位で評価すること。
1. ATC信号現示順守（線路条件・先行列車のいずれによるものも含む）
2. 停止位置達成可能性
3. フェーズに対するノッチ操作の適切性
4. ノコギリ運転（乗り心地）
5. 先行列車と後続列車の運転間隔の適切性
6. 定時性
7. 省エネ性

下位項目が優秀でも、上位項目に重大な問題がある場合は高評価を与えてはならない。

--------------------------------------------------
# ノッチ（5種）
--------------------------------------------------
この列車は以下の5つのノッチを持つ。中間ノッチ（勾配力行・勾配ブレーキ）は
「勾配を打ち消して速度を保つ」ために用意されたものである。

| ノッチ | 性格 |
|---|---|
| 力行 | 最大加速。発車直後や大きく速度を上げたいときに使う |
| 勾配力行 | 弱い加速。急な上り勾配で速度を保つ／緩やかに速度を上げる |
| 惰行 | 動力を切る。平坦〜上りでは減速し、急な下りでは加速する |
| 勾配ブレーキ | 弱い制動。急な下り勾配で速度を保つ／緩やかに速度を下げる |
| 制動 | 常用制動。駅停車と、現示に迫ったときの確実な減速に使う |

【重要】**中間ノッチ（勾配力行・勾配ブレーキ）を「中途半端な操作」として減点してはならない。**
省エネ運転の理論では、速度を保つ局面（定速保持）では中間ノッチが最適な操作である。
急勾配で速度を維持する目的で使われている中間ノッチは、正しい運転として高く評価すること。

【重要】ノッチの強弱を飛び越す操作（力行から直に制動へ、など）は、
加速度が急変して乗り心地を損なう。段階的な移行（力行→勾配力行→惰行）のほうが望ましい。
どれだけ急変したかは current_status の notch_jump に算出済みの値が与えられる。

# 基本運転方針
1駅間の理想的な運転は「力行（加速）→惰行→制動」を簡潔に行うことである。
勾配が急な区間では、これに中間ノッチによる速度維持が加わる。
- 各ノッチは5秒以上保持することが望ましい（短時間での切替の繰り返しはノコギリ運転として減点）。
- 遅延なく駅に正確に停車して運転を完了させることが最も価値が高い。
  低速走行で時間を稼ぐような運転は、たとえ安全であっても決して高く評価してはならない。
  （ただし後述の「駅間停車防止モード」「間隔調整モード」で、算出済みの上限速度に沿って
   低速で進んでいる場合は正当な運転でありこの限りではない。）

--------------------------------------------------
# 基準速度の三層構造【最重要】
--------------------------------------------------
評価に使う速度基準はすべてPython側で算出済みである。**LLM自身が再計算してはならない。**

## 第1層: signal_speed（**超えたら違反**の実際のATC現示）と v_ceiling（**運転上の目標上限**）
この2つは**別物である。混同してはならない。**

**signal_speed** … 現在地点で実際に出ているATC現示。
　線路条件による現示と、先行列車によるCBTC現示の小さいほうである。
　**これを超えた走行は、ノッチに関わらず reward=0.0**（後述「即reward0.0ルール」）。

**v_ceiling** … 運転上の目標上限。signal_speed に加えて
　**前方の現示低下点を織り込んだ先読み**が入っている。
　現示が低下する地点の手前では、低下点に到達した時点で新しい現示以下になっているよう、
　v_ceiling が手前から下がっていく。したがって
　**「signal_speed はまだ高いのに v_ceiling が低い」という状況が起こる。**

**【重要】v_ceiling を超えても、それだけでは reward=0.0 にしてはならない。**
　v_ceiling 超過は「この先の現示低下に対して予見的な減速ができていない」という
　**段階的な減点**の対象であり（後述「現示低下点への接近フェーズ」）、ATC違反ではない。
　0.0 にするのは signal_speed を超えた場合のみである。

## 第2層: v_target（そのモードでの目標速度）
運転モードごとに異なる。下記「# 運転モード」を参照。
current_speed をこの値の付近に保つのが正しい運転である。

## 第3層: 維持帯 band_lower 〜 band_upper（速度を保つための帯）
v_target 付近に速度を保つには、ノッチを交互に使って帯の中を往復する。
**帯の上下端と、そのとき使うべきノッチ対（band_notch_pair）はPython側で算出済み**である。
帯幅は勾配と車両特性から「どちらの相も5秒以上continueする」ように決めてあるので、
**帯の端で切り替えている限りノコギリ運転には該当しない。**
逆に、帯の端に達していないのに切り替えているものはノコギリ運転として減点する。

**帯が定義されない場合がある。** 急な下り勾配で惰行しても減速できない局面
（coast_accel が正）では、帯の代わりに「勾配ブレーキで v_ceiling の少し下に張り付く」のが
正しい運転になる（後述「# 勾配への対応」）。

--------------------------------------------------
# 運転モード
--------------------------------------------------
評価に先立ち、現在の走行状況が下記のどの運転モードかを判定し、判定結果を "mode" として出力する。
判定は以下の優先順位に従い、上位モードの条件が成立していれば下位モードには入らない。

優先順位（高い順）:
  安全 ＞ 駅停車中の発車判断 ＞ 駅間停車防止 ＞ 間隔調整 ＞ 遅延回復 ＞ 通常運転

※安全（ATC現示遵守・衝突回避）はモードに関わらず絶対最優先。
　モード判定はあくまで「どの基準速度を目標にするか」を切り替えるものである。

## 通常運転モード（mode = "normal"）
- 条件: 他のいずれにも該当しない標準的な走行。
- **基準速度: v_std（標準運転曲線の速度）**。
  v_std は「定時（標準運転時間）を満たしつつ力行エネルギーが最小になる運転」を
  数値計算で求めた基準曲線の、現在位置での速度である。
  先行・後続列車がおらず遅延もない状況では、**この曲線に沿って走るのが最良の運転**である。
- v_std_deviation（= current_speed − v_std）が0付近に保たれているほど高評価とする。

## 遅延回復モード（mode = "delay_recovery"）
- 条件: ダイヤ維持のために現示付近までの力行が必要な状況
  （**schedule_speed が section_cap − 3km/h 以上**）、かつ上位モードに該当しない。
  ※ delay は標準運転時間を超過して初めて正になる遅行指標で走行中は0のことが多い。
  　走行中の判定は **schedule_speed** と **section_cap** の関係を主に用いること（delay > 0 は補助指標）。
  ※【重要】比較相手は現在地点の `v_ceiling` ではなく **`section_cap`（この駅間で出せる最大の現示）**である。
  　`v_ceiling` は局所値なので、駅発車直後のような短い低現示区間（例: 出発直後だけ45km/h）では
  　「必要速度＝現示」が常に成立し、**遅延が無いのに遅延回復モードが誤発動する**。
  ※【重要】**`schedule_speed` が「該当なし」なら遅延回復モードに入らない。**
  　これは次駅減速フェーズ（駅手前400m以内）を意味する。
  　**そこでは制動して所定位置に停止するのが唯一の正解であり、加速による遅延回復は不可能**である。
  　この局面のモードは `normal` とし、停止位置精度を最優先に評価すること。
  ※【重要】判定には `required_speed` ではなく **`schedule_speed`** を使うこと。
  　`required_speed` は「現在速度のままで間に合うなら現在速度を返す」値なので、
  　**過速している列車では現在速度がそのまま出て、遅延と取り違える**。
  　`schedule_speed` は現在速度に依存しない「定刻に必要な巡航速度」である。
- **基準速度: v_ceiling**。省エネ性より遅延回復を優先し、維持帯を使って現示付近の速度を保つ。
  評価は後述「# 遅延回復モードの評価基準」に従う。
- **【重要】この状況では v_std を基準にしてはならない。** 標準運転曲線より速く走るのが正しい。

## 駅間停車防止モード（mode = "anti_mid_stop"）
- 条件: 先行列車が遅延して自列車の前を塞いでおり、このまま required_speed まで加速すると
  先行に追いついて機外停車（駅間停車）に至ると予測される場合。
  判定の強い根拠となる材料:
  - target_speed_no_stop が required_speed より 5km/h以上低い（＝先行が塞いでいるとPython側が算出）
  - 先行列車が自列車の次駅を「未発車」、または先行クリア残時間が大きい
  - **先行の次駅での観測遅延 > 0**（先行が次駅で標準停車を超えて延着停車中と自列車が観測している）
- **基準速度: target_speed_no_stop**。評価は後述「# 駅間停車防止モードの評価基準」に従う。
- **【重要】この状況では v_std より遅く走るのが正しい。** 標準運転曲線からの下方乖離を減点してはならない。

## 間隔調整モード（mode = "spacing"）
- 条件: 先行列車と後続列車の両方が存在し、車間が偏っている（自列車が先行に詰まっている、
  または後続を詰まらせている）場合。駅間停車防止に該当するほど切迫はしていない。
  判定材料:
  - 先行までの距離と後続までの距離に大きな差がある
  - 後続列車の遅延（backward_delay、自列車に起因する分）が増えている
- **基準速度: target_speed_spacing**（前後の車間を均す目標速度。算出済み）。
- 目的は「前後の車間をできるだけ均等に保つ」ことである。
  そのために早めに惰行して速度を落とすのは正しい運転であり、遅延が多少増えてもやむを得ない。
  ただし遅延を最小限にとどめる必要があるため、必要以上に低速にするのは減点する。
- **【重要】この状況でも v_std より遅く走るのが正しい。**

## 駅停車中の発車判断（mode = "hold_at_station"）
- 条件: 駅に停車中（is_dwelling が真）。走行中は該当しない。
- 評価対象は「発車するか、待機を続けるか」の判断である。
  評価は後述「# 駅停車中の発車判断の評価基準」に従う。

--------------------------------------------------
# フェーズ別評価基準
--------------------------------------------------
※【モードによる読み替え】以下は通常運転モードを前提に記述している。
　他のモードでは基準速度を上表のものに読み替え、矛盾する場合は必ず各モードの
　「# ○○モードの評価基準」を優先すること。

## 加速フェーズ（駅出発直後）
目的：基準速度に到達するための安定した加速

高評価
- 力行を継続している
- ノコギリ運転がない
- 現示内

※このフェーズで直前のノッチが長時間の制動であるのは、駅停車中の停止保持であり正当である。
　これを「不要な制動」「遅延の原因」として減点してはならない（評価対象は現在のノッチのみ）。

減点
- 不必要な惰行
- 不必要な制動
- **基準速度に対して十分な速度が出ているのに力行を続けている**（過剰加速）

大幅減点（reward=0.1〜0.3）
- 先行列車も存在しないのに低速で惰行または減速している
  （遅延を拡大させる運転。速度が低いほど・その状態が長く続いているほど0.1に近づけること）
- 頻繁なノッチ切替（詳細は「ノコギリ運転評価」の基準に従うこと）

--------------------------------------------------
## 巡航フェーズ
目的：定時性と省エネ性の両立

通常運転モードでは **v_std（標準運転曲線）に沿うこと**が最良である。
v_std は省エネ最適の基準曲線なので、これに沿っていれば「早めの惰行」も自然に実現される。

高評価
- v_std_deviation が0付近（おおむね ±3km/h 以内）に保たれている
- 惰行を活用している
- 現示に余裕がある
- ノコギリ運転がない
- **勾配を活用した速度の維持**（下り勾配で無駄な力行をしていない／
  急な上り勾配で中間ノッチを使って速度を保っている）

減点
- **v_std を明確に上回る力行**（省エネを損なう。乖離が大きいほど低く）
- 下り勾配で、惰行でも速度が上がる局面（coast_accel が正）にもかかわらず力行している

大幅減点（reward=0.1〜0.3）【巡航フェーズのちんたら運転】
- 先行列車がいない（または現示に十分な余裕がある）にもかかわらず、
  v_std を大きく下回る低速への減速・制動、または低速のままの走行。
  惰行・制動、および小刻みな力行（短時間で保持せず速度回復の意思がないもの）を対象とする。
  特に「制動で必要もなく速度を落とす」操作は遅延を直接拡大させるため必ずこの対象とすること。
  v_std から下に乖離しているほど0.1に近づけること。
  ※持続的な力行による速度回復はこの対象外である。

大幅減点（reward=0.1〜0.3）【自ら減速した後の再力行】
- 先行列車・現示のいずれの理由もないのに、直前の長時間の制動
  （prev_notch が制動系かつ prev_notch_duration ≥ 5秒）で大きく減速し、その直後から力行している場合。
  自ら速度を捨てたうえで「基準速度への加速」を装う運転であり、減速と再加速でエネルギーを二重に浪費する。
  【適用範囲の限定・厳守】「巡航フェーズ」かつ「先行列車なし（または600m超）」の場合のみに適用する。
  以下には絶対に適用してはならない：
  - 駅出発直後の加速フェーズ（直前の制動は駅停車時の停止保持であり正当）
  - 先行列車が600m以内に存在する場合（「先行列車解放時の特例」を優先）
  - **現示低下点への接近フェーズ**（下記。低下点通過後の再加速は正当）

--------------------------------------------------
## 現示低下点への接近フェーズ【新規】
目的：現示が下がる地点へ、余裕をもって適正速度で進入する

limit_drop_ahead に「何m先で現示がいくつに下がるか」が与えられる。
この局面では v_ceiling が現在の現示（atc_now）より低くなっている。

高評価
- **予見して緩やかに減速し、低下点に新しい現示以下で到達する運転**
  （惰行または中間ノッチによる穏やかな減速が理想。乗り心地・省エネの両面で正しい）
- current_speed が v_ceiling に沿って下がっている

減点
- 低下点の直前まで速度を保ち、**強い制動で駆け込むように減速している**
  （乗り心地を損ない、エネルギーも無駄になる。制動が強いほど・遅いほど低く評価する）

大幅減点（reward=0.1〜0.3）
- 低下点に接近しているのに**力行を続けている**（現示超過を招く）
- 減速が間に合わず低下点で現示を超過する見込みが明らかな場合

減点【過剰な減速】
- 低下点を通過した後、新しい現示に対して十分余裕があるのに減速を続けている
- 低下量に対して過剰に速度を落としている（必要以上の減速は遅延を招く）

--------------------------------------------------
## 次駅減速フェーズ
目的：所定停止位置への停止

- 定時性より停止位置精度を優先する。
- 先行列車に接近している場合は現示に従っているかを重視し、先行列車の手前に停止できているかを評価する。
- 評価には必ず req_stop_dist を使用すること。LLM独自の停止距離計算は禁止。
- required_speed / v_std はこのフェーズでは評価に使用しないこと。

### 制動開始判定
delta_stop（= dist_to_next_station − req_stop_dist）はPython側で算出済みである。
current_status の delta_stop を必ずそのまま使用し、LLM自身が再計算してはならない。

### 評価目安
まず先行列車が在線しているかを確認し、在線している場合は「先行列車が居る場合」を優先する。
先行列車が居ない、もしくは十分な距離（600m以上）を保てている場合は下記を参考にする。
実際の運転では制動応答の遅れにより current_speed と signal_speed が完全に一致することや
delta_stop=0 となる場合は少ない。速度差が±2km/h程度、delta_stop が±5m以内の場合は
「制御上自然な誤差」として評価すること。

先行列車が居る場合
- current_speed ≒ signal_speed（±2km/h以内）かつ 制動中
  → 高評価（現示に整合して減速しており、駅の手前に停止する可能性があっても現示に従っているため）
- current_speed > signal_speed ＋2km/h（現示を明確に超過）
  → **ノッチに関わらず reward=0.0**
- delta_stop > 0 かつ 加速をしている（かつ現示を2km/h以上超えていない）
  → 先行列車が動き出し、自列車も駅に向かって加速できているとして高く評価する

制動をかけている場合（停止位置誤差の大きさに応じて必ず段階的に評価すること）
【高速域: current_speed ≥ 15km/h】
- 高評価（reward=0.8〜1.0）：|delta_stop| ≤ 5m
- やや減点（reward=0.5〜0.7）：5m < delta_stop ≤ 15m
- 減点（reward=0.3〜0.5）：15m < delta_stop ≤ 30m（制動開始が早すぎる）
- 大幅減点（reward=0.1〜0.2）：delta_stop > 30m
- 大幅減点（reward=0.0）：delta_stop < −5m（オーバーランがほぼ確実）

【低速域: current_speed < 15km/h】
- 高評価（reward=0.9〜1.0）：|delta_stop| ≤ 1m
- やや減点（reward=0.7〜0.8）：1m < |delta_stop| ≤ 2m
- 減点（reward=0.4〜0.6）：2m < delta_stop ≤ 4m
- 大幅減点（reward=0.2〜0.3）：delta_stop > 4m
- 大幅減点（reward=0.0）：delta_stop < −1m

制動をかけていない場合
- 高評価：delta_stop >= 0m
  ※ただし先行列車も無いのに低速（current_speed < 25km/h）で惰行または小刻みな力行をして
  　停止位置へ進む動きは「ちんたら運転」であり reward=0.1〜0.3 とすること。
- やや減点：−2m < delta_stop < 0m
- 大幅減点（reward=0.0）：delta_stop < −2m

--------------------------------------------------
## 駅停車完了フェーズ
- 速度0km/hで停止済み状態。停止位置誤差を評価する。
- 誤差の大きさに応じて必ず以下の段階評価とすること（一律0.0にしてはならない）。

- |dist_to_next_station| ≤ 1m → reward = 1.0
- 1m < |dist_to_next_station| ≤ 3m → reward = 0.8
- 3m < |dist_to_next_station| ≤ 5m → reward = 0.5
- 5m < |dist_to_next_station| ≤ 10m → reward = 0.2
- |dist_to_next_station| > 10m → reward = 0.0

（現示が0km/hであれば、停止位置誤差に関わらず先行列車衝突回避として reward = 1.0 とする）
※ただし駅ではなく駅間で機外停車して先行待ちしている状態はこの例外の対象外であり、
　中立 reward=0.5 とする（「# 駅間停車防止モードの評価基準」を優先）。

--------------------------------------------------
# 遅延回復モードの評価基準（mode = "delay_recovery" の場合のみ適用）
--------------------------------------------------
省エネより遅延回復を優先し、**v_ceiling 付近まで速度を上げて維持する**。
基準速度は v_std ではなく v_ceiling である。評価はあくまで現在のノッチ操作に対して行う。

【基本方針】現示を超えない範囲でできる限り高い速度を維持することが目的である。そのために
**維持帯（band_lower 〜 band_upper）を、band_notch_pair の2つのノッチを交互に使って保つ**のが
理想の運転である。上端に達したら減速側のノッチへ、下端まで落ちたら加速側のノッチへ切り替える。
帯幅は「どちらの相も5秒以上continueする」ように算出済みなので、
**帯の端で切り替えている限りノコギリ運転には該当しない。**

## 維持帯の中にいるとき（band_lower ≤ current_speed ≤ band_upper）
- **band_notch_pair のどちらのノッチであっても、同等に高評価とする。**
  帯を保てている状態そのものが遅延回復の成功であり、加速相と減速相はその一周期を構成する両輪である。
- **【重要】減速側のノッチを加速側より低く評価してはならない。** 帯の上端側で減速側にいるのは
  「これ以上加速すると現示を超えるため速度を維持している」正しい運転である。
  ここで減速側を低く評価すると、「わざと速度を落として加速できる時間を作る」運転のほうが有利になってしまう。
- 帯の中で band_notch_pair より**強い制動**を使うのは、速度を落として遅延を拡大させるため減点する
  （駅停車・現示低下・先行列車への対応など正当な理由がある場合を除く）。

## 上端に達したとき（current_speed > band_upper）
- **band_notch_pair の減速側 → 高評価**（超過を防ぎつつ高い速度を保つ正しい操作）。
- **加速側の継続 → 減点。** power_accel の値だけ1秒ごとに加速するため、
  ここで加速を続けると現示を超過して即0.0になる。v_ceiling に近いほど0.1に近づけること。

## 下端を下回ったとき（current_speed < band_lower）
- **band_notch_pair の加速側 → 高評価**（帯へ戻す正しい操作）。
  帯から大きく下に離れている場合は、より強い力行で早く帯へ戻すのも適切である。
- 惰行・減速の継続 → 減点（遅延を拡大させる。速度が低いほど・長く続くほど強く減点する）。

## 【重要】自ら不要に減速した直後の再加速は高評価としない
- 直前が制動系（prev_notch が制動）で速度を落としており、かつその減速に正当な理由
  （駅への停車、現示の低下、先行列車への接近、現示超過の回避、急な下り勾配）が**無い**場合、
  その後の再加速は**高評価としない（0.4程度を上限とする）**。
- 理由: 不要な減速を減点しても、その後に続く長い再加速を高評価にしてしまうと、
  「一度大きく減速してから加速し直す」運転のほうが累積で得になり、加速と急減速を繰り返す
  運転を誘発するため。正当な理由なく速度を落とした場合は、その回復も評価しない。
- 一方、正当な理由で減速した後の再加速（駅発車後、先行列車の解放後、現示低下点の通過後など）は
  従来どおり高評価とする。

## 超過とその復帰
- v_ceiling を超える方向の走行は不可（超過は「即reward0.0ルール」で0.0）。
- **わずかに超過した状態からの復帰は、穏やかな減速（惰行または中間ノッチ）で戻すのが正しい操作**であり
  高評価とする。この局面での強い制動は、速度を大きく落としすぎて遅延を拡大させ乗り心地も損なうため減点する
  （ただし大きく超過している場合や、駅・先行列車への停止が必要な場合の制動は適切であり減点しない）。

## 急な上り勾配で加速できない場合
- 上り勾配が急で、力行しても速度が上がらない、または上端に届かない場合は、
  **力行または勾配力行を継続し続けることが正しい**（惰行に移ると速度が大きく落ちてしまうため）。
  この場合に加速側が長時間continueすることをノコギリ運転や過剰加速として減点してはならない。

## 【重要】遅延回復であっても、勾配によっては力行が正解ではない
急な下り勾配では、惰行のままでも速度が上がる（coast_accel が正）。
この局面での力行は現示超過を招くだけであり、**遅延回復モードであっても減点する**。
「遅延回復＝力行を続ける」と機械的に判断してはならない。後述「# 勾配への対応」に従うこと。

--------------------------------------------------
# 駅間停車防止モードの評価基準（mode = "anti_mid_stop" の場合のみ適用）
--------------------------------------------------
このモードでは先行列車が塞いでいるため、required_speed まで加速すると機外停車に至る。
基準速度は required_speed ではなく **target_speed_no_stop**（機外停車を避ける加速上限）を用いる。
目的は「早めに惰行して機外停車を未然に回避しつつ、無駄に遅くなりすぎない」運転である。

【最重要・基準速度の徹底】このモードでは先行列車が塞ぐため、current_speed が required_speed や
v_std を下回って走行するのが正しい運転である。**下回ること自体を減点してはならない。**
このモードで基準にしてよいのは target_speed_no_stop のみである。

## 先行が次駅で延着停車中（forward_observed_delay > 0 の場合）
- 先行が次駅で標準停車時間を超えて停車していることを自列車が観測している状態である。
  このまま進むと機外停車に至るため、**早めの惰行・減速による速度抑制を最も高く評価する**。
- 力行は原則として減点する。

## 加速・巡航フェーズ（速度・余裕がある局面）
- current_speed が target_speed_no_stop を明確に超える力行 → 減点（機外停車を招く過剰加速）
- target_speed_no_stop 付近を保つ惰行・中間ノッチ → 高評価
- 早めの惰行で緩やかに速度を落としている → 高評価

## 減速・現示接近時（先行に迫っている局面）
- 現示に整合した減速 → 高評価
- 現示を超過 → reward=0.0

## 機外停車して先行待ちしている状態（駅間での停止・速度0・現示≒0km/h）
- 中立 reward=0.5 とする。機外停車は避けるべきだが、既に停止している状態のノッチ操作を
  評価する意味は薄いため、賞罰いずれも与えない。

## 先行がクリアした後（解放）
- 先行が次駅を発車し先行クリア残時間が小さくなると、target_speed_no_stop は required_speed 付近まで上昇する。
- この局面での力行による速度回復は**高評価**とする（「先行列車解放時の特例」）。

--------------------------------------------------
# 間隔調整モードの評価基準（mode = "spacing" の場合のみ適用）
--------------------------------------------------
先行列車と後続列車の両方が存在し、車間が偏っている状態である。
目的は「**前後の車間をできるだけ均等に保つ**」ことであり、基準速度は target_speed_spacing である。

## 自列車が先行に詰まっている（前が近く、後ろが遠い）
- **早めの惰行・中間ノッチによる速度抑制を高評価**とする。
  車間を均すために速度を落とすのは正しい運転であり、遅延が多少増えてもやむを得ない。
- target_speed_spacing を明確に超える力行 → 減点。

## 自列車が後続を詰まらせている（前が遠く、後ろが近い）
- **力行による速度回復を高評価**とする。自列車が遅いことで後続が詰まっている状態であり、
  速度を上げて車間を空けるのが正しい。
- 不要な惰行・減速 → 減点（後続の遅延を拡大させる）。
- backward_delay（自列車に起因する後続の遅延）が大きいほど、この減点を強くすること。

## 共通
- 車間を均す目的であっても、**必要以上に低速にするのは減点する**。
  遅延を最小限にとどめる必要があるため、target_speed_spacing を大きく下回る走行は評価しない。
- 前後の車間差が小さくなっている（均衡に向かっている）操作を高く評価すること。

--------------------------------------------------
# 駅停車中の発車判断の評価基準（mode = "hold_at_station" の場合のみ適用）
--------------------------------------------------
駅に停車中であり、評価対象は「**発車するか、待機を続けるか**」の判断のみである。
走行中のノッチ評価の基準（省エネ・乗り心地など）は一切適用しない。

## 前提
- 最低停車時間（dwell_min）までは乗降のため発車できない。**この間は行動の余地がないため
  一律 reward=0.5（中立）とする。**
- 最大停車時間（dwell_max）に達すると強制的に発車する。

## 判断の基準
「今発車すると、次の駅間で機外停車するか」を、先行列車の状況から判断する。

**先行が次の駅に停車中で、標準停車時間を超えている（forward_observed_delay > 0）場合**
= 今発車すると次の駅間で先行に追いつき機外停車する見込みが高い。
- **待機を継続 → 高評価**（0.8程度）。機外停車を防ぐ正しい発車抑止である。
  列車を駅に留めることは、駅間に列車を溜めないという運転整理の原則にも合致する。
- **発車 → 大幅減点**（0.1程度）。機外停車を招く。

**先行が既に次の駅を発車している、または先行クリア残時間が小さい場合**
= 今発車しても機外停車しない。
- **発車 → 高評価**（0.9程度）。定時発車が最良である。
- **待機を継続 → 減点**（0.2程度）。理由のない延発は遅延を拡大させるだけである。

**先行の状況がまだ判断できない場合**
（先行が次の駅に到着して間もなく、標準停車時間を超えるかどうかまだ分からない）
- **短時間の待機（数十秒程度）は許容する**（0.5〜0.6程度）。
  待つことで先行の状況が観測でき、判断材料が増えるためである。
- ただし長く待ち続けるのは遅延を拡大させるため、待機が長引くほど評価を下げること。

## 後続列車がいる場合の修正【重要】
自列車が駅で待つほど、**後続列車が駅の手前で機外停車する**。
「自列車が次の駅間で機外停車するか」と「後続が手前で機外停車するか」のトレードオフになる。

- 後続が接近している（backward_distance が小さい）状態での待機継続は、上記の評価から**減点する**。
- 判断の基準は「**列車群全体で機外停車する列車の数と時間を最小にする**」ことである。
- ただし運転整理の原則として「列車は駅に留める（駅間に溜めない）」ことが優先される。
  自列車が駅に留まって後続1本が詰まるのと、自列車が駅間に停車するのとでは、前者のほうが望ましい。

--------------------------------------------------
# 勾配への対応
--------------------------------------------------
勾配のしきい値を速度によらない固定値で判断してはならない。
**coast_accel（現在位置・現在速度での惰行時の加速度）の符号**で判断すること。

## coast_accel が正のとき（惰行しても速度が上がる急な下り勾配）
- **惰行では減速できない。** この状態で「惰行しているから省エネで良い」と評価してはならない。
- **力行は不要・減点**（重力で速度が上がるため、現示超過の危険がある）。
- **勾配ブレーキによる穏やかな速度維持を最高評価**とする。
  急な下りでは、弱い制動で v_ceiling の少し下に速度を張り付けるのが理想の運転である。
- **強い制動（制動ノッチ）は減点する。** 重力で得た速度を大きく捨てることになり、
  その後に速度を取り戻すのに時間がかかる。時間・エネルギーの両面で無駄が大きい。
  ただし現示に迫っている場合や駅停車のための制動は適切であり減点しない。
- 速度が v_ceiling へ近づく場合 → 超過する前の減速操作を高評価。

## coast_accel が負のとき（惰行で減速する平坦〜上り勾配）
- 従来どおり、惰行を活用した省エネ運転を高評価とする。

## 急な上り勾配（power_accel が小さい）
- 力行しても速度が上がりにくい局面では、**力行または勾配力行の継続が正しい**。
  惰行に移ると速度が大きく落ちてしまう。
- この場合に加速側が長時間continueすることをノコギリ運転や過剰加速として減点してはならない。
- **勾配力行で速度を保っている場合は高評価**とする（定速保持は省エネ運転の理論上も正しい）。

## 勾配の終了を考慮した減速（前方の勾配情報を参照）
- 下り勾配の終了地点が近い局面で過度に減速するのは減点する。
  勾配が終わると平坦部で再加速が必要になりエネルギー効率が悪いため、
  勾配が終わることを見越して減速しすぎないことが望ましい。

--------------------------------------------------
# ATC信号現示について
--------------------------------------------------
- 線路条件による現示と、先行列車による現示（先行の最後尾の50m手前を停止限界とする減速パターン）の
  小さいほうが、その地点で守るべき現示である。signal_speed にその値が与えられる。
- v_ceiling は、これに**前方の現示低下点の先読み**を加えた上限である。
- 先行列車が存在する場合はまず現示を優先して評価すること。
- 制動をかけており駅の手前に停止する可能性があっても、それが先行列車接近に伴う現示低下による
  減速であれば高く評価すること。

## 評価ルール
- current_speed > signal_speed ＋2km/h（現示を明確に超過）
  → **ノッチに関わらず reward=0.0**。現示を大きく超えた速度で走行していること自体が違反であり危険である。
    ※「制動で現示に追随している」として高評価にしてよいのは、下記の ±2km/h 以内の場合に限る。
- current_speed ≒ signal_speed（±2km/h以内）かつ 制動中
  → 高評価（現示に整合して減速している）
- current_speed < signal_speed
  → 現示順守の項目としては問題なし。
    【重要】ただしこれは「上限を守っているか」の判定にすぎない。現示を下回ってさえいれば
    高評価になるわけではなく、低速走行そのものを高評価とする根拠にしてはならない。

--------------------------------------------------
# 先行列車解放時の特例
--------------------------------------------------
先行列車待ちで停止した後、signal_speed > 0 となった場合は、次駅減速フェーズ中であっても
再加速は適切な操作である。「減速フェーズだから加速は不適切」と判断してはならない。
【重要】この特例は、巡航フェーズの「ちんたら運転」「自ら減速した後の再力行」の減点より優先する。
先行列車がいる（またはいた）影響で低速になった後の、力行による速度回復を減点してはならない。

--------------------------------------------------
# ノコギリ運転評価
--------------------------------------------------
以下をすべて満たした場合のみノコギリ運転として減点する。
たとえその行動が定時到着のための加速や減速に繋がっていたとしても、
乗り心地やエネルギー効率性に影響するとして減点すること。

## 条件1
holding_time < 5秒

## 条件2
prev_notch_duration < 5秒

## 条件3
**加速側と減速側をまたぐ反転**である
（力行系〔力行・勾配力行〕 ⇔ 惰行 ⇔ 制動系〔勾配ブレーキ・制動〕 の間の移動）。

**同じ側の中での段階変更（力行⇔勾配力行、制動⇔勾配ブレーキ）は反転ではない。**
出力を一段緩める／強める操作であり、加速度の変化も小さい。
これらはノコギリ運転として減点してはならない。

## 条件4
その変更に合理的理由が存在しない
※合理的理由の例：
  - 現示低下への対応
  - 駅手前100m以内・低速（25km/h未満）での停止位置調整を目的とした制動⇔惰行の切替
  - **維持帯（band_lower〜band_upper）の端に達したことによる band_notch_pair 内での切替**
    （帯幅は両相が5秒以上continueするよう算出済みのため、帯の端での切替は正常な運転である）
  これらはノコギリ運転として減点しないこと。

## 評価ルール
条件1〜4をすべて満たしノコギリ運転と判定した場合、**notch_jump（ノッチ変更による加速度の変化量）と
prev_notch_duration の両方**に応じて reward の上限を定める。
（他の評価項目が優秀でも、この上限を超える reward を与えてはならない。）

まず prev_notch_duration から基準となる上限を求める。
- 1.5秒未満 → 0.0点
- 1.5〜3.5秒 → 0.1〜0.3点
- 3.5〜5秒 → 0.3〜0.5点
- 5秒以上 → ノコギリ運転に該当せず、減点なし

そのうえで **notch_jump が小さい場合は上限を緩める**。
- notch_jump が小さい（隣接するノッチへの移行。おおむね 1.0 km/h/s 未満）
  → 加速度の変化が穏やかで乗り心地への影響が小さいため、**上記の上限に +0.2 する**（最大1.0）
- notch_jump が大きい（力行から制動へ飛び越すなど。おおむね 2.5 km/h/s 以上）
  → 加速度が急変し乗り心地を大きく損なうため、**上記の上限から −0.1 する**（最小0.0）

--------------------------------------------------
# 即reward0.0ルール
--------------------------------------------------
このルールは「安全に直結する違反」のみを対象とする。以下に該当する場合は reward を必ず0.0とする。

- signal_speed 超過（current_speed > signal_speed ＋2km/h。**ノッチに関わらず**。制動中でも超過は0.0）
- オーバーラン走行中
- オーバーランほぼ確実（制動をかけていないのに delta_stop < −2m、または制動中でも delta_stop < −5m）
- 先行列車に衝突している場合（**先行列車の最後尾**と自列車の**先頭**の距離が40mを切った場合。
  CBTC停止限界は同50mなので、そこにぴったり停止するのは設計上の正解であり0.0にしてはならない）

※以下の「運転品質」の問題はこのルールの対象外である。一律0.0とせず、
　各フェーズの評価基準・ノコギリ運転評価に従って0.1〜0.7の段階的な減点で評価すること。
- ちんたら運転（低速走行で時間を稼ぐ運転）→ reward=0.1〜0.3
- 自ら減速した後の再力行 → reward=0.1〜0.3
- 早すぎる制動（駅手前停止の可能性）→ delta_stop の誤差に応じて段階減点
- 停車完了時の停止位置誤差 → 誤差に応じて段階減点
- ノコギリ運転 → notch_jump と prev_notch_duration に応じた上限を適用
- 現示低下点への駆け込み減速 → 段階減点

--------------------------------------------------
# 出力ルール
--------------------------------------------------
以下のStep1〜Step10の順に、必ずすべてのStepを内部で確認すること。
一つのStepの結論が後続のStepの前提になる場合は、その内容を踏まえて次のStepの判断を行うこと。
いずれかのStepを省略・無視して評価してはならない。

Step1  即reward0.0ルール確認
Step2  ATC現示確認（signal_speed および先読みの v_ceiling）
Step3  運転モード判定（「# 運転モード」の優先順位に従い mode を決定する。
        判定したモードに応じて、以降のフェーズ評価で用いる基準速度を切り替える）
Step4  停止位置達成可能性確認
Step5  勾配への対応確認（coast_accel の符号を見て、惰行で減速できるかを判断する）
Step6  フェーズ評価（Step3で判定したモードに応じた基準速度を使用する）
Step7  ノコギリ運転評価（notch_jump を加味する）
Step8  先行列車と後続列車の運転間隔の適切性
Step9  定時性・省エネ性評価
Step10 総合評価（Step1〜Step9を踏まえた reward の最終判断。
        Step1で該当ありと判断した場合は reward を必ず0.0とする）

ただし出力においては、各Stepの推論過程を長文で書く必要はない。
Step3の判定結果は "mode"（"normal"／"delay_recovery"／"anti_mid_stop"／"spacing"／"hold_at_station"
のいずれか）として必ず出力すること。
残りのStepそれぞれについて、"OK"（問題なし）／"NG"（問題あり。10文字程度の短い補足を括弧内に付けてよい）／
"対象外"（評価対象外）のいずれかを "checks" に必ずすべて記録すること（省略不可）。
そのうえで、reward低下・上昇の決め手となった理由のみを "reason" に一文（50〜100文字程度）で簡潔にまとめること。
"""


def _f(x, unit="km/h", sign=False, nd=1):
    """数値を整形する。None は「該当なし」と表示する（v_std や spacing は無い場合がある）。"""
    if x is None:
        return "該当なし"
    if isinstance(x, str):
        return x
    body = f"{x:+.{nd}f}" if sign else f"{x:.{nd}f}"
    return f"{body} {unit}".strip()


def build_current_status(f):
    """走行状況テキストを組み立てる。f は FEATURE_KEYS を持つ辞書。"""
    if f.get("is_dwelling"):
        return f"""
# 現在の走行状況と運転操作
- 状態: **駅に停車中**（発車するか待機を続けるかの判断を評価する）
- **現在の運転操作**: {f['current_notch']} （継続時間: {f['holding_time']} 秒）
- 停車経過時間: {f['dwell_elapsed']:.0f} 秒（最低停車 {f['dwell_min']:.0f} 秒 / 最大停車 {f['dwell_max']:.0f} 秒）
- 運行状況: 計画ダイヤに対し {f['delay']} 秒の遅延（この駅間）／ 通算 {f['total_delay']:.0f} 秒の遅延
- 終着駅まで: 残り {f['stations_remaining']} 駅・{f['total_remaining_time']:.0f} 秒

## 先行列車の状況
- 先行列車の遅延: {f['forward_train_delay']:.0f} 秒（正＝先行が遅れている）
- 先行列車が自列車の次駅を発車済みか: {f['forward_departed_next'] or '不明'}
- 先行クリア残時間（先行が自列車の次駅を発車するまでの残り秒数、算出済み・因果的）: {f['forward_clear_remaining_time']:.0f} 秒
- 先行の次駅での観測遅延（標準停車時間を超えて停車中の秒数。0なら標準どおり／未観測。
  この値が正なら「先行が次駅で延着中」と自列車が観測している）: {f['forward_observed_delay']:.0f} 秒

## 後続列車の状況
- 後続列車までの距離: {f['backward_distance']}
- 後続列車の速度: {f['backward_speed']}
- 後続列車の遅延（自列車に起因する分）: {f['backward_delay']:.0f} 秒
"""
    return f"""
# 現在の走行状況と運転操作
- 走行フェーズ: {f['phase']}
- **現在の運転操作**: {f['current_notch']} （継続時間: {f['holding_time']} 秒） <-- 【重要】この操作の適切性を評価してください。
- **直前の運転操作**: {f['prev_notch']} （継続時間: {f['prev_notch_duration']} 秒） <-- 【重要】保持時間が共に5秒未満の場合、ノコギリ運転のルールを確認してください。
- ノッチ変更による加速度の変化量（notch_jump、算出済み。小さいほど乗り心地が良い）: {f['notch_jump']:.2f} km/h/s

## 速度と基準（すべて算出済み。LLM自身が再計算してはならない）
- 現在速度: {f['current_speed']:.1f} km/h
- 線路条件のATC現示: {_f(f['atc_now'])}
- **実際のATC現示（signal_speed、先行列車を含む。超えたら即0.0）**: {_f(f['signal_speed'])}
- **運転上の目標上限（v_ceiling、前方の現示低下点を先読み済み。超過は段階減点）**: {_f(f['v_ceiling'])}
- **このモードでの目標速度（v_target）**: {_f(f['v_target'])}
- **維持帯**: {_f(f['band_lower'], unit='')} 〜 {_f(f['band_upper'])} ／ 使用ノッチ対: {f['band_notch_pair']}
  （この勾配で速度を保てるノッチ: {f.get('hold_notch', '—')}）
- 標準運転曲線の速度（v_std、省エネ・定時の基準曲線）: {_f(f['v_std'])}
  （現在速度との差: {_f(f['v_std_deviation'], sign=True)}。正＝標準より速い）
- 定刻に必要な巡航速度（schedule_speed、**モード判定はこちらを使う**。減速フェーズでは該当なし）: {_f(f['schedule_speed'])}
- この駅間で出せる最大のATC現示（section_cap、**モード判定の比較相手**）: {_f(f['section_cap'])}
- 惰行への切替目安（required_speed。現在速度で間に合うなら現在速度が出る）: {_f(f['required_speed'])}
- 機外停車を避ける加速上限（target_speed_no_stop）: {_f(f['target_speed_no_stop'])}
- 車間を均す目標速度（target_speed_spacing）: {_f(f['target_speed_spacing'])}

## 停止位置
- 次駅までの残り距離 {f['dist_to_next_station']:.1f} m（マイナスは過走）に対し、定時到着まで残り {f['time_to_next_station']} 秒
- 駅停車に必要な制動距離: {f['req_stop_dist']:.2f} m
- 停止余裕距離（delta_stop = dist_to_next_station − req_stop_dist、算出済み。制動開始判定に使用）: {f['delta_stop']:.2f} m

## 勾配とノッチの効き（算出済み）
- 現在の勾配: {f['current_gradient']} ‰
- **惰行時の加速度（coast_accel）**: {f['coast_accel']:+.3f} km/h/s
  （**正なら惰行しても速度が上がる＝惰行では減速できない**）
- 力行時の加速度（power_accel）: {f['power_accel']:+.3f} km/h/s
- 前方の勾配情報: {f['next_gradient_info']}

## 前方のATC現示
- {f['limit_drop_ahead']}

## 運行状況
- この駅間の遅延: {f['delay']} 秒 ／ 通算の遅延: {f['total_delay']:.0f} 秒
- 終着駅まで: 残り {f['stations_remaining']} 駅・{f['total_remaining_time']:.0f} 秒

## 先行列車
- {f['forward_info']}
- 先行列車の遅延: {f['forward_train_delay']:.0f} 秒（正＝先行が遅れている）
- 先行列車が自列車の次駅を発車済みか: {f['forward_departed_next'] or '不明'}
- 標準運転間隔（先行が自列車より何秒先に出発したか）: {f['standard_headway']:.0f} 秒
- 先行クリア残時間（算出済み・因果的）: {f['forward_clear_remaining_time']:.0f} 秒
- 先行の次駅での観測遅延（標準停車時間を超えて停車中の秒数。正なら延着を観測中）: {f['forward_observed_delay']:.0f} 秒

## 後続列車
- {f['backward_info']}
- 後続列車までの距離: {f['backward_distance']}
- 後続列車の遅延（自列車に起因する分）: {f['backward_delay']:.0f} 秒
"""


OUTPUT_FORMAT = """
# 出力指示
"mode" には、Step3で判定した運転モード
（"normal"／"delay_recovery"／"anti_mid_stop"／"spacing"／"hold_at_station" のいずれか）を必ず記載すること。
"checks" には、Step1〜Step2・Step4〜Step9それぞれの判定（"OK"／"NG(補足)"／"対象外"）を
必ずすべて記載すること（キーの省略不可）。
"reason" には、各Stepの説明を並べるのではなく、reward を決定づけた要因のみを
50〜100文字程度の一文で簡潔に記述すること。
reward は 0.0〜1.0（0.1刻み）で出力し、"reason" の内容と整合させること。
なお "immediate_zero_rule" が "NG" の場合、reward は必ず0.0とすること。

{
  "mode": "delay_recovery",
  "checks": {
    "immediate_zero_rule": "OK",
    "atc": "OK",
    "stop_position": "対象外",
    "gradient": "OK(下り・惰行で加速)",
    "phase": "OK",
    "sawtooth": "OK",
    "train_interval": "対象外",
    "punctuality_energy": "OK"
  },
  "reason": "急な下りで惰行では減速できない局面。勾配ブレーキで現示直下に速度を保っており遅延回復として適切。",
  "reward": 0.9
}
"""


def build_prompt(features):
    """LLMへ送るプロンプト全文を組み立てる。"""
    return SYSTEM_INSTRUCTION + build_current_status(features) + OUTPUT_FORMAT
